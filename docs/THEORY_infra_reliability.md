# 基础设施可靠性理论：成熟范式的采纳、映射与边界

> 2026-07-12。缘起：yzg 十轮验收（run5-run10）约 80% 失败源于测试基础设施
>（床/框架/取证/通道），而非知识或语义——K 理论范式在该域**错配**（用户两轮质疑
> 定谳：自创认识论框架从第一性原理现推标准问题，每个解都造得糙）。本文档把该域
> 交还给已成熟 15+ 年的学科——**测试可靠性工程**（test reliability engineering），
> 按文献建立问题分类、检测器谱、环境谱系、运营模式与契约测试，并与 K 理论/行动论
> 划清接口。姊妹文档：`THEORY_k_state_machine.md`（语义域，见其 §2.8 边界声明）。

## 1. 范式定位：两个问题域，两套武器

| | 语义正确性域 | 基础设施可靠性域 |
|---|---|---|
| 核心问题 | 编译出的 excel 是否忠实验证意图 | 跑出来的信号是否可信 |
| 失效形态 | 假验证/意图漂移/知识过期/欠定 | flaky/污染/顺序依赖/取证失真 |
| 理论 | K 状态机（三合取/ask/判例/行动论）——**独创，文献无现成解** | **文献有 15 年积累的标准解，禁止现推** |
| 本项目实绩 | 达标（dongkl→yzg 迁移 23/26 首跑；成本 ¥47→17.6） | 十轮 80% 失败集中于此；补丁式响应 |

**采纳纪律**：本域任何新问题，先查本文档 §2 分类学对号——有标准解的按标准解做
（允许适配，禁止从零现推）；真正的新形态才升理论讨论。这是 engine-verify-loop
理论层的前置步骤。

## 2. 问题分类学（Luo et al. FSE'14 十类 × 本项目实证映射）

学界十类根因（[实证研究](https://mir.cs.illinois.edu/lamyaa/publications/fse14.pdf)，
Apache 项目 201 修复提交；跨语言分布：[Async Wait ~45%、Concurrency ~20%、Order
Dependency ~12%](https://arxiv.org/pdf/2101.09077)，Python 生态 Order Dependency 居首；
[SAP HANA 工业研究 2026](https://arxiv.org/html/2602.03556) 印证工业分布相近）：

| # | 类别 | 定义 | 本项目实证 | 已有对应物（评估） | 差距 |
|---|---|---|---|---|---|
| T1 | **Test Order Dependency** | 结果依赖执行序（污染者 polluter 改共享态未复位，受害者 victim 读到脏态）。**角色分域（R5-Q2/Q7）**：polluter 的卷面责任归 K 域可修（归因反馈自清），victim 的暴露归本域结构治 | 233 vlan 迁移→9 案超时（run7/9/10 三轮，victims 计 40/73=54%——本项目第一根因，文献分布不可外推 R5-Q9）；644 保存族单跑过连跑挂 | 矛盾谓词（∃pass@subset∧fail@delivery）=OD 的**结果侧**检测；床账快照 diff=污染的**状态侧**检测（PolDet 形态）；rerun_isolated=隔离验证 | 无污染者-受害者**配对**（谁污染了谁——iFixFlakies 的核心输入）；无主动重排检测（iDFlakies 形态：改变卷序探测 OD 案） |
| T2 | **Async Wait** | 用 sleep 赌异步完成 | 实测暴露面 **17 步/7 案**（R5 清点，远小于文献先验——优先级降至末位） | 无 | 正解=轮询显式条件替代固定 sleep（emit 层「等待-验证」对）；按暴露面缓做 |
| T3 | **Infrastructure/Network** | 环境抖动、连接瞬态 | 探针单次 % Invalid（run8 床检误报源）；LLM 端点抖动 | probe_failed 判定；is_transient_error（传输契约层）；瞬态归因（复现即非瞬态） | 瞬态案缺 **quarantine 运营**（§5） |
| T4 | **Resource Leak** | 资源创建未释放累积 | segment `.conf.tmp` 尸体；SDNS 配置文件残留 | bed_check 残留探针+cleanup_refs | 已覆盖较好；增长机制待行动判例化（序③） |
| T5 | **Time** | 依赖真实时钟 | 框架 `system date` 写死；跳板机时差（run-identity 用同机时钟规避——已正确处理） | min_epoch/stale_log 门 | 基本覆盖 |
| T6-T10 | Concurrency/IO/Randomness/Float/Unordered | —— | 上机互斥锁已根治并发互踩;RR 轮询的随机性属**被测语义**（K 域的 claim_kind 处理,不是 flakiness） | 互斥锁/verifiability 工具 | 注意界线:被测系统的随机行为归 K 域,测试自身的随机性才归本域 |

**本项目特有的第十一类（文献框架的扩展）**：**取证失真**（evidence misattribution）——
per-case 采证切分错位（248 拿到 233 的 RouterA 输出，#67）。文献近邻是 test artifact
attribution；[Google 2026 已有 LLM 诊断集成失败的工业实践](https://arxiv.org/pdf/2604.12108)，
其前提同样是 artifacts 归属正确。见 §7。

## 3. 检测器谱（对标文献工具的形态，适配到床/卷面）

- **状态污染检测**（[PolDet 2015](https://experts.illinois.edu/en/publications/reliable-testing-detecting-state-polluting-tests-to-prevent-test-)
  形态：执行前后共享状态快照对比，26 项目抓出 324 污染者）→ 本项目床账快照 diff
  即其床版；**升级方向：粒度从批级到案级**（per-case 前后快照过重——上机时长×26；
  折中=可疑案定向快照：矛盾案/保存族案才做案级 diff）。
- **OD 检测**（[iDFlakies 2019](https://philmcminn.com/publications/parry2021.pdf) 形态：
  重排执行序对比结果）→ 我们的矛盾谓词是被动版（撞到才知道）；主动版=对交付卷做
  一次**逆序/随机序子集复跑**作为交付前烟测（一次上机换 OD 提前显形——成本与收益
  待评估，不默认开）。
- **自动修复**（[iFixFlakies 2019](https://www.researchgate.net/publication/335091960_iFixFlakies_a_framework_for_automatically_fixing_order-dependent_flaky_tests)
  思想：从既有 cleaner 测试提取复位代码）→ 我们的对应=归因反馈驱动 233 长出案尾
  自清（已走通）；判例化后同形态自动建议清理步（序③）。
- **污染者-受害者配对**：diff 实体 × 受害案触碰实体的交集（机械可算）——diagnose
  位（X3）的输入结构化，比自由聚类更强。
- **第二文献支（R5-Q10）**：网络运维的 **config drift management / golden config**
  （NETCONF 生态的漂移检测-恢复工具族）——对「设备配置漂移」比 PolDet（进程堆污染）
  更同构：床账≈drift detection，L3≈golden config restore。两支文献结论一致
  （快照-对比-恢复），互为印证。

## 4. 环境谱系（hermetic 阶梯——治本方向）

[Google 的教科书结论](https://abseil.io/resources/swe-book/html/ch14.html)：共享可变
环境是 flakiness 温床，[hermetic ephemeral SUT](https://carloarg02.medium.com/how-we-use-hermetic-ephemeral-test-environments-at-google-to-reduce-flakiness-a87be42b37aa)
（每次跑起一次性隔离环境）是治本。本项目的阶梯与现状：

| 级 | 形态 | 本项目 | 升级代价/收益 |
|---|---|---|---|
| L0 | 共享可变物理床+顺序执行 | **现状**（文献反模式；十轮问题的物理温床） | —— |
| L1 | +状态守恒（快照/账/批后收敛） | 床账已落地（X11）——L0.5 | 已付 |
| L2 | +per-run 命名空间（案/批专属对象名前缀,冲突面收窄） | worker md 已倡导 case-unique artifact names（C 层） | 机械门化=emit 检查对象名唯一性,低成本 |
| L3 | +床快照/恢复（设备配置整体 save/restore per batch） | **原语族已查实（R5-Q3）**：`clear config all/primary/secondary/factorydefault/default/file`（手册 36+ 处）——待真机验证 save/restore 配对语义。INV-9 约束：批前基线快照→批后恢复到基线（基线含他人存量原样），**绝非 factorydefault** | 中成本高收益；54% T1 的结构性根治 |
| L4 | ephemeral SUT（vAPV 虚拟设备,每批新起） | 未评估（产品是否有虚拟形态/license/资源） | 高成本,根治 |

**裁决待做**（工程评估,非理论）：L3 可行性查证是下一个最高杠杆动作——若设备支持
配置基线一键恢复，床账的角色从「恢复机制」退化为「审计机制」，T1/T4 全谱一次性
根治。

## 5. 运营模式（flaky 不清零，管理它）

文献共识：flaky 无法归零（[基准报告](https://testdino.com/blog/flaky-test-benchmark)、
Google ~16%），成熟做法是**运营**：

- **Quarantine（隔离区）**：判为瞬态/基础设施类失败的案，进隔离区状态——不占重编
  轮次、不污染交付信号、不反复烧归因；隔离区案单独小批复跑仲裁，恢复即出区。
  **与 suspended 分立（R5-Q4）**：suspended=用户主权（挂起/恢复归人）；quarantine=
  引擎权限内的自动态（瞬态判定机械、非终态、用户可见可召回——(26) 合规：信息与
  权限均在引擎侧）。需新状态与自动进出机械化。
- **Flake 率指标**：per-批 flaky 占比（瞬态归因数/总 fail）进 DS-4 型监测——趋势
  恶化=床/框架健康度报警，早于崩盘。
- **重跑预算**：瞬态仲裁复跑有全批预算上限（资源规则），防瞬态案吃光上机额度。

## 6. 契约测试：黑盒框架的行为钉住（替代「K_infra 判例」的正确形态）

框架是祖传黑盒，其暗行为（0.8 相似度模糊匹配吞命令、per-case 采证切分、IP 记账
恢复、清理只复位 slb/sdns 对象）此前散落在 CLAUDE.md/记忆/代码注释——**人和 Claude
可读，引擎不可执行**。成熟形态是 [Michael Feathers 的特征化测试](https://blog.nimblepros.com/blogs/characterization-tests-with-snapshot-testing/)
（characterization tests：钉住遗留系统的**实际**行为而非应然行为）+
[消费者驱动契约](https://microsoft.github.io/code-with-engineering-playbook/automated-testing/cdc-testing/)
（CDCT：以消费方的期望为契约）：

- **对 mirror 可离线跑的行为**（get_similar_function 的 0.8 吞命令、found/not_found
  的 DOTALL 语义、切分标记形态）：写成 pytest 特征化测试直接跑 mirror 源码——框架
  升级换 mirror 时**契约测试先行报警**，暗行为漂移不再靠撞。已有雏形：恒真断言门
  从 mirror 语义推导即此思想，但未组织成「框架契约测试套件」。
- **对必须上机的行为**（HA FIP 与静态同址、清理范围的真实边界）：契约=explore 级
  小实验卷（一次性上机验证一个行为断言）+footprint device_verified 记载——已有通道，
  纳入本框架命名管理。
- **契约第 0 条：mirror 同步锚（R5-Q5，重要遗漏修补）**——契约测试钉的是 mirror，
  mirror 与跳板机真机框架可能漂移；**所有 mirror 推导物**（恒真断言门/found 语义/
  切分契约）共享这个此前未声明的单点假设。锚=跳板机框架文件 hash vs mirror hash
  对账（定期或 bed_gate 级），失配=全部 mirror 推导的门与契约进入可疑态并呈报。
- **主动考古纪律**：撞坑后修复必须回答「这个暗行为能否特征化测试钉住」——能而不钉
  =下次换个形态再撞（engine-verify-loop 实现层新增检查项）。

## 7. 证据完整性（取证链的可信性——(27) 的域内展开）

判断质量的上限是证据质量。取证链（设备回显→框架日志→staging 文件→digest 切分→
brief）每一跳都可能失真（#67 实证：切分错一格，归因 fork 拿邻案证据讲出灵异故事）。

- **归属一致性门**（机械，通用形态 R5-Q6）：任何取证附件中的**执行目标**（命令+
  参数）与卷面对应步 G 列机械比对——dig @IP 是首例不是全部（APV 会话/show 回显
  归属同理）；不一致 → 「证据可疑」事实 + brief 如实声明「取证疑似错位，勿采信
  该附件」——引擎自己发现自己的证据坏了（#67 的修复形态）。
- **多源交叉**：同一事实的框架日志面与会话录面（smoke log vs RouterA.txt）互证，
  单源孤证降权。
- 原则：**取证器与审计器同样要被审计**（公式 (18) 的补全——审计器权威成立的前提
  是其输入可信）。
- **过程链是三条的（2026-07-12 R10 补齐第三条）**：①下游取证链（设备→证据
  →brief，本节）；②上游框定链（brief→LLM 注意力→判断，K (37)——PROMPT 布局
  纪律+X8 载荷按引用）；③**问询链**（面板→人→答案→引擎消化，K (41)）——run12
  实测两起失真（长文本传输截断使「停止」被语义兜底误判 retry；批间按键竞态使
  3 案误挂起），用户答案是全系统权威最高的信号却曾是唯一无完整性门的信号；
  机械门=决策 echo-back（消化后的决策复述回面板/收口卡供用户核对——归属一致性
  门的意图链版）。#67（①错位）、035413（②劫持）、run12 面板事故（③失真）
  是三条链各自的实证锚，同等设防。

## 7.5 信任根登记册（2026-07-12 立——把「下一个 Q5」从对抗轮撞见改成盘点发现）

方法论观察：历次对抗轮分量最重的修正几乎全是同一型——**未声明的信任根**（所有
下游推导共享的单点假设，无人声明）：mirror 同步锚（R5-Q5）、沙箱即锚可信性根
（K-C5）、build 锚源②失真（568vs585）、取证归属（#67）、跳板机时钟（run-identity
的同机比较前提）。安全学科的成熟对应=TCB 枚举。纪律：**新机械门/推导链立项时
必须登记其无条件信任的前提**；登记项逐个配锚（可对账的）或显式声明为根（不可
对账的）。存量登记（首批，待逐个配锚）：

| 信任根 | 依赖它的推导 | 锚/状态 |
|---|---|---|
| agent 沙箱完整性 | 全部锚差监控（run_ts/verified_runs 不可伪造） | 声明为根（K-C5） |
| mirror ↔ 真机框架同步 | 恒真断言门/found 语义门/切分契约全族 | I3 hash 对账（队列第 3 位） |
| 手册文件版本纯度 | 类型图/命令存在性门/权威序判定 | 待配：手册目录 hash+版本锚（S1 捎带） |
| MinerU 抽取完备性 | 手册层一切文法推导（截断=静默漏边，覆盖上限 98.8%） | 声明为根+门形态补偿（S6 呈报不硬拒） |
| 跳板机时钟 | run-identity（min_epoch/stale_log） | 同机比较设计已内置（无跨机时差依赖） |
| show version 自述 | build 锚/版本族判定/bed_gate 体检 | 源①优先已裁（源②提交值失真实证） |
| network_topology.json | env_facts 全部投影/可达门 | 声明为唯一事实源（换床只改 JSON） |
| domain_grammar.json provenance | 文法层全部判定 | 每条带手册出处（既有纪律） |
| facts.jsonl `_pid`/append-only | 全部派生视图/DS 度量 | 依赖沙箱根+僵尸确杀纪律 |
| 生产信号流与测试隔离 | k_signals/DS 曲线不被 pytest 污染 | **待修**（monitor #5：38 条测试信号直写生产） |

## 7.6 行为层算子成本前提（2026-07-12 声明——隐藏假设显式化）

本文档 §6 契约小实验、K §3.2 行为层实验、S §5 探针序**共同假设**「设备探针/
实验算子廉价可用」。实证边界：串行 console/跳板机直连时 ≈1 次交互/案（K §6.5
锚定值）；但走 xlsx 全管道做 grounding 曾实测 ¥7.87/18min（DIAG_035413）——
**差一个量级**。工程含义：任何「编写期/归因期设备实验」类构件立项时必须先核
其实际通道成本（轻量工具在位与否），不得默认引用乐观锚定值；工具不在位时
「先造轻量通道」是该构件的前置工程项而非可跳过项。

## 8. 与 K 理论/行动论/S 代数的接口

**S 代数（`THEORY_target_system_algebra.md`，2026-07-12 后置补齐的第三角）**：本文档
多个构件在彼处获得数学根源——污染者-受害者配对=实例图上闭包（(31) 传播定理，替代
自由聚类）；持久化家族=状态面写（(29)-G6 数学定义，通道枚举退役）；床态快照能力
上限=show 投影核（彼 §5）；「底层写最危险」=(31)×(32) 交；清理正解=设备自带 no
逆元（配置面）而非模板/LLM 生成。

- **床账**：既是 (25) 通路一的实现（行动论），也是 PolDet 形态的污染检测（本文档
  §3）——同一构件，两个理论视角，无冲突。
- **瞬态归因**：判定（复现即非瞬态）属本域；判定后的处置走 K 域的归因/重编流。
- **界线判据**：失效可被「换一个完美无缺的卷面」消除 → 语义域（K）；不能 → 本域。
  233 案两面都有：它的卷面该自清（K 域反馈可修），但「共享床上任何案都可能被前驱
  污染」是本域结构问题（hermetic 阶梯治）。
- **随机性界线**（§2 T6 注）：被测系统的随机行为（RR 分布）是 K 域的可验证性问题；
  测试自身的不确定性才是 flakiness。

## 9. 差距清单与优先级（R5 数据驱动重排，与 §11.3 一致；**执行队列唯一权威=DESIGN_v8 §15.3 队列 v3**）

1. **L3 真机验证**（§4）——原语已查实，54% T1 的结构性根治，一次小实验定治理策略；
2. **#67 归属一致性门**（§7 通用形态）——E11 系统性 8.5%，归因地基；
3. **mirror 同步锚**（§6 第 0 条）——所有 mirror 推导物的单点假设收口；
4. **quarantine 机械化**（§5，与 suspended 分立）——T3 12%；
5. **框架契约测试套件**（§6）——存量暗行为工程化；
6. OD 主动检测/污染配对（§3）——diagnose 位（序②）的结构化输入；
7. sleep 治理（§2 T2）——暴露面 17 步，末位。

## 10. 证据索引（本项目实证 → 文献概念）

| 实证 | 文献概念 |
|---|---|
| 233 vlan→9 案超时（三轮） | state-polluting test（PolDet）+ order dependency（T1） |
| 644 保存族单跑过连跑挂 | order-dependent flaky（iDFlakies 检测对象） |
| run8 探针单次 % Invalid | infrastructure flakiness（T3） |
| 248 采证拿到 233 的 dig 输出 | evidence misattribution（§7，文献框架扩展） |
| 框架 0.8 模糊匹配吞命令 | 未钉契约的遗留系统暗行为（Feathers 特征化测试的对象） |
| 卷面 sleep 3 依赖 | async wait（T2，文献第一大根因） |
| segment .conf.tmp 尸体 | resource leak（T4） |
| 共享床+顺序执行 | 非 hermetic 反模式（Google ch14） |

## 11. 对抗检查 R5（2026-07-12，十攻 + 全量实验数据定量检验）

**数据基座**：run5/7/8/9/10 全量备份（facts+last_run+卷面），73 个 fail 逐案机械分类
（脚本 infra_audit：dig 归属比对/归因层/污染家族签名）。

### 11.1 定量检验结果（修正三个宣称，验证两个）

| 宣称 | 检验结果 |
|---|---|
| 「~80% 失败在基础设施域」 | **修正为实测 72%**（53/73；other 7 个未分类人审后区间 72-81%）。量级成立，数字数据化 |
| T1 污染的严重性 | **强于宣称**：单一根因（233 家族）40/73=**54%**——文献里 OD 仅第三（12%），共享物理床把它放大 4 倍为第一根因。**本项目实测分布（T1 54%/T3 12%/E11 5% vs 语义域 17%）与 Java 生态（Async 45%/Conc 20%/OD 12%）截然不同——文献分布不可直接外推，hermetic 优先级比文献语境更高**（Q9 裁决） |
| sleep（T2）暴露面 | **弱于文献先验**：全卷面仅 17 步/7 案——§9 优先级降至末位（Q8 裁决；文献第一根因在本域形态下暴露面有限） |
| 取证失真（E11）是否孤例 | **验证为系统性**：4/47 可判样本（8.5%），run8/9/10 连续出现——#67 优先级坐实 |
| 两域分立的因果 | **run10 提供翻转实验**：存量清偿+床账后，T1 从 14→**0**、总 fail 20→9、K_semantic_V 从 2→5 成为主导——**基础设施修复让语义域真问题浮出**，run5（首跑净床，fail 全为 K 域 3 个）同构印证。两域理论获得干预性证据，非仅相关性 |

### 11.2 攻击与裁决（Q1-Q10）

| # | 攻击 | 裁决 |
|---|---|---|
| Q1 | 80% 是拍的不是数的 | 修正：72%（见上）；文档以数据替换估值 |
| Q2 | 233 同时是污染者与受害者，T1 归类混角色 | 修正：T1 细分 **polluter**（233 自身的失败是其方法问题——VLAN 载体在无 tag 路径的床上不可验，归 K 域 method_vs_implementation）与 **victims**（9 案，纯 infra 暴露）。角色分域 |
| Q3 | L3 假设「设备有基线恢复原语」未证 | **站住且升级**：手册查实 `clear config all/primary/secondary/factorydefault/default/file` 原语族（36+ 处记载）。L3 从待查证升级为**可行待真机验证**。INV-9 约束：形态必须是「批前基线快照→批后恢复到基线」（基线含他人存量原样），绝非 factorydefault |
| Q4 | quarantine 与 suspended 语义冲突/越权 | 修正：**两个状态分立**——suspended=用户主权（挂起/恢复归人）；quarantine=引擎权限内（瞬态判定机械、非终态、用户可见可召回，(26) 合规:信息与权限均在引擎侧）。文档 §5 措辞更正,不复用 suspended |
| Q5 | **契约测试钉的是 mirror,mirror 与真机框架可能漂移——契约有效性无锚** | **中,重要遗漏**：补「mirror 同步锚」为契约套件第 0 条——跳板机框架文件 hash vs mirror hash 对账（bed_gate 级或定期）;失配=全部 mirror 推导的门与契约进入可疑态。此前所有 mirror 推导门（恒真断言/found 语义）共享此单点假设而无人声明 |
| Q6 | 归属一致性门只覆盖 dig@IP 一种形态 | 修正：门推广为**执行目标交叉验证**通用形态——任何取证附件中的执行目标（命令+参数）与卷面对应步 G 列比对;dig 是首例不是全部 |
| Q7 | 「换完美卷面能否消除」判据分类不唯一（233 两域都沾） | 修正：按**角色**精化——作为污染者的卷面责任（K 域可修）与作为受害者的暴露（infra 域结构治）分开归类;一个失效可以两域各记一半 |
| Q8 | async wait 清点缺失 | 已做（17 步/7 案）,优先级降 |
| Q9 | 文献分布外推风险 | 修正：见 11.1——本项目分布自测,与文献显著不同,结论反而强化 hermetic |
| Q10 | 遗漏更贴近的文献支：网络运维的 **config drift management / golden config**（NETCONF 生态、drift detection 工具族） | 中：床账≈config drift detection、L3≈golden config restore——网工领域对「设备配置漂移」的标准实践比 PolDet（内存堆污染）更同构。补为 §3/§4 的第二文献锚;两支文献结论一致（快照-对比-恢复）,互为印证 |

### 11.3 修订后的优先级（§9 重排，数据驱动）

1. **L3 真机验证**（Q3 升级：原语已查实——54% 的 T1 可被结构性根治，床账退化为审计;一次小实验定乾坤）;
2. **#67 归属一致性门**（E11 系统性 8.5%，通用形态 Q6）;
3. **mirror 同步锚**（Q5：所有 mirror 推导物的单点假设，一条 hash 对账收口）;
4. **quarantine 机械化**（T3 12%）;
5. 框架契约套件（存量暗行为工程化）;
6. OD 主动检测/污染配对（diagnose 输入）;
7. sleep 治理（暴露面 17 步，末位）。
