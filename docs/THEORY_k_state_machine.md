# 理论：用例正确性的三合取模型与 K 状态机

> 状态：理论基座 v1.1（2026-07-13 形式化加固批）。本文沉淀 2026-07-08/09 推导 + 对抗性反驳后的存活结论，
> 2026-07-13 两轮清算：①中等力度校准（引用校正、单域/极小样本点估计表整体删除、丢失脚本定量降定性）；
> ②形式化加固（响应外部审计五漏洞：中心公式②的 ≅proj 越界、信息量香农测度未定义、单调律集合外延误杀、
> (43)(44) 未定义态上求值破坏严格性、§4 中心论断 f 无度量不可证伪——逐一重写为可判定/可证形式）。
> 审计两处提议被**批判性拒绝**并记录在案：ρ_poison(调用图出度)/ρ_complete(意图满射比) 依赖的 quarantine
> 字段与 φ(D) 统一提取器均未实现，写成"可计算代理"= 用新伪可算换旧伪可算，一律标注「规格·待建」；
> 群论逆元律 δ(s,g∘g⁻¹)=s 对 CLI 过强——`no X` 是**对象级复位算子**非精确群逆（复位后子状态可与原态
> 不同），τ 责任集与 inverse_forms 使用复位语义，不引群公理。
> 纪律：本文只保留三类内容——**定义/公理**（规定性，无真值负担）、**有推导的定理**（从定义可证）、
> **有盘上证据的事实**（facts/_pid/测试锚可复核）；经验断言一律带实现状态或证据锚,无锚即删。
> 编号纪律：章节号与公式号**只增不改**（跨文档/代码引用稳定，0ba63f4c「零重编号」先例）——
> §2.1–2.4 为保号空缺（从未存在,§2 正文即其内容）;(28)-(34)(38)(39) 居 S 文档、(14) 降注记
> 后编号位保留,均非缺漏。
> 用法：任何新机制立项前先过本文 §2 三合取与 §4/§5 状态机——落得进=不需要新概念；落不进=对理论的反例，先修理论。

---

## 1. 核心对象与记号

| 记号 | 含义 | 访问方式 |
|---|---|---|
| **R\*** | 设备真实语义（ground truth） | 只能经设备访问，且**分层可查询**（§3.2） |
| **R_spec** | 设计规格（手册/配置文档声称的语义） | 文本，可离线读 |
| **K** | 知识库 = **K_is ⊔ K_ought**（双层，§2.6）：K_is = 先例卷可跑形态 ∪ CLI 手册 ∪ footprint ∪ known_defects；K_ought = spec/design ∪ 需求单 ∪ bug 裁决史 ∪ 人写历史 excel 的验证意图 ∪ 欠定决策史 ∪ app 手册 | 检索；K_is 是对 R\* 的部分近似，K_ought 载**异步的人类意图** |
| **C = (D, P, E)** | 测试用例 = 描述（意图）+ 配置过程 + 预期结果 | 编译产物 |
| **O** | 上机观测 = Exec(P) + ε | ε 为观测通道噪声（实证存在，§3.1 第④类） |
| **锚 a** | (build 指纹, 实证时间 run_ts, 血统 lineage) | lineage ∈ {manual, device, precedent}；工程 schema 以 autoid 承载 lineage（autoid≠∅⇒device），见 §5.3 |

D 的本质：**人对意图的有损自然语言投影**，混叠"意图本体"与"实现提示"（608 的 disable 是提示、down 是意图）。但对抗反驳（§9-C2）表明：不应对 D 做集中规范化——分布式解码的多样性冗余（570 解对/608 解错→即时写回流动）优于单点解码的审计性。

## 2. 正确性三合取（中心公式）

一条用例**正确**是三个合取同时成立（2026-07-13 形式化加固：②从 `E ≅ proj(D)` 重写——
≅ 在未定义两空间等价结构时是概念越界；proj 的域是非结构化意图空间，无定义）：

```
Sound(C) ≝ (P ⊩ D) ∧ (φ(D) ⊆ closure(E)) ∧ (R*(P) ⊨ E)
            ─────①      ────────②              ─────③
  ① 配置实现描述的场景（不多配、不少配、不变形）
  ② 意图特征覆盖:φ: D → F_intent 把意图解析为显式行为特征集（如
     {AAAA_lookup, CNAME_pool_hit}），closure(E) 为断言集在目标系统代数下
     可判定导出的行为边界闭包——覆盖关系 ⊆ 是集合论良定的（不弱化、不翻转、不越权）
  ③ 设备执行配置的行为满足预期
```

φ 的实现状态（诚实标注）：**无统一提取器**（§2.5.3-C1 粒度问题）；已实现的可判定投影=
intent_record_type_gap（记录类型词面显式子集）、τ 覆盖判定核（tau_coverage）、
expected_hits 状态机——每个都是 φ(D)⊆closure(E) 在一个特征子域上的机械检查。

**上机验证只测合取③**：`Verify(C) = [O ⊨ E]`，O 是一次执行的具体 trace。

> **验证盲区定理（构造性证明）**：③ 的任意多次成立不蕴含 ①∧②。
> 证明=已存反例（PASS 卷 ∧ ①或② 被违反的实录）：644 删 AAAA 断言（②弱化，PASS）；
> 首批 608/570 毒卷 E 反写（②翻转，PASS 且入库成"金标准"）；493 多挂 CNAME 池
> （①变形）；035373 not_found→found 迁就（②翻转）。∎
> 结构原因：[O⊨E] 是关于单条 trace 与 E 的判定；①② 是 D、P、E 之间的关系，
> 不出现在该判定的任何前提里——重复③只积累对③的证据，对①②的证据量恒为零。

**盲区的分布（两轮修正后）**：21 张交付卷机械保真审计查出 2 案漏维——644（压力路径：
frozen override 删断言）与 **373（R1 首轮即漏**：复杂流程 case 的 AAAA 半维静默丢失，两批复发）。
即失真有两种机理：压力驱逐（重编时删掉会 fail 的覆盖）与注意力占据（首轮流程复杂时漏维）。
两者同被 intent↔卷面投影比对（词面显式子集）覆盖；压力路径另需**单调性守恒律**
（2026-07-13 形式化加固：理论形式从集合超集升级为逻辑蕴含——纯集合外延会把
「两条显式断言重构为一条等价组合断言」的合法重编误杀，语义强度未减而元素集不同）：

```
Monotonic(new | old) ≝ M(Asserts(new)) ⊆ M(σ(Asserts(old)))
   （M(E) = 断言集 E 在目标系统代数下的模型集;新卷的模型集是旧卷经等价变换 σ 后
    模型集的子集 ⟺ 新断言集逻辑上至少与旧断言集同强——删除/弱化必然使模型集变大,被拦）
```

模型集包含判定的**已实现近似**（充分条件,机械）：emit 单调门（compile_emit,
2026-07-09）比较**语义类集合**——观测动词类+DNS 记录类型集合不减（类级比较对
"断言改写但记录类型保全"的重构免疫,比原始字符串集合外延温和一级）;削减须
`coverage_reduction_reason` 显式声明放行+记 `monotonicity_violation` 信号。
该近似不完备（类内弱化检不出）——完备判定=②的全量 oracle,不可判定（§9-C1,
撞 grade 之死）;单调门是②在压力路径上的可判定投影。

**合取③的语境参数（2026-07-10 yzg 终验实证）**：③ 的完整形式是 R\*(P, ctx) ⊨ E——执行语境
ctx（卷面组成、执行顺序、**床态向量 B**——本机磁盘持久层/对端节点态/分区文件系统/床级
互斥态，2026-07-10 手册取证四通道）是量化变元，不是背景常量。ctx = (π, B)：π 是卷内排列
（只影响通道①），B 是跨案/跨设备/跨批的持久床态；锚差监控 Δ（公式7）的适用面由 build
一维扩到床态多维。"pass" 因此是**二元谓词
Pass(c, ctx)**；交付判据是 Pass(c, ctx_delivery)（整卷连跑）。把它投影成一元 Pass(c) 等于把
量词从「交付语境下成立」偷换成「∃ 某语境成立」——投影无损的充要条件是 **case 间独立性假设**
（per-case 清场 ⇒ 任意语境同结果）。该假设的边界在其自己的声明里：清场只清*运行配置*；
write file/net/all 的产物是*盘上文件*，跨案存活。yzg 保存族三案（668015/030/044）实证：
子集语境 pass ∧ 整卷语境 fail——∃-pass ≠ 交付-pass，投影在假设边界外失真。（锚注 2026-07-16：该族
主根因后判为 σ 链断裂+oracle 残差（窗口失真，§2.6.6/DESIGN §18.10）——此三案对 ctx 依赖的证据力
降位（部分 fail 可能是取证失真非真语境依赖）；ctx 定理的独立支柱=手册四通道持久语义、233 家族、
run13 再生毒源，定理本体不依赖此锚。）

**互扰消解推论（2026-07-10 渲染层设计两轮对抗定形——行动的理论根）**：设 δ(c) 为 case c
在 Λ（per-case 清场）之外存活的持久写集。case 间独立性假设的精确形式是 ∀c: δ(c)=∅。互扰的
**消解手段是复合施加**：案内自清理（c 在案尾清空 δ(c)——唯一使独立性假设重新成立的解，对全部
后继案同时生效）**∘** 排卷尾（兜底自清理覆盖不到的残余）。「接受单跑」**不在消解集合中**——它是
把 Pass(c, ctx_delivery) 偷换为 ∃-pass 的量词投影（本节上文的失真形式），只能作为用户显式止损
裁决存在，且必须标注不符交付预期、记入未通过卷。
**目标函数与终态（2026-07-10 三轮对抗定形）**：引擎目标 = 脑图每一案编译成真通过的自动化
用例。可导性定理保证 fail 有限步归到唯一根因；Ω 五类中**只有③产品缺陷是案侧无修法的类**——
故唯一非通过的正当终态 = 缺陷候选（四查+换形态坐实）；其余 fail 全是中间态，引擎必须继续
（挂起是资源界的暂停不是终态）。断点续跑的**已验范围（2026-07-13 run17@79 恒真校准）**：
恢复问询跨批再问、裁决落账、事实语义（suspended/resumed 配对、qid 幂等、facts 时序+_pid
归属）全部按设计工作——run17 六会话 333 事实逐 pid 审计可复核；**「恢复→重编→上机→翻正」
的完整闭环至今无一次走通的实录**（run15/17 两次都在面板交互层丢答案，见 (41)④），闭环有效性
是待验命题非结论。「接受单跑」不是引擎出口——只在用户主动输入时被接受。
**Ask 充要条件 → §2.6 Ask 定理（四稿定形，此处只留演化史）**：一稿三选项菜单（把定理退化为
投票）→ 二稿菜单生成器（仍是选择题）→ 三稿"意图权/资源权"二分+修法队列空门 → 四稿用户驳正
定形：**"手册 vs 实机不符"不是实验可裁的**——实验只能裁"差异存在"（实然），裁不出"谁代表真意"
（应然），三稿把它错标为"不问"。终版分界是实然/应然（is/ought），资源权归并入应然（所有权/授权
皆规范命题），完整定理、K_ought 检索次序、收敛律与对抗检查见 §2.6。三稿的有效构件保留：修法
队列空门（=实然手段试尽的机械面）、缺陷四查门、「接受单跑」仅收用户主动输入。yzg 实证支持：030 的终验翻挂恰发生在尾序前驱 015 换
卷之轮（CLI Ch20：clear sdns all 不清盘上文件，需显式文件级清理）——015 复合修法后 030 应随之
稳定，一个修法救两案（可证伪预测，下批检验）。
**推论（对验证盲区定理的补充）**：oracle 裁决的效力 = min(oracle 强度, 消费链最弱环节)。
上机是唯一语义权威，但其裁决若未被状态机消费（yzg：终验 3 fail 落盘后从未被读），
效力为零——盲区不仅在「测什么」（①②测不到），也在「测了之后谁听」。

## 2.5 信息量三分解：I(C) = I_G·e_G + I_E·e_E + I_V·e_V

### 2.5.1 定义（2026-07-13 形式化加固:废弃香农测度）

旧定义 `I(a) = -log₂ P(O⊨a | 行为无关假设)` 已删——参照分布 P 从未被公理化定义,
测度未定义则其上的对数测度彻底失效,"香农熵"在此只是修辞。改用**约束测度**
（有限状态空间上的计数测度,良定）：

```
设 S = 目标系统可区分的配置×行为状态空间（CLI 值域有界 ⇒ S 有限）。
I(a) ≝ 1 − |{s ∈ S : s ⊨ a}| / |S|      —— 断言排除掉的状态空间占比
   恒真断言: {s:s⊨a} = S → I = 0         （zero_info_assertion 原理的测度论本体）
   恒假断言: {s:s⊨a} = ∅ → I = 1 且无鉴别力（排除一切=断言与被测行为无关）
                                          —— 必崩门拦截对象
```

> **可算性诚实声明**：I 良定（有限空间计数）但逐点枚举不可行——本理论只消费其
> **可判定谓词**：I_V(a)=0（恒真族,从 mirror 窗口语义机械判定,✅ 必崩门六族）与
> 维度计数 dim_V。实际运转的是二值 {零信息, 正信息} + 维度计数,公式的价值是把
> zero_info 从启发式变成定义的推论,不是提供数值。

断言按观测算子性质归层（layer(a) ∈ {G, E, V}，机械判定已实现：observe_kind + F 列算子）。
设 e_G, e_E, e_V 为三层**相互正交的特征基**（e_i·e_j = 0, i≠j）：

```
I(C) = I_G·e_G + I_E·e_E + I_V·e_V ∈ ℝ³₊    —— 分层向量（正交直和,旧记号 ⊔ 系
                                                不交并算子误用于向量空间,已废）
```

**正交性的两条公理（旧"不可通约"的严格化）**：

1. **不可补偿（正交性推论）**：e_G·e_V = 0 ⇒ I_G 沿 e_G 轴任意叠加,在 e_V 轴上的
   投影恒为 0——配置检查写一百条也不产生行为验证信息。
   实证：588691 恒真族卷——G 段"信息丰富"、I_V = 0、当年照样 PASS 入库。
2. **用途不同**：V = **覆盖信息**（意图的行为验证，决定这条用例存在的价值）；
   G/E = **诊断信息**（fail 时定位根因层——分层判定树的"配置生效了吗"一步消费的正是 G 段）。
   G/E 信息量的价值在 fail 路径上兑现，V 的价值在 PASS 路径上兑现——两类信息在不同事件下变现，故不可交换。

### 2.5.2 覆盖判据与守恒律

意图 D 的信息量需求主要在 V 维（D 的每个行为断言张一个 V 维度）：

```
覆盖判据:  dim_V(C) ≥ dim_V(D)          （Cov = dim(span(A)∩B)/dim(B) 的直和表述）
守恒律:    重编/override 不得使 ΔI_V < 0  （单调性检查的信息论内容）
```

644 假 PASS 的定量刻画：意图 dim_V(D)=3（CNAME/A/AAAA 三类行为断言），删 AAAA 后 dim_V(C)=2 < 3——
但 Verify 只测 [O ⊨ E]（总体可满足性），对 I_V 维度亏损盲。**验证盲区定理的定量版**：
PASS 信号携带的信息 = "现有断言全部成立"，不携带 "断言集张满意图空间"。

### 2.5.3 可计算代理（理论量 → 已有估计器的映射）

不发明不可算的测度——每个理论量给出已实现/可实现的估计器：

| 理论量 | 可计算代理 | 实现状态 |
|---|---|---|
| I(a) = 0（零信息） | count_tautology / is_config_existence_check / 恒真锚点族 | ✅ 必崩门+原理层 |
| layer(a) | observe_kind + F 列算子 + matched_config | ✅ observe_ops |
| dim_V(C) | genuine_v_count（真 V 断言计数） | ✅ grade_extract |
| dim_V(D) | **无统一代理**（对抗检查 A2：expected 粒度不均——644 的"返回service ip"是一条 expected 却含 A/AAAA 两维，按条计数抓不住标志性违例）；可判定子集=结构化投影（记录类型/观测对象词面） | ⚠ 粒度问题是 C1 的另一面 |
| ΔI_V ≥ 0（守恒） | 重编前后 ObsCmds/Asserts 集合差（单调性检查） | ✅ 单调门（compile_emit，2026-07-09）：旧卷为基线，观测动词类+DNS 记录类型集合不减；`coverage_reduction_reason` 显式声明放行+记 `monotonicity_violation` 信号 |
| dim_V 的意图侧缺口 | intent_record_type_gap（词面显式子集） | ✅ 本轮（覆盖面见 C1） |
| K 条目信息量为负（误导） | stale/poisoned 标注（§5） | 部分 |

> K 条目同样有信息量与符号：verified 条目 I > 0（约束 R\* 的猜测空间）；stale/poisoned 条目 **I < 0**
> （主动误导——044572 被一条可疑缺陷记录带偏即负信息量变现）。
> **注记（原 (14)，2026-07-16 降位——C14「公式须有工程消费者，无消费者的降为注记」自裁）**：
> K 运维的目标可修辞化为「保持库的净信息量 Σ_k I(k) 为正」——I(k) 逐点不可算（自认）、Σ 更不可算，
> 无检测器/门/监测消费它；可操作内容即 §5 的 stale/poisoned 检测与消费端契约。修辞保留在此，
> 不再占公式位。

## 2.6 Ask 定理：实然/应然分界、K_ought 检索与收敛律（2026-07-10 四稿定形）

### 2.6.1 分界与权威

**实然（is）**：关于系统当前状态的可观测命题——设备对 X 回了什么、文档写了什么、卷面长什么样、
库里记载了什么。全部可由 {读文档, 查 K, 设备实验, 计算} 有限步决定（可导性定理管辖）。
**应然（ought）**：关于"该是什么"的规范命题——该测什么、该期望什么、该用什么方法验证、这个行为
算不算对、该不该动别人的资源。**实验永远建立不了应然**：它能证明"设备做了 Y"，不能证明"Y 是产品
该有的行为"——is-ought 鸿沟，正是 observe-then-assert 红线的理论本体。

**权威结构**：每个制品都是某人意图的投影——D=用例作者的测试意图；手册/spec/design=研发的产品
意图；R\* =实现（可偏离设计=缺陷）。应然的权威=意图所有者（用例作者/研发，经用户代理通道触达）。
两个投影冲突时，冲突在意图之间，只有所有者能仲裁；引擎硬选任何一边都是改写某人的意图（照实机写
断言=改写测试意图且缺陷从此测不出；照手册写=用例永挂在可能无错的设备上）。

**同形判据（开放列表，不枚举场景）**：手册↔实机、脑图预期↔上机结果、验证方法↔功能实现形态、
编排假设↔持久化语义……凡「A 源说 X、B 源说 Y、选边即改写某方意图」即中此判据。新冲突形态=
新数据实例，不需要新理论。

### 2.6.2 K_ought 检索先于 ask（异步的人先于同步的人）

自然人卡住时先翻 spec、翻旧用例、搜 bug 史——那是在检索**异步的人**；都翻不出才去敲门。故
ask 不是应然的第一通道，是**检索余项**：

```
应然决策 → 检索 K_ought（次序:spec/design → 人写先例卷意图 → bug/需求裁决史 → 欠定决策史）
  ├─ 命中且过采信门限(公式21) → 径直采用，继续产出（不问、不重复写回）
  └─ 缺失 / 记载互斥 / 记载与实机冲突 → ask（呈报-确认式，见 2.6.3）
```

**自然人行为公理（四条）**：①不硬编（不 observe-then-assert、不擅改预期）；②不撂挑子（不自判
env_blocked/escalated 终结——引擎无单方终结权）；③总结呈报差异与**自己的理解**去确认；
④确认后继续产出（目标函数：非产品缺陷一律产出 excel）。

### 2.6.3 题面唯一形态

> 差异呈报（双方引文+各自的锚）＋已检索证明（查过哪些 K_ought 源、各自结果）＋引擎的理解 Z
> ＋「理解正确按 Z 继续 ／ 纠正：___ ／ 确认为产品缺陷走候选单」

挂起/停止是**用户常驻特权**（任何面板可敲），不是引擎推销的选项。修法、诊断、方向选择永不上
面板（实然可导出）。ask 的机械前置=实然手段试尽（重跑/换形态/通道复核先行——ε 消除在冲突呈报
之前，防瞬态差异被当应然冲突）+修法队列为空。

### 2.6.4 收敛律与采信规则

**收敛律（公式20）**：人的每次确认必须以判例键+锚写回 K_ought ⇒ 同键应然问题至多问一次，
ask 数随批次**版本族内**单调不增（跨版本重问=新真意的正确代价，A12）。**自愈的完整定义**：K_is 靠真 PASS 写回自动增长（既有），K_ought 靠
裁决写回自动增长（本节），两层同增使"少问人"与"该问必问"同时成立。

**采信规则（公式21）**：检索命中的应然记载是**证据不是公理**——采信门限：命中记载间无互斥
（单源命中即满足）∧ 与实机不冲突 ∧ 不**改写**意图面（与 D 文本/既有 E 语义相抵才算改写；
从记载**填充**欠定空缺是遵从既有意图投影，不是改写——A11）→ 径直采用；任一不满足 → 呈报。**K_ought 写入权=人源专属**：
引擎自己的推断只能以 uncertain 观察带语境入库，不得写成应然判例（防未经确认的理解自我复制，
与 footprint validity 二态同构）。

### 2.6.5 对抗检查（2026-07-10，12 攻 6 修 6 站——A10/A11 打中的是本节自产公式的 bug；补出的参数即"缺的参数"）

| # | 攻击 | 裁决 | 补出的参数 |
|---|---|---|---|
| A1 | is/ought 分界机器判不动 | 站住（补参数） | **意图触碰面** = {D 语义, E 语义, 交付预期}；P 在正解等价类（§3.3）内变形属实然，触面即应然 |
| A2 | 应然记载可错可陈旧（毒先例/过时 spec） | 修正 | **应然锚**：K_ought 条目同样携 (版本, 时间, 血统)；跨版本记载冲突呈报不自动选新；人写卷的"想验什么"（意图，不被假 PASS 污染）与"能不能跑"（实然，归 device_verified）分离评估 |
| A3 | 收敛律没有"同一问题"判据会重问/错答 | 开洞→补 | **判例键** = (意图/特性签名, 冲突形态, 版本族)；检索失配保守回落 ask——宁多问一次不错自答 |
| A4 | 意图所有者不可达（研发也不知道） | 站住 | 挂起特权+缺陷候选自足覆盖，无第三态 |
| A5 | 采信后写回 → 毒判例自我复制滚雪球 | 修正 | 采信不写回（记载已在库）；K_ought 写入权人源专属（2.6.4） |
| A6 | 纯工程故障封顶（无应然缺口，如序列化 bug 三连拒）——"封顶=欠确认"不总成立 | 修正 | **cap 二分**：有待决应然→强制呈报确认；无→escalated 工程故障呈报（证据附上）。两者都是呈报，均非预算请求 |
| A7 | ε/瞬态差异被当应然冲突误报 | 站住（次序显式） | ask 前置=实然手段试尽（2.6.3），ε 消除先于冲突呈报 |
| A8 | 用户≠作者/研发本人 | 站住（声明） | **代理假设**：回答以用户转述为准，判例锚记"用户代理"；代理失真是可接受残余风险 |
| A9 | 每个应然决策全查 5 路检索 → token/时延爆炸 | 修正 | **检索触发条件**：全序 K_ought 检索仅在同形判据命中或 verifiability 判欠定时启动；常规编写走既有先例+footprint 常规序 |
| A10 | 「多源一致」把单源命中全逼去 ask → ask 爆炸（本表自产公式的 bug） | 修正 | 采信条件改写：**命中记载间无互斥**（单源命中即满足）∧ 不与实机冲突 ∧ 不改写意图面 |
| A11 | 「触意图面必问」与「检索本来就为定 E」自相矛盾（自产 bug 之二） | 修正 | 意图面判据精化为**改写 vs 填充**：与 D 文本/既有 E 语义**相抵**才是触碰；从 K_ought 取记载**填充**欠定空缺=遵从既有意图投影，非改写，可采用 |
| A12 | 产品升级后旧裁决全失配 → ask 重新爆发,收敛律破产? | 站住（声明） | 收敛律是**版本族内**单调（判例键含版本族本就如此设计）；跨版本重问=新真意的正确代价，与床态版本锚同型 |

### 2.6.6 对称怀疑推论（2026-07-14;证据锚经对抗评审 T3 校准）

单向护栏既有（§2.6.1 is-ought 分界 + (21) 内 K_ought 写入权人源专属 + A5——
初稿误引作 (20)(21),(20) 是收敛律,已更正）:实然不得改写应然。本推论补对偶方向:
**实然与应然冲突时，冲突本身不构成对任一侧的判决**——设备行为偏离脑图期望 ≠
设备缺陷，正如设备回显 ≠ 期望值来源。冲突的唯一合法出口是矛盾即问边（§8 第三条
ask 边），且呈报前有**第三源取证义务**:与该机制同构的人源判例（人工套件）、
手册记载、缺陷库必须先行对照。**证据锚（校准后）**:log_backup 极性对照事件——
人工金标准对「恢复带回配置」是正向预期（found "sdns host name…"）,第三源一击
给出既有极性;写保存族本体的主根因**后判**为 σ 链断裂+oracle 残差（窗口失真,
DESIGN §18.10),非真是-应冲突,F1 已对该族降位——推论本体（冲突≠判决、第三源
义务、词表缺臂）不依赖该族失真观测,仍立。工程对应缺口:归因词表只有
defect_candidate 一臂可表达"非我方错"，缺 expectation_suspect 臂——词表结构
决定表达偏差。这是设计方案缺口非理论缺口:本推论+矛盾即问边即理论位
（DESIGN §18.11;处置分类学 (40) 需同步增补第七类,见 §2.12 对账）。

## 2.7 行动论：判断开放、入库门闭合（2026-07-11 yzg 233 实证补；两轮对抗定形）

### 2.7.1 缺口的发现（实证）

yzg 真机验收：233 案把 listener IP 从物理口移至 vlan100 后清理段未还原 interface 层，
下游 9 案同 IP 超时（R1 6/26 的主因）。两个归因孔的散文**独立且完全正确**（因果链、
受影响案清单、两个修法方向），但认知穿不过三道人写的墙——disposition 枚举无「前驱
污染」词位（正确散文被拍扁成 env_blocked）；固定图上没有「按公共原因组织应对」的
路；引擎无「执行 LLM 提案动作」的出口。若按「加检测器/加枚举条目」应对，则每类
新问题都要人加代码——应对上限=人的预见力上限，是人写规则的自动化，非 agent 自动化。

**理论定位**：本理论此前是知识论完备、行动论缺失——K_is/K_ought 的流动、写回、收敛
均有公理（§2.6），但**行动空间**（能重编/复跑/重排/问询）被隐式锁在人写的 py 层
（图拓扑+工具+枚举）。四层封闭回答了「新知识零代码进入」（判例层），没有对称回答
「新形态的行动如何零代码进入」。知识可自愈生长、行动不能——不对称即缺口。三个
具体冲突：①判定表误分类一次（「失败后的应对组织」会因场景而异，应归 LLM 判断，
却入了确定性流程的格子）；②「LLM 不当胶水」的实证（长上下文执行失控、942 配对）
证明的是**执行序列**不可交 LLM，被过界引用到**判断的组织**；③DESIGN §11.7 已触到「信息或
**权限**不在引擎侧才 ask」，但未展开「引擎权限内的行动从哪来」。

### 2.7.2 行动论公理

**(22) 行动生成律**：行动空间 A 非常量。A_{t+1} = A_t ∪ {a：a 为 LLM 从原始事实生成
的提案，且 oracle(a)=pass，高危类首执另需人批}。**闭合的不是动作集合，是入库门**
（oracle+人批）；规则不是人写的，是验证出来的。

**(23) 知行对称律**：行动侧与知识侧形态严格对称——

| | 知识侧（§2.6 已立） | 行动侧（本节） |
|---|---|---|
| 开放生成 | 观察/记载（validity 分级入库） | 提案（工作流：动作序列+参数） |
| 闭合的门 | 人源专属写回（A5）+verbatim 门 | oracle 验证+高危首执人批 |
| 判断归属 | 孔（LLM 判层/判形态） | LLM 组合原语与给参 |
| 落账归属 | 引擎（事实流/判例库） | 引擎（执行+效果记账） |
| 收敛键 | 判例键（意图签名×形态×版本族） | 行动判例键（原因签名×形态×版本族） |
| 收敛律 | (20) 同键至多问一次 | 同键免生成免批（检索命中→机械/轻确认执行） |

**(24) 前筛律（效率）**：批级判断的触发条件必须是机械前筛（如 fail 签名最大簇占比
≥θ）。θ 类规则是**资源规则**（同 max_rounds/墙钟/并发上限），不随世界形态增长，
不违背 agent 自动化；**场景规则**（interface 词表、vlan 检测器）才是被本节禁止的
增长方向。

**(25) 写外部世界的双通路**：写共享资源（设备床）只有两条合法通路——机械逆放己方
记账的写操作（框架 IP 契约的推广：写时记账，恢复=逆放账本），或人批准的提案（携
行动判例键，批准即写回，同键复现免批）。LLM 对外部世界**只有触发权与组合权，没有
无验证的生成执行权**（INV-9 的行动论表述）。

### 2.7.3 文献坐实（2026-07-11 调研；每构件均有独立验证，无一需自行发明）

| 构件 | 外部验证 | 对应我方已有/待建 |
|---|---|---|
| 行动=成功后入库的可执行技能，检索组合复用 | Voyager (arXiv 2305.16291)：技能库+迭代环，**3.3x** 物品/15.3x 里程碑（引用校准 2026-07-13:原记 3.1x 为笔误,论文原文 3.3x unique items） | 已有（卷面线）：worker 生成→上机 oracle→先例/footprint 写回→compile_precedent 检索。缺：运维应对线 |
| 技能漂移=契约违反，失效由契约验证暴露非人预判 | SkillGuard (2605.10990) | 已有且更严：version_family 判例锚/device_verified/凭证 mtime/床态 build 锚 |
| 改进靠失败归因到具体步骤，非扩库非全轨迹反思 | SkillAdaptor (2606.01311) | 已有：归因孔+定向重编（942 配对的独立复现） |
| 变更必须过实证验证才留下；archive 保多样 | Darwin Gödel Machine (2505.22954, ICLR26) | 借验证纪律；**不借自改引擎代码**（oracle 成本模型不同+可审计不变量） |
| 成功轨迹蒸馏成带变量工作流，检索注入 | AWM (Agent Workflow Memory) | 待建：行动判例库的学术形态 |
| 低重复+隐式奖励环境的经验管理 | Tree-of-Experience (2606.06960) | 场景精确命中（每批用例不同、oracle 贵） |
| 记忆无衰减/固结则自退化 | EvoMemBench (2605.18421)（引用校准 2026-07-13:论文真实,但「无衰减则自退化」是本文档引申——该论文实为 15 种记忆法基准对比,headline 是「长上下文基线仍有竞争力」,未直接结论「无固结即退化」;此条降为**方向性类比**非硬背书） | 已有：validity 分级/observed_under/dream consolidate |
| 经验自进化的安全风险成独立研究题 | Safety Risks (2604.16968) | 背书 (22)(25) 的入库门与人批非保守而是必要 |

### 2.7.4 对抗检查 R1（架构面，7 攻 2 毙 5 修）

| # | 攻击 | 裁决 |
|---|---|---|
| A1 | 批级判断位=orchestrator 还魂，爆炸半径 26 案 | 修正：降格为**产假设事实的观察者**（不调工具不发动作），假设须 per-case 证据 verbatim 交叉验证 |
| A2 | LLM 生成写设备命令顶穿 INV-9（自行 del 崩整卷实证在案） | **毙**「自由生成执行」；活路=(25) 双通路。Voyager 语境差异点破：它是单人沙盒，我们是共享生产床 |
| A3 | 处置开放化→冻结/瞬态护栏失锚，漂移无界 | 修正：枚举保留，但枚举对象从「场景规则」纠正为「能力原语」；LLM 自由度在组合与参数 |
| A4 | 「LLM 全判对」样本量 1+因果链恰在本批回显内 | 半中：跨批靠检索半径（判例/footprint 拉通道）非加孔；产出过 verbatim 结构校验挡模型漂移 |
| A5 | 为 10% 场景给 100% 批加税 | 修正→(24) 前筛律；触发阈值是资源规则 |
| A6 | 开放行动瓦解 fold 全函数性（未建模转移无状态语义） | 对「开放动作类型」成立=毙；对「原语组合」不成立（原语状态语义已定义）。闭集是 fold 全函数性的必要条件 |
| A7 | 提案洪水淹没 ask，自由文本无键，收敛律套不上 | 修正：提案必携行动判例键，批准即写回，(20) 原样复用 |

### 2.7.5 对抗检查 R2（效率与 token 面，2026-07-11 当日实测数据裁决）

实测基线：yzg 本轮编写期 ↑13.2M tokens/¥41（run5 全程 15M/¥47.5）；归因 fork 均价
↑849k（run5 256k 的 3.3 倍，全历史喂入+深思考）；emit 自纠往返（blocks 字符串×26 案）。

| # | 攻击 | 裁决 |
|---|---|---|
| E1 | 批级诊断位每轮固定加税（~40k↑/轮），多数批白付 | **被数据反杀**：公共原因批（yzg 9/20 同因、zhaiyq 22/50 时序翻转——「历史占比不低」基于 2 批窗口）现状是 9 个归因 fork 各自重讲同一故事（9×849k↑）；前筛触发后=1 深归因+8 轻校验+1 合成 ≈1.06M↑ vs 现状 7.65M↑，**省 ~86%（单批数字的算术推算，未实测；落地序②的 Langfuse 对照验收即其检验）**；独立失败批被零 token 机械前筛（签名簇占比）挡住 |
| E2 | 工作流检索注入=brief 膨胀+「注入≠遵循」（topology 注入仍犯 8 次实证） | 修正：行动判例**不进 worker brief**（worker 不消费运维工作流），只在诊断位按需检索 top-2（~1KB×一处）；库>百级后靠 slug 语义键+版本锚过滤（B 片同款） |
| E3 | 蒸馏成本（成功应对→工作流=额外调用） | 站住但可忽略：~10-30k↑/次×每批 0-1 次，flash 档可跑；真风险是蒸馏质量→由 (22) 入库门（oracle/人批）挡 |
| E4 | 首执人批=交互延迟 | 站住且净改善：今天 4+ 道本质相同的题 vs 批级 1 题；频率随库增长按收敛律递减 |
| E5 | **最大效率风险在既有系统不在新构件**：本轮实测三个泄漏点（blocks 无双收自纠往返/归因 brief 全历史载荷/自纠反馈粒度） | 站住→落地序第一位：先还效率债再上新构件，否则新构件的收益被既有泄漏淹没、归因失真 |
| E6 | 冷启动期纯付出（库空：蒸馏付出/检索 miss/人批频繁） | 站住有界：公共原因形态种类有限（床污染/瞬态族/版本漂移/互斥…量级 ~10），3-5 批后命中；知识侧判例库已接受同一成本模型 |
| E7 | 批级与 per-case 并存=双份成本；替代=丢精度 | 修正：分层触发——无簇纯 per-case（现状零变化）；有簇=代表案深归因+成员轻校验（flash ~20k/案）+合成一次，见 E1 数字 |

**R2 落地序裁决**：①还效率债（blocks 双收/归因 brief 载荷裁剪）→②机械前筛+批级诊断
（省钱构件先行，Langfuse 对照验收 token 收支）→③行动判例库（蒸馏+检索+人批门）→
④同键免批复用。每步独立可验收，任一步数据不支持即停——(22) 的验证纪律适用于本
落地序自身。

### 2.7.6 二次实证与覆盖裁定（run8，2026-07-11；序①上线后同坑复撞）

序①三修上线后重跑（床污染故意保留）：编写期 ↑5.85M/¥18.6 vs run7 的 ↑13.2M/¥41.4
（**省 56%**，blocks 双收为主）；同坑如期复撞（655154 等再判 env_blocked）。对「合并卷
初始化未清理 vs 用例资源重叠未判定」两种形态假设的理论覆盖裁定：

- **形态一（清理缺失）**：理论已覆盖（互扰消解推论：自清理∘尾序），机制健全**且当轮
  在工作**（emit persistence 门同轮拦了别案的 missing-clear）；漏 233 纯因 interface/vlan
  不在 persistence_channels 四通道枚举——X4 数据缺口的第二面（merge 通道①排尾同枚举，
  233 未被排尾）。**机制无罪，枚举的增长方式是罪**——该条目应为序③首个「提案→人批→
  探针验证→自动入库」演练标的，而非人手写。
- **形态二（资源重叠）**：通用读写集冲突分析=场景检测器（(24) 否决方向）；正解=diagnose
  位（233 动 34.70 接口×9 案 dig 34.70，批级视野送分题）+按需床态对照（复用 bed_check，
  资源规则）。ctx=(π,B) 的 B 维交互理论位早已留好。
- **X8 代价面（诚实记账）**：单案证据文件（序①）让 run8 归因跨案追溯变浅——fork 做了
  同批 pass 对账（正确排除系统性）但未追到 233 源头（run7 整批视野时两 fork 均追到）。
  不回退 X8（3.3 倍 token 账在先），由 diagnose 位（X3）结构性兜住——此为 diagnose 的
  实证论据②。
- **C 层缺口（新，小）**：233 的意图本身是 method_vs_implementation 的 ought-欠定
  （测 VLAN listener 而触发端 untagged 打不了 tagged 流量）——§2.6 意图触碰面与 panel
  的 conflict_shape 枚举**均已覆盖**，两轮孔皆未呈报而试图硬编：判断层未触发，非理论/
  schema 缺口。修法=孔 md 补形态提示（C 层一句话）∨ diagnose 位兜。

裁定：本坑不产生新理论构件；落点=X4（数据）×X1/X3（结构）×C 层（提示）交点，
是落地序②③的第二实证 justification。

### 2.7.7 非法 ask 判据与床账通路（run9 用户裁决，2026-07-11；R4 对抗定形）

实证：run9 的 env 面板（655173 等 4 题「确认环境/隔离复跑」二选）被用户裁定**根本不该
触发**——上批 vlan100 污染本批，引擎该自动处理。按 (19)/DESIGN §11.7 检验：
- **权限在引擎侧**：污染物=本引擎上批产物（己方）；INV-9/bed_gate 设计原文「自动清理
  只限床账内己方未复原产物」——账内自动清理是既定授权，非新开口子。
- **信息在引擎侧的应然可达集内**：「它是己方的、原值是什么」应由床账（设计在案、O-3
  未接线）承载。**缺件是工程债，不豁免 ask 的非法性**——否则任何懒惰实现都能借
  「我不知道」把 ask 合法化，ask 退化为实现质量的泄压阀，收敛律失效。

**(26) 非法 ask 判据**（充要条件 (19) 的对偶可执行面）：
ask 非法 ⟺ 决策所需信息与权限均在引擎侧**应然可达集**内（实际可达 ∪ 记账义务内应载）。
应载而未载 = 非法 ask + 工程债双记：修债，不问人。

**(25) 通路一修正**：记账主体不限框架——**引擎床账是第一记账人**。记账覆盖=引擎可观测
的己方写（批前/批后床态快照 diff，见 R4-G4 的己方交叉验证）；created/restored 跨批配对
接力（批后 closing 尽力恢复+入账，bed_gate 据账接力——双保险为崩溃安全，G6）。

**R4 对抗（6 攻 2 修 4 站）**：

| # | 攻击 | 裁决 |
|---|---|---|
| G1 | 账基线污染：首批建账时床已脏，快照基线=污染态，自动恢复只回到脏态 | 站住（定性）：床账是**增量守恒**机制（保「我批前批后不变」）非「绝对干净」机制；存量清偿=一次性人工或一次合法 ask（账空时来源不可证=非己方，INV-9 的 ask 合法）。run9 的 4 题严格说是**账空时代的最后一批合法 ask**；用户裁决的实质=从今建账，同形态永不再问 |
| G2 | 床态快照的投影集（探哪些状态面）是枚举——场景规则复辟？ | 修正（表述）：投影集=bed_probes 既有探针+通用网络状态面（接口地址/vlan 表），随**平台**增长而非随**坑**增长——平台状态面级≈资源规则；投影外的写快照盲（C2 同型首撞学费，诚实边界） |
| G3 | 自动逆放写设备=A2 亡灵还魂？ | 站住（区别声明）：账内逆放不是 LLM 生成命令，是**机械回放**（快照原文/账内 created 记录）；恢复后必跑验证探针（diff 应清零），失败→合法 ask。(25) 通路一定义原文 |
| G4 | 共享床上他人并行动床 → diff 误认己方 → 误恢复别人的东西（INV-9 破） | 修正（补机械验证）：diff 项须与**本批执行命令面**（last_run 的 sends-command 记录，机械 grep）交叉验证——本批命令流里出现过的 diff 项才认己方可逆放；对不上的=非己方→ask（合法）。批内进程互斥挡不住人肉并行，交叉验证挡 |
| G5 | 成本：批后快照+diff+账追加 | 站住：几条 show+文本 diff+jsonl 追加，可忽略 |
| G6 | 恢复时机：批后（谁弄脏谁收拾）vs 批前（下批接力）二选一？ | 站住（原设计正确）：两处都要——closing 尽力恢复+入账（批退出即净），bed_gate 据账接力（防批被 kill 后账悬），DESIGN §3 原文双保险 |

## 2.8 范式边界：K 理论的适用域收缩声明（2026-07-12 用户两轮质疑定谳）

yzg 十轮验收的失败分布对账：**实测 72%**（53/73，R5 定量校准；初稿估值「约 80%」
已被数据替换）源于**基础设施可靠性域**（床污染/flaky/顺序依赖/
取证失真/框架暗行为）——该域是成熟学科（test reliability engineering，15 年文献），
本理论在该域**错配**：用认识论框架从第一性原理现推标准问题，每个解都造得糙（vlan
信号/模板库两次被用户拦下即其代价）。裁决：

- **K 理论收缩回语义正确性域**（意图保真/ask 收敛/判例/编译正确性——独创且实绩达标：
  dongkl→yzg 迁移首跑 23/26、同类知识型失败零复发（dongkl→yzg 两批窗口））。**不立 K_infra 本体**——框架
  暗行为的正确载体是特征化/契约测试（可执行的知识），不是知识条目。
- 基础设施域移交 `THEORY_infra_reliability.md`（分类学/检测器谱/hermetic 阶梯/
  quarantine 运营/契约测试/证据完整性）。两域界线判据：失效可被「换一个完美卷面」
  消除 → 本理论；不能 → 彼域。
- **被测系统 S 本体移交 `THEORY_target_system_algebra.md`**（2026-07-12 用户裁定：
  668059/233/床态一族的病根是 S 从未被建模——分层配置代数/承载图/传播定理/复位
  差集/版本参数化,公式 (28)-(33)）。三角关系：K=关于 S 的知识,infra=作用于 S 的
  过程,S 代数=对象本体——三者交而不混。
- **中心论断（§4）适用域限定**：偏差是 K 的状态函数——**在基础设施信号可信的前提下**
  成立；基础设施失真（污染/flaky/取证错位）产生的偏差不是 K 的函数，混入会让 §4 的
  监测（DS-4）失真。工程含义：flake 率（彼域 §5 指标）是 DS-4 的前置排除项。
- **闭合宣称修账**：A 层「闭合于框架版本」改为「闭合于**框架行为的已知子集**」——
  框架是黑盒，认知开放（0.8 模糊匹配 2026-07-11 才被发现）；已知子集的扩张通道=
  特征化测试（彼域 §6），撞坑不再是唯一发现方式。原理层检测器承认已增长至 7
  （+签名聚类、床态差分——§2.7 行动论的原理级观察者）。

## 2.9 跨域元定理：判决三链、统一写权律与框定保真（2026-07-12，全史考古+五路检验驱动；对抗 R7 见 §2.11.4）

缘起：v1→v8 全史考古（30+ 份历史文档四路审阅）+ 五路检验（三数据+两文献，
证据锚 `RESEARCH_theory_data_checks.md`）。本节把三份理论**共享而未言明**的
结构显式化——零新本体（R7-M4 自查），每条元定理要么收编既有公式、要么给
既有工程散件理论归属。

### 2.9.1 (35) 判决三链分解

一个交付 PASS 是**三条正交保真链的合取**：

```
(35) Verdict = Chain_intent ∧ Chain_object ∧ Chain_process
     意图链: 作者意图 → D → C          （编译期保真;合取①②,本文档辖）
     对象链: C 在 S 分层结构上的承载     （运行期设备侧;承载合取律(30),S 代数辖）
     过程链: 执行与取证的保真           （框架/床/证据监管,上下游双向,infra 辖）
```

三份理论文档不是历史巧合，是判决的三个因子空间——理论三角的**完备性论证**。
证据映射：644/608 死在意图链；233/zhaiyq 死在对象链×过程链；#67/终验吞 fail
死在过程链。**完备性审计办法**：沿「作者意图→脑图→卷面→执行→回显→digest→
台账→报告→用户」逐跳走，每跳必须有（不变量、检测器、修复路）三件套——考古
审计实证：未吸收 delta 恰好聚集在三链接缝处（载荷通道/测试隔离/交付透明度）。
第四条链的出现=本定理的预注册证伪点。

### 2.9.2 (36) 统一写权律（修复方向定理）

全系统每类失效都是「同一事物的两个表示不一致」——K vs R\*、mirror vs 真机、
床基线 vs 床现状、台账 vs oracle、证据 vs 案身份、手册层级互斥、类型图 vs
实例图。检测端全部是机械 diff（锚差公式 (7) 是全系统母式，不只是 K 域的）；
**修复方向由权威侧唯一决定**：

```
(36) 任何写(对世界或对库)的合法通路由权威归属唯一决定:
     权威在引擎应然可达集内 → 机械执行(带锚带账)
        实例: 床基线权威→修世界(恢复);设备权威→修表示(重验);权威序内→机械采信
     权威在人(应然)        → 呈报确认
     权威缺失或冲突        → ask
```

**收编声明**：(21) 采信规则、(22) 行动入库门、(25) 双通路、(26) 非法 ask 判据
是本律在四个基底上的实例——新机制立项时用一步判据（权威在哪）替代四公式模式
匹配。**推论（人的边界定理）**：人在系统中不可消去的位置恰好两个——应然权威
（意图仲裁）与信任根声明（infra 信任根登记册辖）；其余一切人工介入都是工程债
（(26) 的推广；与 DESIGN §14-R5「人工怎么处理的 agent 就按同法处理」同向）。
可证伪：出现需要第三种修复方向或第五种写通路的机制即破。

### 2.9.3 (37) 框定保真公理（(27) 的孪生；与中心论断的两层和解）

(27) 证据保真管的是**下游监管链**（设备→证据→brief）；它有个未言明的孪生——
**上游框定链**（brief→LLM 注意力→判断）同样是保真对象：

```
(37) 进入 LLM 判断的输入框定是一等保真对象:
     ①待证假设不得以结论形态开路(降级为待证伪假设,响度低于事实)
     ②意图/任务焦点占 recency 最高位(内联,不按引用降级)
     ③修复轮 brief 的历史错误方向是污染源(最新轮内联+早轮引用,X8 已落)
```

**与中心论断 (9) 的和解（两层，非矛盾）**：K 健康度解释**跨批 R1 命中率**方差
（一阶，Δ6/13——selfheal 受控对照。口径注 2026-07-16：selfheal 族 R1 数字经 07-13 校准存在终态误标史
（11/13→首跑 6/13），此处引用取校准后口径，详 §6.5）；框定解释**修复轮内收敛/发散**（修复轮
一阶，Δ2-3 且错方向锁死——035413 三轮 vs main 一次、pe1 布局 A/B。口径注 2026-07-16：pe1 原始数据
不在盘（07-13 校准明记），此处引用降为定性方向，详 §6.5）。变量不同
层：K 是知识存量，框定是知识带宽。文献同构：Contextual Drag（arXiv:2602.04288，
上下文失败尝试使后续偏向结构相似错误、外部反馈救不回——与 035413 三轮锁死逐点
对应）、Premise Order（arXiv:2402.08939，>30% 落差）、context rot、Anthropic
多 agent +90.2%=干净上下文窗口。公理化表述是本项目提法；适用边界如实记：等
token 预算下单 agent 可反超（arXiv:2604.02460）、brief 规格错误是多 agent 的
**主要**失败模式（MAST 2503.13657;引用校准 2026-07-13:原记「第一失败源」不准——
MAST 14 模式中最频为 Step Repetition≈15.7%,「Disobey Task Specification」≈11.8%
居第 3-4,规格错误是显著模式但非榜首）。工程载体已在：`PROMPT_ENGINEERING_STANDARD.md`
布局纪律、brief 去劫持（A3）、X8——本公理给它们理论归属，防止被当成可随意
回退的「格式偏好」。

### 2.9.4 (45) 判例源权威与防自指公理（2026-07-14，V6/V8 跨版本实弹定形）

**背景**：判例空间存在两个血统——**人源判例**（人工套件卷，词汇独立于脑图）与
**机生判例**（引擎写回的 verified 卷，与脑图共享同一套标题词汇）。相似度检索在
词面上结构性偏向机生血统（同 autoid 族、同标题措辞），人源判例即使机制同构也
词面遥远（实弹：log_backup 人工卷与写保存族机制完全同构，却以"日志备份"命名）。
机生判例的判定若含不健全成分（oracle 残差,§18.10），检索放大即成**自指投毒正反馈**。

**(45) 判例采信分层**：人源判例的采信**先验**高于机生判例；机生判例的可采信性
以其判定经过 oracle 残差审计为前提（未经窗口对账的历史 verified 卷降级为
uncertain 级判例）。**与 (21)/文档源等价裁决的合成规则（对抗评审 T4 定形）**:
分层只作用于**检索排名/可见性**（(45b) 辖区）与记载间**无互斥**时的先验;一旦
人源与机生记载**互斥**,(21) 采信门限原样接管——呈报/设备仲裁,人源**不自动赢**
（分层的正当根据是自指风险不对称,非内容权威序;静态判赢会压掉该去设备的信号,
2026-07-13 用户裁决同理）。血统第三档:用户 adopted/correct 背书的机生卷记
`human_adjudicated`（一次点击不将引擎产文洗入人源层）。

**(45b) 防自指约束**：编写/裁决时刻的判例检索结果不得被自产血统垄断——
排名机制必须保证人源判例的可见性（分层、配额或机制 token 索引任一实现均可，
工程选型归设计）。

**与 A5 的关系**：§2.6.5 A5 已封 K_ought 通道（采信不写回、写入权人源专属）；
本公理封其对偶——device_verified 判例通道的同型滚雪球。两者合起来：**任何
判例通道的自产物都不得在无独立审计的情况下成为后续生成的主导输入**。

**实弹证据（2026-07-14）**：worker 检索头名=自家上一轮假 PASS 卷（相似度 0.38）；
投毒库存实测 yzg 3 卷 + dongkl 3 卷（103 全量窗口清点:dongkl 假 PASS 4 起/3 案、
假 FAIL 68 起/14 案;写保存族四案在 103/79 两床、V6/V8 两代引擎同型假 PASS）。

### 2.9.5 (46) 问询三元组律（ask 即理论对象的投影,2026-07-14 run22 题面实弹定形）

**(46)** 引擎向用户发起的每个欠定问询,题面必须完整呈现三元组

> ⟨ **R**(带脑图出处的行为主张) , **阻碍事实**(本环境为何验证不了原写法) ,
> **已推导的最优替代**(或如实声明无替代及原因) ⟩

选项即三元组的处置投影:采纳替代 / 用户另给 / 如实挂起。**推论(山穷水尽的结构
形态)**:没有完成推导就没有资格问——三元组第三分量非空或如实说明"为何推不出",
是发问的前置义务((21) 呈报义务在题面层的投影)。**出处保真**:R 的每个引称必须
是脑图原文子串(与 submit_attribution 的 evidence⊆device_context 同型——防转述
失真;(37) 框定保真在问询方向的应用)。

**反例(定形本律的实弹)**:run22 写保存族题面——worker 已推出配置面模型等价
(三元组齐全),但通道把它压缩成单 reason 字符串截 120 字塞进机械模板,选项文案
"加请求/观测次数到可验水平"与重启案毫无关系;用户三问皆无解("选项是机械的
而不是 LLM 思考给出的解决方案;ask 的内容也没说明白具体问题;这是结构性问题")。
病理:**理论对象在通道中被压扁**——K 域有 R、有阻碍、有替代,题面层全丢。
本律的机械保障归设计(DESIGN §18.13:schema=三元组、出处子串门、渲染零加词)。
**诚实边界**：分层≠人源无谬（人工套件同样未经窗口审计）；(45) 是采信先验，
终判仍归 oracle（经审计）与人。

### 2.9.6 (47) 执行位阶律（Enforcement-Locus Law；2026-07-16，593516 误分类 + 团队范式对标驱动）

(35) 分解判决、(36) 定「写走哪条通路」（键=权威）、(40) 定「fail 走哪个出口」（键=污染角色）。
三者未答的**第四问**：给定一条要强制的不变式 I，**其强制该落在哪个位阶**。补此推论——是自由度
原则（CLAUDE.md 按自由度分层）、§2.10（构造>过滤、只约束形态非推理）、(36)（分类器元形式）的
**合成**，零新本体（∴推论非公理）。范式对标背书：Kaplow 规则/标准（事前定 vs 事后判，频繁+同质→
规则）、"make illegal states unrepresentable"（形态编进文法则违反不可构造）、proposer-verifier 不对称
（验证器须在**独立/更强可靠性类**才授权信任——同类 LLM 判 LLM 零增益，942 配对 3pp 实证 §9-C1）、
Reason 瑞士奶酪（层须**独立**否则共模孔破栈）、Sutton Bitter Lesson（编 formal/logical 脚手架合法、
编 domain/semantic 内容不合法）。

**四位阶**（皆已在系统内实存）：
- **L_struct 结构/文法门**：只强制**形态**谓词，制品+文法/mirror 源机械可判，**内容无关**（换内容不翻案）。
  实例：emit 崩溃门/必崩门六族/单调门/`found_times`/lint/S§5 π-schema 锚。判据（ABC 记忆
  `[[compile-quality-abc-three-layer]]` A 层）：**误判即必然真错、不误杀好制品**。成本≈0。
- **L_oracle 确定性 oracle / 物理事实读 / 可证伪性工具**：读**地面真值**或做**条件于外部事实的确定性数学
  判定**，非拍板。实例：上机验证（唯一语义 oracle）；协议级硬事实（`^`/非设备提示符/ping，DESIGN §0）；
  **`compile_check_verifiability`**（分布可证伪性数学：算法+n_requests+n_pools→verifiable/欠定+最小请求数，
  **回灌 LLM 非硬拒**——ABC 记忆 B 层）；**序列↔周期自洽**（`check_sequence_periodicity`，2026-07-16
  E10b：断言 found/not_found 的位置排布与声明算法周期的模剩余类可满足性——矛盾是**内容无关的数学恒假**
  （778012 实证形态），但「严格轮转」的模型类本身是对设备行为的假设（dongkl 实测有设备不轮转），
  故恒假走 **advisory 呈报**、永不做 lint 硬门——硬门化=把严格轮转假设走私进 A 层。参数是**周期
  语义类** ``cycle_kind`` 非算法名（2026-07-16 用户通用性裁决返工：算法语义零入 .py——「算法名→
  语义类」映射由 grammar ``algorithm_classes.uniform_rotation`` 数据现查或 worker 语义抽取供给，
  新算法=加 JSON 条目零代码）：仅 uniform_rotation 类判；weighted（剩余类占位依赖调度器交织实现）/
  语义未知/period 未声明一律 fail-open 中性放行不误杀）。
- **L_human 人升级**：ask/呈报，受 (26) 非法 ask 判据 + (46) 三元组律约束。用于 is/ought（权威在人）。
- **L_model 模型判断**：fork 内 LLM，高自由度领域判断（worker 构造 / claim_kind 抽取 / attributor 判层）。
  **红线（grade 之死/DS-2）：模型判断永不单独充当对自身产物的质量门。**

**判定函数**（键=⟨自由度 f, 错误代价 c〔含**自放大**子轴〕, 机检性 m〕）：①m=形态可判 ∧ f=低 → **L_struct**，
且**优先构造约束、检出即路由构造、非 reject-and-strand**（§2.10 构造>过滤 + §2.11.4 M5 fail-open；
reject-and-strand 仅当无任何合法构造存在，那属欠定→L_human）；②需地面真值/条件于外部事实的确定性判定 →
**L_oracle**；③is/ought 决策或 f 高 c 高无廉 oracle → **L_human**；④其余（m≠形态可判 ∧ 无廉 oracle ∧
非 is/ought）→ **L_model** 为**残差位阶**，不论 f；f 低时（唯一正确答案而不可机判）c 高则叠加下游位阶分解
（先例/判例注入 + oracle 后验 + 复现后 L_human），c 低容错。（2026-07-16 对抗修订：原「④f 高」前件使
⟨f 低, m 不可判, 无廉 oracle, 非 is/ought⟩ 落不进任何位阶——残差类非空：claim_kind「分布 vs membership」
语义分诊即落此格（唯一对答案、只有语义能判），enforcement_locus_research Brief1 第 5 条的三比特警告在此兑现；
按文档自己的可证伪条款这构成一次内部证伪事件，修法=④放宽为残差类。）

**核心（否则与用户"不 reject-and-strand"裁决打架）**：一条不变式的正解**常是分解到多位阶、非二选一**；
位阶误分类 = 把本应分解的强制**塌缩到单一位阶**。**A/B 分界是最硬的坑**：L_struct 只收**内容无关恒真形态**；
"命中恰好 N 次对 rr/wrr 可不可验"这类**条件于外部事实（算法随机性）**的判断是 **L_oracle 的 B 层**
（`compile_check_verifiability` 回灌，非硬门）——**把 B 误当 A 做硬门 = GA-CUT 回归**（ABC 记忆专警，
`verifiability.py` 顶注红线"非分布类返 verifiable=True 不误杀"）。

**与既有复合**：与 (36) **正交复合**（2026-07-16 对抗修订：原「特例/推广」措辞不成立——(36) 键=权威归属，
管写通路与修复方向；(47) 键=⟨f,c,m⟩，管不变式的强制位阶。两键**互不可导出**：权威归属推不出机检性（用户
拥有的决策可以完全形态可判，如「接受单跑」的记账），⟨f,c,m⟩ 推不出权威（f 低 c 高的不变式其权威可在人）；
且 (36) 含 (47) 没有的内容——修复方向（修世界 vs 修表示）与带锚带账义务。既非特例亦非推广，是**正交刀**：
同一机制立项时两问都要过——写权归谁（(36)）、强制放哪（(47)）；(36) 的"机械"臂在 (47) 视角下细分为
L_struct/L_oracle，且 L_model 产物须经 (36) 的通路才能成为写）；(47) 在 (40) **上游**（放对位阶使坏形态
不 emit→更少 fail 抵达）；与 h-代数（S §0.5）**正交**（h-位置说 I 是什么、(47) 说 I 在哪强制）。

**实证锚（593516 误分类）**：I=「RR 配比断言须绑 h-不变量（集合/区间），不得绑 h-样本（命中计数/落点）」。
f=低（h-样本结构上测不出配比）；c=高且**自放大**（违反→flaky pass→假 PASS 判例→投毒判官源，(45) 反面）；
m=部分形态可判（`Hit:\d+` 恒真=L_struct；命中计数/落点可验性=L_oracle-B；claim_kind「分布 vs membership」=
L_model 语义判断）。**正解=分解**：恒真→L_struct 门 + 可证伪性→`compile_check_verifiability` 回灌（在场）+
claim_kind 分类→worker 构造指引（通道消歧 dig=采样/show=存在）+ 写回→血统护栏（(45)）。**误分类实况**：
worker 把「p4 参与加权轮转」误归 membership（claim_kind 灰区）→ 没触发在场的 falsify 工具。**修法非新硬门**——
是接通 L_model 分类使在场的 L_oracle-B 工具被调用（bug 是 B 层 advisory 因灰区没触发，非"§0 禁了该有的门"；
∴ §0"结构门留/语义门禁"经此**从严化**：L_struct 恒真形态合法、B 层可证伪性判断保持 advisory）。

**可证伪**：出现第五个执行位阶，或一条 ⟨f,c,m⟩ 齐全却落不进四位阶的不变式。C13/C14 自查：零新本体/零新
文档；统一 4 处零散陈述（自由度纲/§2.10/(36)/DS-2）+ retrodict 593516（收缩性）；消费者=S §0.5 判据的
处置规范、§0 豁免的原理基、(40) 上游关系。

**A2′ 自愈闭环覆盖条件推论（2026-07-16，dongkl 8/9 挂起分布案零观察入库 + zhaiyq 532862 缺陷级观察
湮灭实证；对抗修订后形态——原「按终态枚举扩白名单(+S_SUSPENDED)」是 §2.7.6 所禁的增长方式）**：
观察入库谓词的键 = **观察级判据**（源窗口 ok（(43)）∧ evidence verbatim 过既有门），案终态（含
suspended/escalated/terminal）只作 `observed_under` 语境标注，**不作准入白名单**。broken 段窗口的
「观察」不入行为库（(43) 吸收态推论——窗口失真上的观察是毒，run13 三条撤销先证）。入库须
per-(fact,case,round) 幂等 + 内容归一化（剥时间戳/计数器易变 token）——fact_key=逐字 hash 时含易变
token 的同一观察每轮生成新 key，绕过去重、纯计数观察组被跨轮重复伪造多语境。**成立域限定**（原
「否则 L_model 前提不成立」过强——worker 还有 probe/手册两条腿）：对 **clean 设备探针结构性不可得的
行为类知识**（输出形态依赖前置配置——593516 probe 得空输出；计数器语义/跨客户端行为依赖运行时流量），
判例层是 L_model 的唯一可达通道——此类知识不入库，则 (47) 在 L_model 位阶的判断力结构性缺失
（检验形态：该类知识入库后，同型案下批还靠不靠猜）。

## 2.10 编译目标语言 L 的表达力边界（Cap(L)；2026-07-12 考古回收）

三份理论各有本体（K=关于 S 的知识/S=对象/infra=过程），**编译的目标语言 L
（xlsx DSL）此前无理论归属**——而 Cap(L) 决定合取②的可实现上界：φ(D) ⊆ closure(E)
要求意图特征集能被 L 的可表达断言空间张出的闭包覆盖，否则要么欠定呈报、要么
被迫弱化（假验证的结构性诱因之一）。

- **存量形式化按引用**：`compile_subsystem_design.md §8.9`（582 卷 54436 行穷举
  实证）——L=框架 Python 的严格子集、带命名寄存器的指令式语言（H 捕获/I 引用/
  `found`=子串包含非标量相等/execute 逃生舱闭集）。数据不复制进本文档
  （机制在此、数据按引用，红线不变）。
- **V_K/V_R/V_D/V_U 可知性四分类回收**（§8.9.2 遗产）：V 段期望值按可知性分诊
  ——V_K 配置可推导常量（**条件于 config 后 H=0，可离线算**）/V_R 观测间关系
  （捕获比较）/V_D 设备生成不透明单值（`<RUNTIME>` 回填）/V_U 真不可表达
  （欠定呈报）。§2.5 的序结构与此相容：四分类是**编码动作判据**不是测度——
  它决定「这个期望值该用哪种 L 形态写」。既有 verifiability 机制是 V_U 判定器；
  **V_K 检测器（识别「有信息但可离线推导的常量」）是唯一新立项**，pending。
- **学术定位（先行者声明）**：PROSE/FlashMeta 的「表达力×可反演性权衡」、
  Sketch/Rosette 受限语言传统是方法论类比锚；「表达力边界作为编译可验证性
  理论对象」无现成定理，形式化自担（R7-M4）。
- **原则（v4 命题 3.18 教训正式化）**：**构造中不可表达 > 生成后过滤**——能前移
  成构造约束的门优于事后拒绝（blocks 展开器 vs literal_\n 单病种 17 卷）；带
  硬限定：**只约束形态、不约束推理**（文献双反例：格式约束伤推理
  arXiv:2408.02442、贪心掩码扭曲分布 arXiv:2405.21047——现行「自由判断+结构化
  信封」分层恰是正确形态）。姊妹原则（v3 红线9 回收）：**生成器与评判器必须
  共享断言形态本体论**——隔离≠认知割裂，任何 generator/validator 对（今天=
  worker vs lint 门族）形态词表不同步就制造盲区（grade 时代捕获断言被当弱断言
  CUT 的实证）。

## 2.11 意图空间几何与生成质量构件（2026-07-12 定量线翻案；检验三+文献调研）

### 2.11.1 意图空间几何（解释性理论，非运行时判据）

2026-06 线代形式化的存量实证（数字标注时点，模型/库演化后须重测——R7-M7）：
意图空间 I 低秩（113 意图 r_eff≈4.8，80% 能量 5 主成分）、输出空间 T 高秩
（r_eff≈68.6）、映射 φ\* 弱光滑（剥样板 ρ≈0.17）；跨脑图句式精确命中 0%、
域内大族骨架重合 45-51%（v4 调研 A/C/D）。**解释力**（入册理由）：
- 判例键/行动键收敛律 (20) 为什么可行——意图范式少，同键必复现；
- 整例复用为什么死（族摊销数学必亏）而 blocks/骨架部分复用为什么活——φ\* 不
  光滑但分解后 Template 稳定（SkCoder「检索→抽骨架→编辑」三段式同构，
  arXiv:2302.06144）；
- 冷启动学费为什么按域结构性存在——跨脑图零重合+写回增益按域（v4 步骤4 修订）。
反例入册：相似代码检索常为负收益、top-1 封顶（arXiv:2503.20589）——「照抄
相似例」有害，「检索已验证事实」（footprint 现行）才稳。

### 2.11.2 难度预测构件（预测器定资源，oracle 定交付）

检验三（n=72 配对，`RESEARCH_theory_data_checks.md §3`）：意图复杂度机械特征
批内判别力成立（dongkl n=33：字数>99 → R1 25% vs 82%，Fisher p=0.0016；步数/
检查点/跨设备同向 p<0.02；LOO AUC 0.788）；K 覆盖**词面**特征证伪（复杂度
代理——测真 K 覆盖需历史快照+语义检索，构造路径另立）。与 (9) 相容：K 定批级、
复杂度定案级排序。**用途铁律**（文献一致 + DESIGN §14-R3/R7 裁决）：预测器只
决定资源——思考深度前移（「首败即升」可升级为「预测难即首编 max」）、k 采样数、
先例注入量、ask 提前——**永不当质量门**（AUC 0.7-0.8 是路由器天花板；grade
之死的防线以另一种形式常驻）。文献锚：RouteLLM/BEST-Route/IRT-Router；言语
自评之死与文献并轨（Xiong ICLR'24 2306.13063）。

### 2.11.3 稳定性度量（碰撞熵翻案；二级构件）

2026-06 被弃的「碰撞熵=跨次不一致性」获 semantic entropy（Nature 630:625,
2024）精确同构背书（碰撞熵=Rényi-2 熵）。**代码域正确移植不是 NLI 聚类而是
执行等价**（CodeT 样板 2207.10397）：同案 k 份编译产物按**断言集/步骤结构
等价类**（structural_gate 现成可算）聚类，熵高=欠定/证据不足信号→接
needs_decision/升预算。**级联纪律**：只对 2.11.2 预测为难的子集跑（k 倍成本，
DESIGN §14-R3 授权但按 §14-R7 效率轴节制）。**HACK 盲区声明**（arXiv:2510.24222）：模型稳定
地一致地错时熵为零——稳定性必要非充分，oracle 地位不动（observe-then-assert
式系统性错误正属此类）。

### 2.11.4 对抗检查 R7（2026-07-12，8 攻 3 修 5 站——覆盖 §2.9-2.11 全部新增）

| # | 攻击 | 裁决 |
|---|---|---|
| M1 | (35) 三链完备性可疑：L 语言、引擎自身归哪条链？ | 站住（归属声明）：L=意图链的 codomain（§2.10 即其理论）；引擎自身=过程链的上游半（(37) 框定保真+载荷通道），过程链本就双向；报告→用户跳=过程链下游末端。新链出现=预注册证伪 |
| M2 | (36) 过度抽象——收编四公式却无新可执行内容？ | 站住（新内容点名）：①一步判据替代四公式模式匹配（新机制立项 checklist）；②人的边界定理（人工介入=工程债的判定式）；③修复方向判定（此前床修世界/K 修表示是隐式惯例，现在是定理推论） |
| M3 | (37) 与 (9) 的「两层和解」是 ad hoc 补丁——数据能分离两个效应吗？ | 修正（预注册监测）：现有证据（K 效应 Δ6/13 批级受控对照 vs 框定效应 Δ2-3 案级 A/B+锁死机理）支持两层，但无同批双因子实验；预注册：DS-4 增列框定配置维度，若未来框定改动的批级效应 ≥K 效应，两层和解被打 |
| M4 | §2.10 是理论自我扩张第四次（C13）——为一个 xlsx 格式立「语言理论」？ | 站住（自查过）：零新本体——存量 §8.9 形式化的理论归属化+一个 pending 检测器；不建独立文档；数据按引用。C13 判据=增长带适用域声明：Cap(L) 只管合取②可实现上界 |
| M5 | V_K 检测器会重蹈「机械化必崩」（v1 纯代码管线 41→1）？ | 站住（边界声明）：V_K 判定=「期望值可由 config 离线推导」的识别与演算，是**结构约束类**（低自由度查表演算）非骨架选择；H_G≠0 红线不动——识别出 V_K 由确定性演算给值，识别不出落回 LLM/欠定，fail-open |
| M6 | 2.11.2 难度预测：DESIGN §14-R3「不考虑成本」已授权全量 max，预测器还有什么用？ | 修正（动机换轴）：§14-R7 裁决=成本代价危害**输出效率**——全量 max/全量 k 采样加长批墙钟（效率退化）；预测器把深度/采样花在刀刃上，动机是效率轴不是省钱；且 ask 提前是质量构件与成本无关 |
| M7 | 2.11.1 几何数字是 2026-06 快照（113 意图/38 对），模型与库已变，结论会漂 | 修正（降级声明）：几何入册为**解释性理论**（解释三个既有现象），不作任何运行时判据；数字带时点标注，重测门槛=有人想把它升级为判据之时 |
| M8 | 2.11.3 稳定性构件与「fork 隔离消除自评估」矛盾——k 份自比较算不算自评？ | 站住（区别）：grade 之死死于「LLM 言语判断 LLM 产物」；稳定性是**机械等价类计数**（structural_gate 算，零 LLM 意见）；且信号语义是「欠定/证据不足」路由 needs_decision，不是 pass/fail 判定 |

## 2.12 R10 修正批：处置分类学、问询与报告保真、研发回流（2026-07-12；对抗 R11）

R10 全量审计（三通道：run12 实弹+fresh-context 双 agent+范式对标）产出 25 个
盲区，第一档七项修正如下（τ 六元组与写者全集公理在 S 文档 (38)(39)）。

### 2.12.1 (40) fail 处置对象完备分类学与终止性（V8.5 场景需求补课）

fail 案的处置对象按「污染角色 × 案侧可修性」分类，**每类恰有一个非绕路出口**
（R5 标准②的理论保证；面板出口集完备性=对本表的满射）：

```
(40) 处置类 → 唯一正解出口:
  受害者(床被他者弄脏,卷面无错)      → 床治理后复跑(bed retry)
  自污染者(卷面缺 τ,每次执行毁床)    → 重编补 τ(復跑=再污染,是毒药出口)
  互污染者(双方卷面均缺 τ/共享写)    → 双方重编补 τ + 交换子重排
  真环境(非任何案所致,外部)          → 呈报人(床权/环境权在外)
  真缺陷(h 边缘化后统计量仍偏离)     → 缺陷候选单(五查+换形态)
  欠定(R 依赖 h-in-λ)               → R 边缘化改写(verifiability 通道)
  期望可疑(R 本身与实机+第三源矛盾)  → F1 面板(§2.6.6;人源修订 R 或确认缺陷)
```
（第七类 2026-07-14 增补,对抗评审 T5:前六类均预设 R 为真;expectation_suspect
质疑 R 本身,在表外则满射不变量被静默破坏。其环有界性由 (20) 同键一次担保——
修订 R 后重编至多一轮回到前六类。面板出口集完备性=对**七类**的满射。）

**终止性论证**：处置映射下每案有限步到终态——自污染者重编补 τ 后转受害者或
pass（τ 使其不再产生新污染）；受害者床净复跑后 pass 或转真缺陷/真环境；每次
处置严格降秩（污染源数量或未决类数单调减）。run12 五次循环的结构原因=自污染
者被错配了受害者的出口（复跑），处置映射非良定——本表使该错配在设计层不可写。
**分类可判性**：自污染者=diagnose 的自扰判定+触碰画像含差集内写而无 τ 步
（机械）；其余沿 h 位置+四查既有判据。

**（2026-07-16 增补·处置跨轮一致性——zhaiyq 517027/600046/533020 全事实链一手复核驱动；
「处置单调律/污染分歧裁决律」两提案经对抗**换形态**收编，不立新律——N1/N2 落得进
(40)+(24)+§3.2+contra 既有边，C13 自查通过）**：

- **N1a（台账本体论，机械，L_struct）**：归因 disposition 字段/事件只承载**语义判断**；生命周期终局
  （用户停批/设备失联/escalated 封顶）落**独立事件类型或字段**，不得以 `attribution` 事件形态写入、
  不得覆写语义归因；报告与 facts 分列两轴。一手实证：zhaiyq 全部 3 条终态 `env_blocked`（517027/600046/
  533020）是同一形态的**用户停批记账**（round 硬编码 99、layer=E、evidence="user"）——没有一条是 LLM
  归因；「defect_candidate 被 env_blocked 覆盖」实况=簿记与语义判断混用一个字段，单调律治不了这个，
  字段分离才治（(47) 判 L_struct 的形态：m=形态可判、f=低）。
- **N1b（claim 级证据粘性律，替代「处置单调」——摆事实非机械门，守 dongkl 定稿 §0）**：案的每条
  **强处置 claim**（defect_candidate/expectation_suspect 的 [主张,证据] 对——同标签下可有多条不同
  claim：517027 的 r1「不返回 AAAA」与 r2「Timeout=0」是两条，r3 只反驳了 r1、r2 未经反驳即消失）
  由引擎机械保留并注入后续归因 brief 为**必须消费事实**；后轮结论与在场 claim 不同者，须逐条显式
  采纳/反驳/声明不适用，静默消失落 `strong_claim_unaddressed` 信号（审计用，不硬拒）。**硬单调被否
  的理由**：已知强处置误判 ≥5 例且 100% 落在它要钉住的方向（DS-1 三条 wrong_attribution 全是
  dc/product_defect 向 + 668044 + 517027-r1 一手复核反转——r1 dc 是误判、r3「降级」是带证据的正确
  纠错）；且 044572 的翻案路径是换形态重编→PASS——**推翻误判所需的实验恰是硬单调禁止的动作**，
  逃生通道结构性饿死。∴ 换形态检验轮属 defect_candidate 处置的**组成部分**，不构成降级；
  expectation_suspect 的粘住以第零层域分诊（§2.12.5）通过为前置——床污染批里被钉住的错误期望修正
  会毒进 K_ought（人源血统洗白，(45) 防不住机制内的人）。本律同时补全「归因修法生效性闭环」的半程
  模式（`_prev_attribution` 写而无机械读者——方向已证伪禁同向再开是既有律，方向已证实禁静默弃是其对偶）。
- **N2′（污染分歧=分辨实验缺失；有界性已在 contra≥2 边，不另立界）**：fork 污染假设（h_s0）与机械
  配对结论（polluters=[]）相左时，**第一次**分歧即令隔离复跑携批前/批后床态快照 diff（分辨实验，
  §3.2「实验二分假设空间」的污染域实例：跑前脏=受害者/跑后脏=自污染缺 τ/两头净又翻=flaky 或取证）；
  同案隔离复跑至多 2 次（(24) 资源规则，数值锚=同签名冻结/contra≥2 既有惯例，不假装有统计依据）。
  实验将污染定位到**账外非己方**→(25)/G1 合法 ask（真环境出口）；实验尽仍不可分→escalated 工程故障
  呈报（A6 臂，附双方证据，题面按 (46) 携已二分的假设空间——不再盲问「如实降级」）；缺陷候选仅
  四查+取证保真（(27)）坐实后（run13「再生毒源」叙事骑在窗口失真上的前科）。**分歧本身不升 ask**
  （(19)：污染归属是实然，用户零信息优势；(26) 判所需信息在引擎应然可达集内=非法 ask+工程债双记）。
  一手复核：533020「无限振荡」不成立——2 次 delivery 翻转→contra≥2 既有边→用户裁决收敛；真缺口
  是①两圈=白烧设备轮②用户被盲问（rerun_isolated 对受害者/自污染/flaky 三假设零判别力，机械配对
  候选集不含本案自身前轮——「分歧」是两个不同问题的答案）。
- **F11′（对照差分，advisory 形态——「premise-falsified 机械证据」提案降格）**：同型（断言归一化
  模板同 ∧ 配置仅差受控维）对照案 PASS（**已过窗口审计**——(44) 边界增补是本条采纳前置，不可倒置：
  对照案的 PASS 可能是空真，778012 的 not_found 正是在超时空窗上「通过」）∧ 本案同断言 FAIL——该
  **事实**由引擎机械提取、以事实形态注入归因 brief（注入「对照案 X 同断言 PASS」这个事实，不注入
  「前提已被证伪」这个结论——(37)① 待证假设不得以结论形态开路），attributor 必须显式消费（同 N1b
  粘性语义）；连续 2 轮同签名 fail ∧ 对照事实在场 → 禁第三轮同向重编（接既有 frozen「换法」语义，
  把「法」细化到证据面——换观察对象/断言支点，不只换配置形态，零新机制）。触发排在第零层域分诊
  之后（床污染批会批量假触发：run14 失联批 s₀ 配对恒命中误诊 11 案同型前科）。**机械改 disposition
  被否**：多维差异下「证伪的是哪条前提」欠定（517027 vs 532862 差记录类型+地址族+listener 三维；
  NEW-1/NEW-4 两机制并存实证）——这是条件于外部事实的判定，(47) 判 L_oracle-B advisory 位阶，
  机械化=GA-CUT 重演。

### 2.12.2 (41) 问询链保真公理（(27)(37) 的第三孪生）

(27) 管取证链（设备→brief）、(37) 管框定链（brief→LLM），第三条链
**面板→人→答案→引擎消化**此前无保真门——而用户答案是全系统权威最高的信号。

```
(41) 用户裁决从呈现到消化的每一跳可失真,消化结果必须可被用户核对:
     ①传输保真:答案载荷完整性(run12 实测:长文本截断使「停止:…」变语义兜底
       误判 retry——权威最高的信号在传输层静默变形)
     ②对位保真:答案落到所问的题(run12 实测:批间切换按键竞态,3 案被误自动挂起)
     ③消化保真:token 化后的决策与用户原意一致——机械门=**决策 echo-back**
       (把消化后的决策复述进面板历史/收口卡,用户可一眼核对;归属一致性门的
       意图链版,成本≈零)
     ④提交保真(2026-07-13 补,两次实弹同型):用户以为已答的题集 = 实际提交的
       题集。多题面板按键语义(数字只高亮不选中、Tab 切题不落答案、Enter 只提交
       聚焦题)使 run15 与 run17 两次 3 题各丢 2 答——答案在①②③之前就没出生,
       echo-back 的「缺失行」是唯一事后信号(run17 靠"仅 668044 有 echo"翻案定位)。
       机械门方向:Enter 确认时存在未答题 → 挡板提示,不静默部分提交。
```

### 2.12.3 (42) 报告保真不变量（台账→报告→用户跳的三件套补齐）

实证在案：yzg「名义 26/26 实测 23/26」——报告向用户宣称的数字与 oracle 事实
分叉，且报告信任是 poisoned 的**未设防产生点**（用户按报告签收→假卷入库成
先例）。补齐 (35) 过程链下游末端的三件套：

```
(42) 报告的每个计数与结论必须可从事实流重算:
     Report = render(fold(facts)) 且 render 无自由度(确定性投影)
     机械门:closing 后独立重算 totals 与 render 输出比对,失配=拒绝交付+告警
```

INV-1 已覆盖 engine_report.totals；本公理把辖区扩到 delivery_report 的全部
数字与终态陈述（「21 个通过」这句话必须是视图重算的输出，不是渲染层的加工）。

### 2.12.3b (43)(44) 错误吸收态与断言求值三分（run13 668030 空真实证,2026-07-13）

实证在案：668030 的恢复步（config all tftp）执行失败（`Failed to get the file
from tftp server`,两轮确定性复现）,其后的 not_found 断言在空窗口上「通过」——
**该案想验证的功能从未被验证过,每一轮的 check1 成功都是空真（vacuous truth）**。
理论根源：(2) 把 Exec 写成全函数（O=Exec(P)+ε,ε 只建模观测噪声）,42 条公式
与 S 六定理均无条目覆盖「σ 第 k 步失败后,后续步在什么状态上执行、断言真值
是否有定义」。这是 xUnit ERROR≠FAILURE 区分的理论对应物——C14（自我推导漏
行业常识）第三次实证（τ、ERROR、本条）。

形式（2026-07-13 加固:初版把 exec 写成部分函数、让断言在「未定义制备态」上求值
——指称语义上任何作用于 ⊥ 的严格函数输出必须仍为 ⊥,在未定义态上凌空返回一个可
流转的第三态破坏迁移系统封闭性。改用**带错误吸收态的标记迁移系统（LTS）**,exec
恢复为全函数,broken 成为吸收态在求值域上的忠实投影而非语义外补丁）：

```
(43) 错误吸收态: 扩展状态空间 Σ* = Σ ∪ {⊥_err};迁移函数 δ: Σ* × A → Σ* 全函数;
     动作步 g 带机械可判的 ok(g) 谓词(判据=执行回显失败形态闭集,文法数据携
     provenance):  ¬ok(g) 于状态 s ⇒ δ(s,g) = ⊥_err;
     吸收律:       ∀g ∈ A: δ(⊥_err, g) = ⊥_err        （封闭性,后续步不再逃逸）
(44) 断言求值三分: Evaluate(s, e) = Broken  若 s = ⊥_err        （≙ xUnit ERROR）
                                  = Pass    若 s ∈ Σ ∧ s ⊨ e
                                  = Fail    若 s ∈ Σ ∧ s ⊭ e
     推论:断言 R 的真值当且仅当其支配动作步(R 之前全部制备/恢复步)全部 ok 时
     取 Pass/Fail;任一支配步 ¬ok ⇒ 案=broken——非 pass 非 fail,不计签名、
     不进深归因、不入交付分母
```

消费者（C14 盘点纪律）：(43)→采集面三扫描器（teardown verdict/setup 相位/
case timeout,DESIGN §18.5）;(44)→verdict schema 三值化+fold S_BROKEN 派生态
+归因短路（DESIGN §18.1）。防误 frozen（broken 不计 fail 签名）与报告分母
（broken 单列）是 (44) 的两个直接推论。

**（2026-07-15 dongkl 定稿·用户裁决）**：broken 子分类**采纳 pyATS 七码**（§2.13）——但仅按
**协议级硬码**细分：语法拒绝符 `^`／敲错门（命令畸形/执行主机错）＝Errored → **reflow 重写交付**
（不空跑同卷面）；设备 ping 不通＝Blocked → **env 呈报**；协议级分不清 → 维持**单态 rerun＋streak 升级**。
这与「broken 不深归因、不子分类」不冲突：细分基于**物理码非语义猜测**（无假签名风险），目的是「完整交付」
——把「写坏了导致的没跑成」（如 210998 空串 host）拉回重写、而非白烧设备轮。见
`docs/DESIGN_dongkl_finalization.md §④`。

**（2026-07-15 yzg 实弹补·broken 复跑受 (40) 终止性约束）**：(44) 给了 broken 的**局部**语义（⊥_err、
不计签名、处置=复跑），但**未给对应的降秩终止证明**——(40) §2.12.1 的 fail 七类处置「每次严格降秩
（污染源/未决类数单调减）」是「山穷水尽才 ask」满足「欠定必问」(I1) 的**理论前提**（fail 有限步收敛
⇒ live 必归零 ⇒ gather 必达 ⇒ 欠定必问）；而 broken 的「复跑」**无界时不降秩**——复跑一个 `not_run`
仍 `not_run`，秩不降 → live 恒 >0 → gather 永不可达 → **欠定案被饿死**（yzg run 实弹：2 undetermined
broken 让 7 欠定案从未被问）。**补约束（纳入 (40) 满射）**：broken 的复跑必须**有界**——同 case
（跨 reflow）复跑/重编计数 ≥N 仍 `not_run`/broken → 强制降秩到 **escalated**（未决类数 −1，一次降秩）。
errored（→reflow，`rounds_used` cap 封顶降秩）/ blocked（→env 呈报，真环境出口）子类已各有终止出口；
本补注补的是 **undetermined broken** 这一类。这补全 **(40)↔(44)↔「欠定必问」的 liveness 三角**（C14 型
缺口：(44) 从 668030 自推局部语义却没回头延伸 (40) 终止性）。落地见 `DESIGN_dongkl_finalization.md §⑥`
（INV-flush 前置门）+ 实现 streak per-case。

**（2026-07-16 dongkl 778012 边界补·断言级前置）**：支配步集合**含 R 自己的观测窗口源步**——(44) 推论
原文「R 之前全部制备/恢复步」字面不覆盖产生被断言窗口的那次观测（778012 r3 的 dig 全程 connection
timed out，not_found 在超时空窗上求值=真：按 (43) 精神该断言在失败窗口上应无真值，按 (44) 字面它合法
Pass 了——理论字面有缝）。增补：窗口源步命中执行失败形态闭集（dig 超时/tftp 失败等，文法数据携
provenance）⇒ 该断言 **Evaluate=Broken**（断言级，≙ pyATS 分节结果）——不作 pass 证据、不进 fail
签名、不作归因与对照差分证据、不入观察库；失败步之前的断言保留 Pass/Fail，案级 verdict 按滚动优先级
折叠（§2.13 pyATS 映射），不必整案 broken。现状实现只在 pass 案整案降级（`batch_tools.py`
`if _anom and verdict == "pass"`），fail 案内空真断言此前经 B2 缺陷可进签名（A1 结构化解析已断此路）
——本边界是 F11′ 对照差分的**采纳前置**：今天没人消费的假过，F11 一落地就有机械消费者（兄弟案的空真
PASS 会被当机械证伪证据）。778012 的 fix_plan「无害」判词精确化：交付通道无害、该案当轮未变现、
**通道结构有害**。（顺带取证：778012 的 attr_evidence `_round` 字段标 1 而时戳/内容属第 3 次编写运行
——证据链轮次归属误标，与 A1 的 B 族同域，并入其验收观察。）

### 2.12.4 研发裁决回流弧（(20) 收敛律的缺失通道）

「唯一非通过正当终态=缺陷候选」——但候选**提交之后**研发的裁决（确认缺陷
修于 vY / by design / 无法复现）此前无回流路径：不经 ask（不触发收敛律写回）、
非 PASS（不触发 K_is 写回），§5.2 迁移图无出弧。补：

- 迁移弧 `defect_candidate → {confirmed(fix_version), wontfix, cannot_repro}`，
  载体=known_defects 条目的版本区间字段（既有 schema，接线未建）；
- **wontfix=R_spec 的修正**（应然更新）：触发同签名 K 条目与既有 E 的连锁重估
  ——它是 K_ought 裁决，经人工录入通道入库（人源专属 A5 不破），同键判例生效
  后同族案不再重复四查（(20) 在此通道接通）；
- 实证锚：644「待研发确认」悬置至今——本弧未建的直接代价=同族脑图每批重付
  四查+换形态学费。

### 2.12.5 判定树第零层：域分诊（§3.2 结构修正，详见彼处）

Ω 与判定树的入口此前默认 fail 已属语义域，而实测 72% 在 infra 域——见 §3.2
修正与 §3.1 Ω 的 ⑥ 类补位。

### 2.12.6 对抗检查 R11（首次按 C14 三通道协议；6 攻 2 修 4 站）

通道声明：范式对标=xUnit/CI 实践/HCI 确认模式已扫（τ/echo-back/报告重算均有
工业对应物）；数据检验=run12 实弹（(40)(41) 的实证直接来自它）；以下自我攻击
仅作初筛：

| # | 攻击 | 裁决 |
|---|---|---|
| P1 | (40) 分类学的「互污染者」类无实证——发明了一个没见过的格子？ | 站住（声明）：留格是完备性要求（两案共享写的对称形态逻辑上必然存在）；无实证前其出口沿用自污染者对偶（双方补 τ），首例实证到来时按 (22) 纪律验收 |
| P2 | (40) 终止性依赖「重编补 τ 后不再污染」——worker 编的 τ 可能不完整，循环未必断 | **修正（补门）**：终止性的成立以**配对恢复门**（S §0.4）为前提——τ 完整性由 emit 机械核对（差集内写 ⊆ 恢复步覆盖），不依赖 worker 自觉；门失效时降级为「循环计数封顶+呈报」资源护栏 |
| P3 | (41) echo-back 增加一次用户交互=打扰税 | 站住（成本核）：echo 是**展示**不是问询（复述进面板历史/收口卡，无需应答）；对位/传输失真的代价实测=一圈无效循环（¥5+20min），echo 成本≈零 |
| P4 | (42) render 零自由度与「人话叙事」矛盾——叙事必然有措辞自由 | **修正（辖区切分）**：(42) 辖**数字与终态陈述**（计数/状态/结论），叙事措辞（怎么判断的散文）不在辖区；判据=报告中任何可判真伪的命题必须可从事实流重算，修辞不承载新事实 |
| P5 | 研发回流弧的 wontfix 连锁重估=自动改写 E，越权（A5 人源专属被绕）？ | 站住（通路澄清）：wontfix 本身是人（研发经用户代理）的裁决=合法 K_ought 写入；连锁重估产生的是**重估任务**（同签名条目标记待复核），改写动作仍走既有人批/oracle 通道，无自动改写 |
| P6 | 本批一次加 5 条公式+1 类+1 弧——C13 扩张？ | 站住（消费者清单）：(38)→维护日志通道+登记册；(39)→配对恢复门+L τ 槽位；(40)→面板出口映射表（片4.5 直接消费）；(41)→echo-back 门；(42)→报告重算门——五条全部有指名工程消费者，且全部由 R10 实证/审计触发而非自发扩张；C14 的消费者盘点纪律同步生效 |

## 2.13 成熟范式对标背书：LLM 测试自动化竞品与非 LLM oracle 框架（2026-07-15）

> 触发：`AUDIT_design_theory_gaps.md` §6 第 4 条明令「成熟范式对标是抄作业不是选修课——infra 47% 转化率最低恰说明
> 抄完没做比不抄更危险，它制造『已覆盖』的幻觉」。本节把六项外部框架（LLM 竞品 3 + 成熟
> 非 LLM oracle 3）系统对号入座，兑现这份该抄的作业。全文详析见
> `docs/RESEARCH_llm_test_automation_comparison.md`。**本节零新公式、零新本体**——每条都是
> 既有构件的外部背书或活证据（C13/C14 判据：背书≠扩张，同 S §8-G7 之 YANG/OpenConfig 背书）。

**核心收敛（一句话）**：三个 LLM 竞品（NeTestLLM arXiv:2510.13248 / iPanda 2507.00378 /
Keysight US12505309B2）**全部无独立行为 oracle**——判对错靠相似度/LLM-judge/人审；而成熟
**非 LLM** 框架（Cisco pyATS/Genie/NAPALM）有强 oracle，机制正是我们理论所指但实现只做一半
的那个：**断言打在结构化解析对象的 key-path 上、不打 raw CLI 文本**（S 代数 §5 投影核 π 的
忠实实现）。∴ 我们的理论方向领先所有 LLM 竞品，oracle **原语**却落后成熟非 LLM 框架十年。

**构件对号入座**（左=既有理论构件，右=工业对应物；纯背书，非新增）：

| 既有理论构件 | 工业对应物（外部锚） | 价值 |
|---|---|---|
| (44) broken 第三态（≙ xUnit ERROR） | pyATS 7 码结果代数 + 滚动优先级 `Errored>Failed>Aborted>Blocked>Passx>Skipped>Passed` | 我们从 668030 空真自推的三值，工业早有 7 码成熟模板；映射 Errored/Aborted→不重编、Blocked→env_blocked、Failed→重编——broken 路由的现成锚 |
| §5.5 oracle 残差公理 + window-audit（§18.10） | **三个 LLM 竞品全缺**（NeTestLLM 承认「if an artifact passes its own checks it is then subject to manual review」；未来工作明列 "formal methods for test-case evaluation"） | 我们的独有差异被外部反证坐实=这正是全领域公开的未解问题；NeTestLLM 明说要做的正是我们已做的 |
| 断言有效性（(44) 支配步全 ok 才有真值） | AutoSpec（2511.17977）分级 MA/TA（Message/Trace Acceptance）+ canonical-flow 限制（discount 平凡接受） | 二值 pytest-pass 的成熟替代=分级接受度；canonical-flow=§2.5 I(C) 零信息断言的形式化信息含量门的外部同构 |
| S 代数 §0.5 h-in-λ「R 边缘化为 h 不变式(集合)」 | NAPALM `_mode: strict` 集合相等；AutoSpec 独立/依赖约束分类学 | RR 类采样敏感断言的正解形态=集合相等，外部有现成算子 |
| S 代数 §5 承载链下降（OSI 逐层 show 探针） | NAPALM getters / Genie `learn <feature>` 分层结构化状态 | 我们标为「实现缺」的 OSI 链下降=他们的分层 getter，机械探针序有现成形态 |
| §2.9.2 统一写权律 / τ 配对恢复门 | Genie `learn`+`diff` 对象级快照 diff；Keysight 资源清单一致性门 | 床污染治理的对象级 diff（时序抖动不产假 diff）外部成熟 |

**活证据（dongkl 2026-07-15，只读观察轮，`docs/ANALYSIS_dongkl_ask_review.md`）**：
- **210998 = broken（第三态活实例）**：G 层 reflow 后产出的案既非 pass 非 fail，正是 (44) 的
  Broken 态在实弹里首次以显式 verdict 现身（此前实证只有 668030 空真的 vacuous-pass 一侧）
  ——坐实 §18.1 broken 全链的现实必要性。
- **593516 = §0.5 h-不变式的活违反**：dig-命中计数断言是 h-变量（RR 采样敏感），子集复测 pass
  （9:1 分布采样够散即过）并 writeback verified——把 flaky pass 编码成事实，污染判例源
  （§2.9.4 (45) 防自指的反面：采样噪声被写成第二权威源）。正解=绑轮转集合=期望成员集
  （(44) 支配步 ∧ §0.5 集合不变式）。**这是「实现丢理论合取」病（DESIGN §18.14）的第三例**
  ——实现放行了理论明禁的 h-变量断言，前两例 s₀ 假阳 / reachability 假阳。
- **env_blocked 系统性误判（2/2）**：777976（算法分布误判环境，反证同卷 Hit≥1）、994986
  （dispatch 错误误判环境，反证回显含 Linux 提示符 `root@...:#`）——(40) 处置分类学的
  env_blocked 出口缺**内部一致性机械后校验**，与 §18.14 attributor s₀ 复核同型（DESIGN §18.15）。

**方法论收获**：本次对标兑现 AUDIT_design_theory_gaps §6 第 4 条「抄作业」纪律——不是发明，是确认理论方向正确 + 找到 oracle
原语落后的收口模板。所有收口方向都是「闭合已有理论的实现 gap」、不加新理论，落 DESIGN §18.15。

## 3. 失效根因空间与分层可导性

### 3.1 根因空间 Ω（六类 + ②′ 细分）

fail（O ⊭ E）的根因，实证枚举（每类均有 ground truth 案例）：

| # | 根因类 | 实证 |
|---|---|---|
| ① | 用例三要素错（D/P/E 任一或互相不一致） | 413（P 缺 lastresort 形态）、453（P 用不存在的操作）、493（P 过约束）、588691 族（E 锚点恒真）、044572（P 语法变体） |
| ② | 知识源**错误**（K ≠ R\*） | 首批毒卷（先例假 PASS）、手册 re-query 描述与 1::1 实测不符 |
| ②′ | 知识源**不完备**（K ⊉ 所需语义）——与②本质不同：无从推导，只能实验补全 | 493 状态聚合语义（无文档）、570 本地域名语义（spec 有、消费层缺） |
| ③ | 产品缺陷（R\* ≠ R_spec） | 644 的 1::1 值域边界（fc00::231 工作、1::1 ANSWER:0，待研发确认） |
| ④ | 观测通道失真（ε ≠ 0） | exp_recheck5 三案：RouterA 采集瞬态 + 迟到输出级联错位（同拓扑从跳板机打点 NOERROR） |
| ⑤ | 欠定（D 声明的行为在 R\* 下非确定，E 只能是区间/关系）——**编译期可先验排除**，fail 期出现=verifiability 漏网（对抗检查 A14：与其余根因时序不同质） | rr/wrr 分布类（verifiability 机制已覆盖；dongkl 批实测欠定基率 21%） |
| ⑥ | **基础设施域**（床污染/顺序依赖/取证失真/框架暗行为——非 C 三要素与 K 之过，「换完美卷面不能消除」）——**Ω 此前无此格**（R10 盲区：72% 的 fail 在枚举里无家可归，只能被拍成 env_blocked/ε），本类的细分与治理全在 `THEORY_infra_reliability`，本表只设格子与转出口 | 233 家族 40/73、保存族、#67 取证错位（R5 定量） |

### 3.2 分层可导性（可导 ≠ 离线可推理）

R\* 不是黑盒，是**分层可查询结构**，每层有原生算子：

| 层 | 原生算子 | 成本 | 可判定性 |
|---|---|---|---|
| 语法层（命令+参数形态） | `?` 逐位置枚举（设备内建语法反射），`^` 定位错误 token | O(1) 零副作用 | **完全可判定** |
| 状态层（对象存在/配置生效） | show 只读 | O(1) 零副作用 | 完全可判定 |
| 行为层（触发→响应语义） | 配置+触发+观测（do-操作） | O(实验) 有副作用 | **实验算子下可导** |
| 值域层（参数边界行为） | 代表元实验（1::1 vs fc00::231） | O(实验×值域) | 边界逐点可导 |

**归因判定树**（fail 后按层下行，每层用本层算子终结，禁止跨层用贵算子）：

```
O ⊭ E
├─ 第零层·域分诊(R10 补): 床/序/取证信号机械前筛 → ⑥ infra 域转出            [O(批) 机械]
│    判据=批级对账(diagnose s₀ 配对/同批 pass 集冲突/取证归属门/flake 率)——
│    72% 的 fail 属此域(R5 实测);入树前不分诊=以最贵算子处理零成本前筛事件
│    (233 被拍成 env_blocked 的结构原因;可导性定理的量词范围=语义域,守门员在此)
├─ ^ 语法拒绝?    → ? 直查该位置          → 用例错 | 文档错/build差异 | 特性缺失   [O(1) 读表]
├─ 配置生效了吗?  → show 状态对账          → P 未生效(用例错) | 生效了 ↓          [O(1) 对账]
├─ 观测可信吗?    → 第二通道复查            → ε(通道失真,重跑) | 可信 ↓           [O(1) 对账]*
└─ 行为不符      → 分辨实验(换形态/控变量/换值域)                                  [实验,二分假设空间]
                   → 换后通过 = 用例错(形态问题) | 多形态复现 = 缺陷候选坐实
```
\* 第二通道（跳板机直接 dig）当前只在人工诊断路径存在——自动化框架的触发端只有 routera，
引擎内的通道层近似算子是 digest 跨轮对照（重跑消失=瞬态）。对抗检查 A13 如实标注。

**可导性定理（修正版）**：单点观测下 Ω 中多个根因观测等价（044572：①与③在单形态单轮下不可分——attributor 猜③，真值①）；加入分层算子后每个 fail 在**有限次定向操作**内归到唯一根因。语法/状态/通道三层是读表与对账（无需实验设计）；只有行为层需要实验，且实验的角色是**二分假设空间**（五案手动裁决即此算法的人肉执行：历史形态重跑、disable 对照段、换 v6 地址）。

> 推论：形态检验轮（缺陷候选必须换形态复现才坐实）是行为层可导性的结构必需，不是工程偏好。
> 推论：`^`→`?` 自动接线是语法层可导性的算子本体缺口（044572 判死的根因是在可读表的层用了猜测）。

### 3.3 唯一正解（修正版）

- P 不逐字唯一，**正解等价类**唯一：Sound 定义等价类（570 的 monitor default down 与健康检查指不可达 IP 同类；644 的任意合法 ULA 同类）。
- 正解**可查**依赖 K 完备：413 的 lastresort 在手册内（K 足够，检索失败是可达性问题）；493 的聚合语义不在任何文档（②′，只能实验）。
- **K 完备时正解等价类非空且可判定；K 不完备时经有限次实验逼近**——自愈环即逼近过程：

```
K_{t+1} = K_t ∪ Obs_t（观察入库+PASS 升级） \ Poison_t（污染剔除）
```

（旧「R1命中率(t)=f(三参),f 单调」式已废——无度量的聚合名词上谈单调性不可证伪,
见 §4 退化维度表;逼近的可证内容=(8) 演化式 + 规划质量≤模型质量的定义性质。）

## 4. 中心论断：偏差是 K 的状态函数

对抗性反驳（§9 前身）淘汰了"目标函数缺 2/3 需全量 oracle""D 需集中规范化""重编需搜索理论"三个过度理论化的诊断后，存活的中心论断：

> **硬骨头偏差随 K 的健康度涨落；prompt 质量的边际影响远小于 K 状态。**

> 推导版（2026-07-12，S §0.6）：K=对被测机 M 的部分模型，编译=模型上的规划——
> **规划质量 ≤ 模型质量是规划的定义性质**，本论断由经验命题升为推论；LLM 是
> 规划器，换更强规划器突破不了模型上界。（修复轮内的框定效应见 (37) 两层和解。）

> **量化脚手架已删（2026-07-13 中等力度校准）**：原 pe1/selfheal1/selfheal2「三点 R1 斜率对照表」
> 与 §6.5「数据锚定」点估计表整体删除——pe1 数据不在盘、selfheal2 的「11/13」经 engine_ledger
> 核实是**终态被误标为 R1**（首跑实为 6/13）、三点非同口径，不可作定量斜率证据（详见本轮三路
> 核查）。本论断的定量支持仅保留下述**可复现的同批受控对照**（同脑图/同引擎/同批，仅改环境锚），
> 不再用跨批小样本斜率；论断的**可推导根**是「规划质量 ≤ 模型质量」（推导版），不依赖这些数字。

重编效率同样是 K 的函数（定性）：K 残缺期重编易困死（历史 PASS 卷被清则条件采样发散），K 健康期
重编一轮翻正——"搜索低效"的实证全部采自 K 残缺期；K 健康时条件采样分布尖锐对准正解等价类。

**yzg 双回合受控对照（2026-07-10，锚差维度的极端演示）**：同批 26 case、同引擎、同指令——
错误环境锚（设备被换 10.4.6，K 全部失效）R1=3/26；换回正确 build（10.5.0.585）+ 健康 K
（22 份老先例在库）**R1=24/26**（口径注 2026-07-17：与 §2.8「迁移首跑 23/26」并存两数——本处为
受控对照第二回合 R1 命中口径，引用前先核 ledger 归一，team4 审计 P2-6），剩 2 案（保存族）max 思考重编一轮翻正。环境锚正确性是
K 健康度的一部分（K 是"对 R\* 的有时效近似"，build 漂移即整库瞬间 stale）。（定位校准 2026-07-16：
原称「迄今最干净的一组」——该对照改变的是**执行靶**（设备/build），同时切断「K 正确指导编译」与
「R\* 保持编译时假设」两条因果路，二者贡献不可分（即使编译全知全能，10.4.6 上按 10.5 脑图预期跑
照样大批 fail）；且按 §2.8 适用域限定「设备被换」恰是床/靶信号——把环境锚定义进 K 合法，但这使
本组数字对中心论断的支持接近定义性重言（锚错⇒全错）。完备/污染/可达维度⇒编译质量的**非平凡**
证据是 413/453/493 同床翻正与 dongkl→yzg 迁移批，不是这组数字。）

**退化维度表（2026-07-13 形式化加固:废弃 R1=f(完备度,污染度,可达度,锚差) 及其
"单调性"宣称——对未度量化的聚合名词谈函数单调性=不可证伪的伪代数;外部审计提议的
ρ_poison(引用出度加权)/ρ_complete(意图满射比)同样依赖未实现的 quarantine 字段与
φ(D) 统一提取器,不得冒充可计算代理）**。可证内容收缩为两层：
①可推导根（上方"规划质量≤模型质量"）——K 沿哪个维度退化,模型质量就沿该维度退化,
这是模型-规划关系的定义性质,不需要 f;②每维度的**已实现检测器/证据锚**（无锚维度
如实标"规格·待建"）：

| K 退化维度 | 可判定信号 | 实现状态 |
|---|---|---|
| 锚差（build 漂移） | bed_gate 版本锚三方比对（match/mismatch/unknown/probe_failed） | ✅ 活门,run17@79 实弹 |
| 污染（毒条目） | 恒真族反扫（325 卷法,归档区 3 命中）;stale 谓词（anchor.build 已落,派生判定待建）;quarantine 字段 | ⚠ 部分;quarantine ❌ |
| 完备（意图覆盖） | 记录类型词面子集（intent_record_type_gap）;φ(D) 统一提取器 | ⚠ 子域;提取器=规格·待建 |
| 可达（检索命中） | kb 检索 + lex_unreach 事后审计谓词（§5.3） | ⚠ 审计谓词,无在线度量 |

DS-4 曲线在各维检测器齐备前只能记录**维度标注的事件序列**,不做函数拟合。

逐案归属检验（奥卡姆检验——中心论断+两个机械缺口覆盖全部七案，无残留）：

| 案例 | 归属 |
|---|---|
| 413 / 453 / 493 | K 缺失（先例被清）→ K 恢复后一轮翻正 |
| 608 | K 未流动（批内不可见）→ 即时写回已修 |
| 首批毒卷 | K 污染（历史遗留）→ 已清+体检 |
| 044572 | 语法层归因用了猜测 → 缺口 A：`^`→`?` 读表未接线 |
| 644 删覆盖 | 压力路径削减覆盖 → 缺口 B（已闭，2026-07-09）：emit 单调门拦截重编删观测维度 |
| 归因一轮判死 | 越权 → 形态检验门（已修） |

## 5. K 状态机

### 5.1 状态空间

**σ(k) = (validity, reachability, anchor)**，两维正交 + 一锚。

**有效性 validity（条目内容与 R\* 的关系）——七态：**

| 态 | 定义 | 实证 | 检测信号 |
|---|---|---|---|
| `absent` | 所需语义不在 K | 493 聚合语义、570 本地域名语义 | 无直接信号；代理：检索零命中日志 ∧ 换形态后 PASS |
| `uncertain` | 观察级未实证 | P1 入库 6 条 fail 轮观察 | validity 字段（已实现） |
| `verified` | 实证带 PASS 锚 | device_verified 写回 | 锚完整：autoid ∧ run_ts ∧ **build** |
| `conditional` | 多语境并存（verified 精化；**知识的本态非坏态**） | disable→DOWN（纯 service pool）vs 恒 UP（挂 CNAME 池）；obsA/obsB | 同主题互异 observed_under ≥2（观察组计数，已实现） |
| `contested` | 血统间断言冲突待分辨 | 手册 re-query vs 某形态实测 ANSWER:0 | manual↔device 同主题谓词冲突；常为 conditional 未分化前身 |
| `stale` | 曾真、R\* 已漂移 | 644 历史卷（当年 1::1 PASS，本 build 不响应） | **无自发信号（最危险）**；唯一可靠信号=锚差：条目 build ≠ 当前 build |
| `poisoned` | 从未真、带假印记 | 首批 608/570 毒卷；644 弱化卷（写回即毒） | 入库后：意图↔断言方向体检；入库前：单调性/投影门 |

> verified 不是终态，是需要维护的态——stale 与 poisoned 都伪装成 verified，且 stale 的形成完全无声。
> 这是"K 运维"区别于"K 积累"的本质：积累管入库，运维管**印记的持续有效性**。

**可达性 reachability（条目与消费者的关系）——四态：**

| 态 | 定义 | 实证 | 信号 |
|---|---|---|---|
| `reachable` | 正常命中 | — | — |
| `latent` | 在库但时窗错位 | 570 R2 正解批末写回、608 R3 检索不到（即时写回已修） | 写回时间戳 > 消费时间窗 |
| `lexically-unreachable` | 词汇鸿沟 | 413"回退池"↔lastresort | 检索零命中 ∧ 事后条目存在；该信号对本身可入库为词汇映射条目 |
| `suppressed` | 设计性不可达 | uncertain 不进 allowlist | 显式规则 |

正交性：413 = (verified, lexically-unreachable)；644 弱化卷 = (poisoned, reachable)。**单维视图必漏。**

### 5.2 迁移图与信号源

```
absent ──fail观察入库──→ uncertain ──同fact_key PASS实证──→ verified
                             │                                │
                       (分辨实验仲裁)              ┌──────────┼─────────────┐
                             │              第二语境观察   build漂移      体检/拦截命中
                             ↓                    ↓           ↓              ↓
contested ──分辨实验──→ conditional          conditional   stale?        poisoned
                                                          (降级待重验)   (隔离+回滚+引用清算)
latent ──PASS即时写回──→ reachable
lexically-unreachable ──词汇映射条目入库──→ reachable
```

| 迁移 | 触发事件 | 信号载体 | 实现状态 |
|---|---|---|---|
| absent→uncertain | fail/escalated 轮观察 | behavior_candidates + closing ingest | ✅ |
| uncertain→verified | PASS 实证 | merger 升级分支（不降级） | ✅ |
| verified→conditional | 互异语境观察到达 | 观察组计数 + observed_under | ✅ |
| contested→conditional/resolved | 分辨实验 | 冲突横幅→定向实验→升级（A/B 已人肉验证） | 半自动 |
| verified→stale? | **build 变更事件** | 条目 build 锚 vs show version 指纹 | ⚠ 锚已落（2026-07-09：verified_runs.jsonl 记 build → device_run_ref 透传 → 条目 `evidence.device_run.build`）；stale 派生判定（锚差比对+信号）未建 |
| →poisoned 发现 | 体检扫描 / 写回拦截 | 方向体检 + 单调性门 | 单调门已机械化（2026-07-09，compile_emit）；体检扫描/quarantine 未建 |
| latent→reachable | PASS 即时写回 | writeback_one | ✅ |
| lexical→reachable | 鸿沟事件→映射条目 | 检索日志 ∧ 事后正解 | ❌ 缺 |
| defect_candidate→{confirmed(fix_version)/wontfix/cannot_repro} | 研发异步裁决回流（§2.12.4，R10 补——此前无出弧，644 悬置实证；wontfix=R_spec 修正须连锁重估） | known_defects 版本区间+人工录入（A5 人源专属） | ❌ 弧新立，接线未建 |

### 5.3 具体状态机：append-only 事实 + 派生谓词（可实现规格）

**设计原则（回应 C4 复杂度挑战）**：状态**不存储枚举、不写迁移代码**——条目上只追加事实字段，
状态是查询时的派生视图。迁移 = 事实追加，状态自动跟新。若实现中出现按状态组合分支的 if 树，即违背本规格。

**条目 schema（在现有 RawFact/节点条目上的增量，✅=已实现字段）**：

```jsonc
{
  "fact_key": "...",                    // ✅ 身份
  "validity": "verified|uncertain",     // ✅ 主态(唯一存储的枚举,二值)
  "observed_under": "……",              // ✅ 观测语境(自由文本短句)
  "conflicts_with": "fact_key",         // ✅ 冲突声明(人工/机器均可追加)
  "evidence": {...},                    // ✅ 血统载体(source_file→manual / device_evidence→device)
  "anchor": {                           // ⚠ 增量:三元锚
    "autoid": "…", "run_ts": 0.0,       // ✅ 已在 device_evidence
    "build": "10.5.0.583"               // ✅ 已落(2026-07-09)——链路 verified_runs.jsonl(digest 记提交 build)
                                        //    → device_run_ref 透传(writeback/closing) → 条目 evidence.device_run.build;
                                        //    stale 派生判定(锚差)待建
  },
  // build 的三类来源(2026-07-09 用户补充,各服务不同的锚语义):
  //  ① show version(设备自述)——device 血统条目的实证 build:写回/digest 时采集
  //  ② 框架 report 目录名(InfosecOS_Beta_…_10_5_0_568)——跑批时零成本旁路采集
  //  ③ 缺陷库记录(报于 vX/修复于 vY/合入 vZ)——known_issue 条目的锚不是单点而是
  //     **版本区间**:stale 判定 = 当前 build 与 [报出,修复) 区间的关系(044572 的
  //     attributor 曾做此推理但未结构化:「r44409 修复于中信分支,被测设备未包含」)
  "fix_version": "…",                   // known_issue 专属:修复/合入版本(来源③)
  "quarantine": {"reason": "…", "ts": 0} // ❌ 缺——poisoned 处置=隔离标注(不删,留案底+引用清算)
}
```

**派生谓词（全部 O(条目) 可计算，查询时求值）**：

```
lineage(k)      ≝ device  if k.anchor.autoid≠∅  else manual        （血统）
stale?(k)       ≝ validity=verified ∧ lineage=device ∧ anchor.build ≠ current_build
conditional(k)  ≝ ∃k' ∈ 同节点: 主题(k')=主题(k) ∧ observed_under 互异非空   （观察组计数,已实现）
contested(k)    ≝ conflicts_with ≠ ∅ ∨ ∃k' 同主题异血统且谓词相反
poisoned(k)     ≝ quarantine ≠ ∅        （由体检/单调性违例回溯**追加**,非实时判定）
absent(q)       ≝ 检索(q) = ∅           （查询的属性,非条目属性——只有代理信号,见 C2）
latent(k, t)    ≝ k.write_ts > 消费窗(t) （已被即时写回消灭为 0 时窗）
lex_unreach(q,k)≝ 检索(q)=∅ ∧ k 事后被证含正解  （事后审计谓词,产出词汇映射条目）
```

**迁移逻辑 = 事实追加表（谁、在什么事件、追加什么字段、渲染副作用）**：

| 事件 | 执行者 | 追加的事实 | 派生态变化 | 渲染副作用 |
|---|---|---|---|---|
| fail/escalated 轮观察 | closing._ingest（✅） | validity=uncertain + observed_under + anchor.autoid | absent→uncertain | `[uncertain\|语境:…]` |
| 同 fact_key PASS 实证 | merger 升级分支（✅） | validity→verified + 新 evidence | uncertain→verified | 标注消失/更新 |
| PASS 写回 | writeback_one（✅） | 条目 + anchor(autoid,run_ts+build ✅ 2026-07-09——与本节 schema 注同步;stale 派生判定仍未建,见 §5.2) | →verified(锚全) | — |
| 第二语境观察 | closing._ingest（✅） | 新条目 + 互异 observed_under | →conditional（计数派生） | 观察组组头 |
| 冲突发现 | 策展/对账器 | conflicts_with | →contested | ⚠条件冲突横幅（✅） |
| 分辨实验裁决 | attributor/人工 | 胜者 validity=verified + 语境钉死；败者 quarantine 或改写语境 | contested→conditional/resolved | 横幅解除 |
| **build 变更** | 跑批入口探测（❌） | 无追加——current_build 变化使 stale?(k) 谓词翻真 | verified→stale?（批量派生） | `[stale?\|实证于X,当前Y]` |
| stale 复验 | 懒惰:消费前重验（❌） | 通过→anchor.build 刷新；不过→quarantine | stale?→verified / poisoned | 标注更新 |
| 体检命中 | 方向体检扫描（⚠手动预演） | quarantine{reason:方向翻转} | →poisoned | 条目不再渲染,案底可查 |
| 单调性违例 | emit/merged 门（✅ 2026-07-09,缺口B已闭——与 §4/§8/公式(3) 同步） | 拒绝产出（入库前拦截,无条目产生） | —（防 poisoned 出生） | worker 收到违例说明 |
| 词汇鸿沟事后审计 | 归因复盘 | 新条目:意图词→实现词映射(自身带锚) | lex_unreach→reachable | 检索命中 |

**消费端契约**（信号如何改变使用姿势——唯一允许的"按状态分支"，且全部是读侧权重）：

```
verified ∧ ¬stale?   → 直接引用
uncertain            → 可引用,标注不确定;设计实验时优先当仲裁对象
conditional          → 对照自身配置形态选语境分支;不匹配任何语境→当 uncertain 用
contested            → 禁止单边引用;触发分辨实验或如实报告矛盾
stale?               → 引用前复验(语法层 ? 对账便宜先行,行为层懒惰重验)
poisoned(quarantine) → 不渲染;归因/复盘可查案底(负信息量,严禁进 brief)
```

### 5.4 统一原语：K 运维 = 锚差监控

三大运维职能还原为同一操作——**比较条目的锚与现实**：

- **污染检测** = 有效性锚差：断言方向 vs 意图方向（poisoned）；build 锚 vs 当前 build（stale）
- **对账** = 血统间锚差：manual 条目 vs device 观察的同主题谓词冲突（contested 发现）；语法类条目 vs 设备 `?` 自反射（语法层批量对账——文档错误从"事故现场发现"变"库存盘点发现"）
- **流动** = 可达性锚差：写回时间 vs 消费时窗（latent）；意图词 vs 索引词（lexical）

**信号传递模式**（沿用 P1 已验证通道）：状态标注在条目、渲染层带标——`[uncertain|语境:…]` 是首例，同通道容纳 `[stale?|实证于build568,当前583]`、`[contested|与手册冲突,实验可仲裁]`。**信号改变消费姿势，不改变可见性**——与"uncertain 不冒充 verified 但照样入库"同一哲学。

### 5.5 第二状态机（case 台账）的同构纪律与 oracle 残差公理（2026-07-10 yzg 实证补）

case 台账与 K 库是并列的两个状态机，但截至 yzg 批，**K 库的三条纪律没有一条被搬到 case 面**——
yzg 终验吞 fail（3 案整卷 fail 被 pass 终态锁静默丢弃、照常写回先例库）暴露的正是这组缺失：

| K 库已有纪律 | case 台账对应缺失 | yzg 实证 |
|---|---|---|
| 条目带 (validity, observed_under, anchor)——结论永远带语境 | `passed` 是无语境布尔（∃-pass 冒充交付-pass） | 子集 pass 锁死后，整卷 fail 无处落笔 |
| 矛盾观察共存（观察组）+ 实验仲裁，不销毁 | 矛盾裁决被恒等补全吞掉（δ(passed, oracle_fail) 未定义→保持原状） | frozen 信号发了、无人消费，1 秒后写回 |
| 信号闭集从迁移表对抗推导（C1-C10 挑战过） | case 迁移表从未做对抗性补全，「oracle 反证终态」事件类未被枚举 | 无信号名、无 ask 边、无报告计数 |

由此固化四条公理级纪律（编号续 §6 公式表）：

- **语境锚**：case 终态必须携带取得语境（passed@subset ≠ passed@full-volume）；交付判据只认
  ctx_delivery 下的 pass。
- **oracle 残差公理**：每条上机裁决必须恰好落入 ①改变状态 / ②显式核对一致 / ③产生显式残差
  （信号/升级/ask_user）之一，**禁止无痕吸收**。部分函数 δ 的未定义点不得以恒等补全——
  「事件被正确处理且状态不变」与「事件被丢弃」观测不可分辨，是最危险的静默失效形态。
- **全射对账**：digest→台账按 **dom(V)**（本轮跑了谁）正向遍历，不按 L⁻¹(failed)（台账里谁挂着）
  反向索引。两个集合在子集轮巧合相等（掩盖了缺陷两个回合），终验轮不相交（3 fail 落空）。
  问题#7（子集轮 frozen 失活，路径键错位）与 #10（终验吞 fail，状态键错位）与更早的
  「瞬态护栏 dead code」是这同一条缺失公理的三次独立显形。
- **审计器权威**：审计一条假设的机制必须比该假设权威更高。整卷终验是「子集≡整卷」独立性
  假设的审计器，却被置于该假设之下（假设先锁终态，审计结果反而进不来）——审计沦为仪式。

**问询纪律的对称化**：现行 ask_user 两条边（编写期欠定 / 终局升级转述）都处理*认知不确定性*，
且都长在合法状态迁移下游。缺第三条边——**矛盾即问**：oracle 反证已锁结论时，
只有用户有权裁决（如实报告红线）；题面形态后经 §2.6 收敛为呈报-确认式
（不再是「降级/重排/接受单跑」选择题——那是三稿前的形态）。
进不了状态机的事实永远变不成问题，故此边的前提是 oracle 残差公理先成立。

### 5.5.6 (18b) 辖区判据：业务态单源 vs 防伪凭证独立（2026-07-18，folding 门变体 A 实证定形）

(18) 审计器权威管**权威序**；其**凭证面孪生**管**凭证独立性**——一个数据归 INV-7「业务态以事实流为准」
还是 gates-on-credential-path「门挂独立凭证路」，由其**角色**唯一决定：

```
(18b) 辖区判据：
  业务态（引擎派生结论:counts/verdict/deliverable）→ facts 单一真相源（防显示漂移;INV-7/(42)）
  防伪凭证（防伪门放行依据）           → 独立于被检查方（防其自证;gates-on-credential-path）
  INV-7 辖业务态、不辖凭证——凭证本就该独立于事实流。
```

**时序判据（凭证独立性的精确形式）**：抗伪性**不来自**「凭证在 facts 流外」，来自**写入点相对不可绕过
的外部凭证信号（interrupt resume / event.wait）的时序位置**——写在信号前=被检查方可自达=不抗伪；
写在信号后=需真实外部信号才可达=抗伪。精确判据=**门不能读被检查方在凭证信号之前能自达写入的账**。
旧表述「门不能读引擎写的账」**过粗**——会误杀合法 post-signal 引擎写入（lint 凭证 / device_verified
写回 / user_answered 皆引擎写、皆记「外部信号已发生」的确定事实）。

**推论A（门专用凭证的空间选择）**：单源纯度**只对业务态必需**；门专用凭证（非业务态、单一消费者=门）
post-signal 下**独立凭证路 > 塞 facts 单源**——塞 facts 需靠「写点必 post-signal」的隐形不变量（无编译期
守护、判据脆弱面），独立凭证路（ask_user 内 `event.wait → write` 同函数体）是**结构锁定的 post-signal**。

**推论B（F 豁免二分）**：「引擎写的账」分两类——写引擎自己的判断（grade 自评 / 归因结论）**禁**作抗伪凭证
（自评之死，DS-2）；写外部不可绕过信号已发生的确定记录（lint 记「机械门已过」/ user_answered 记「真实
应答已发生」）**应豁免**。判据=写入点是否被引擎不可自达的外部信号门控。

**folding 门三源恒等（本轮实证，与 DESIGN §18.16 互为投影，各写各域、互引不重复）**：变体 A 的抗伪闭合
= post-signal 写点 ∧ 折叠正确性（**凭证 folded_members == 代表题面披露成员 == 用户实际看到**）——第三个
恒等靠展示层截断修保证（串联知情同意链，缺一环链断；守门测试只保证组装侧恒等，用户侧完整送达依赖
截断修）。

**工程消费者（C14）**：folding 门变体 A（先问后落门读凭证路、folded_members 经 event.wait 后落盘）；
lint 凭证 / device_verified 写回的合法性由推论B 解释。**可证伪**：出现①合法防伪门必须读被检查方在凭证
信号之前自达写入的账，或②业务态计数被正确地从非 facts 源供给——则辖区判据破。守门族关系：**(48) 管事实
进流前不被误吞、(16) 管裁决入流后不被无痕吸收、(18b) 管凭证归属**——三者封闭事件溯源的进/显/凭证三口。

## 6. 理论公式汇总

```
(1) Sound(C)      ≝ (P ⊩ D) ∧ (φ(D) ⊆ closure(E)) ∧ (R*(P) ⊨ E)
                     （②=意图特征覆盖,集合论良定;φ 无统一实现,已实现子域投影见 §2）
(2) Verify(C)      = [O ⊨ E],  O = Exec(P) + ε          （只测合取③;Exec 语义见(43)）
(3) Monotonic      ≝ M(Asserts(new)) ⊆ M(σ(Asserts(old)))   （模型集包含=逻辑蕴含;
                     实现=语义类集合不减的充分条件近似,合取②在压力路径的可判定投影）
(4) Ω              = {D错, P错, E错, K错, K缺, 缺陷, ε, 欠定, infra域}
                     （⑥ infra域 R10 补格:细分与治理居 THEORY_infra_reliability,
                     判定树第零层域分诊即其转出口,见 §3.1/§3.2）
(5) 可导性         : ∀fail, 沿分层判定树在有限次原生算子调用内 ω 唯一
                     语法/状态/通道层 O(1)；行为层 = 实验二分
(6) σ(k)           = (validity, reachability, anchor),  anchor = (build, run_ts, lineage)
(7) 检测原语       : Δ(k) = dist(anchor(k), reality)    （锚差；所有信号的母式）
(8) K 演化         : K_{t+1} = K_t ∪ Obs_t \ Poison_t
(9) 中心论断       : 规划质量 ≤ 模型质量（推导版,定义性质）;K 沿锚差/污染/完备/可达
                     四维退化 ⇒ 编译质量随之退化（各维检测器状态见 §4 退化维度表;
                     旧 f(·) 单调函数式因无度量不可证伪,已废）;prompt 为低阶项
(10) 正解          : K 完备 ⇒ Sound 等价类非空可判定；K 不完备 ⇒ 经实验有限逼近
(11) 信息量分解    : I(C) = I_G·e_G + I_E·e_E + I_V·e_V（正交直和;e_i·e_j=0 ⇒ 不可补偿）
                     I(a) = 1 − |{s∈S: s⊨a}|/|S|（有限空间计数测度;恒真 ⇒ I=0,可判定谓词
                     即必崩门;旧香农式因参照分布未定义已废）
(12) 覆盖判据      : dim_V(C) ≥ dim_V(D)   （代理: genuine_v_count vs step_intents 行为断言数
                     ——右侧为粗代理,按条计数的粒度问题见 §2.5.3-A2,无统一提取器）
(13) 守恒律        : 重编 ΔI_V ≥ 0          （单调性检查的信息论内容；644 违例=dim_V 3→2）
(14) K 净信息量    : 〔降注记 2026-07-16——C14 纪律「公式须有工程消费者」自裁:I(k) 逐点
                     不可算(自认)、Σ 更不可算,全 repo 零检测器/门/监测消费它;可操作内容
                     已全部在 §5 stale/poisoned 检测。编号位保留(零重编号),原文降为
                     §2.5.3 末注记,不再作为运行目标函数引用〕
(15) pass 语境化   : Pass(c, ctx)；交付判据 = Pass(c, ctx_delivery)；
                     ∃ctx.Pass ⇒ Pass(ctx_delivery) 仅当 case 独立性成立（保存族反例）
(16) oracle 残差   : ∀c ∈ dom(V): consume(V(c)) ∈ {迁移, 显式一致, 显式残差}——禁止第四种
(17) 全射对账      : 对账遍历 dom(V)（本轮裁决集），非 L⁻¹(failed)（台账反查）
(18) 审计器权威    : authority(审计机制) > authority(被审计假设)，否则审计为仪式
(18b) 辖区判据     : 业务态→facts 单源(防漂移) / 防伪凭证→独立于被检查方(防自证);
                     时序判据:门不能读被检查方在凭证信号之前能自达写入的账(「门不能读引擎写的
                     账」过粗——误杀合法 post-signal 写入 lint/device_verified/user_answered);
                     推论A 门专用凭证 post-signal 下独立路>facts 单源(结构锁定免隐形不变量);
                     推论B F 豁免二分(写判断禁/写信号门控的记录豁免)               §5.5.6
(19) Ask 定理      : ask ⟺ 决策依赖应然 ∧ K_ought 检索余项非空
                     实然=可由{读,查,实验,算}决定(永不问)；应然权威=意图所有者
(20) 收敛律        : 确认必写回 K_ought(判例键+锚) ⇒ 同键至多问一次，ask 单调不增
                     自愈 ≝ K_is(真PASS写回) 与 K_ought(裁决写回) 双层自动增长
(21) 采信规则      : 采用(记载) ⟺ 命中记载间无互斥 ∧ 不与实机冲突 ∧ 不改写意图面
                     改写=与 D 文本/既有 E 语义相抵；填充欠定空缺≠改写(A11)
                     K_ought 写入权=人源专属；引擎推断只入 uncertain 观察
(22) 行动生成律    : A_{t+1} = A_t ∪ {a: LLM 提案 ∧ oracle(a)=pass ∧ [高危→人批]}
                     闭合的不是动作集合是入库门；规则是验证出来的不是人写的
(23) 知行对称律    : 行动侧(提案开放/oracle+人批闭门/组合归LLM/落账归引擎)
                     ≅ 知识侧(观察开放/人源写回闭门/判断归孔/落账归引擎)
(24) 前筛律        : 批级判断触发=机械前筛(签名簇占比≥θ)；θ 是资源规则非场景规则
(25) 外界写双通路  : 写共享资源 ⟺ 机械逆放己方记账 ∨ 人批提案(携行动判例键,同键免批)
                     LLM 只有触发权与组合权,无无验证的生成执行权(INV-9 行动论表述)
                     记账主体第一人=引擎床账(快照diff+本批命令面交叉验证认己方,2.7.7)
(26) 非法 ask 判据 : ask 非法 ⟺ 所需信息与权限均在引擎侧应然可达集(实际可达∪记账义务应载)
                     应载而未载=非法 ask+工程债双记:修债不问人(收敛律的实现质量护栏)
(27) 证据保真公理  : 进入判断的证据必须携来源链+归属一致性自检结果;不一致=证据可疑
                     事实+判断降级如实声明。取证器与审计器同样要被审计((18) 的成立前提)
                     域内展开见 THEORY_infra_reliability §7
—— (28)-(33) 居 THEORY_target_system_algebra;(34) 时间轴为候选(缓立,见彼 §10)——
(35) 判决三链      : Verdict = 意图链 ∧ 对象链 ∧ 过程链(三理论=三因子空间;
                     完备性审计=管道逐跳查三件套;第四链出现=预注册证伪)  §2.9.1
(36) 统一写权律    : 写通路由权威归属唯一决定——引擎应然可达→机械(带锚带账)/
                     权威在人→呈报/缺失冲突→ask;(21)(22)(25)(26) 为其四实例
                     推论:人的不可消去位=应然权威+信任根声明,其余人工=工程债  §2.9.2
(37) 框定保真公理  : 上游框定链(brief→注意力→判断)是一等保真对象——假设不开路/
                     意图占 recency 高位/修复轮历史错向按引用降响;(27) 的孪生
                     与 (9) 两层和解:K 定批级、框定定修复轮(M3 预注册监测)  §2.9.3
(38) 写者全集公理  : Writers(S)={案级 CLI,流量,框架例行,管理面,运维,归因实验}——
                     污染分析/床账/影响面的正确性以写者覆盖面为上界;运维写必须
                     入账(维护日志通道)                                    S §11
(39) 用例六元组    : case=⟨s₀,σ,e,π,R,τ⟩——τ 还原为构成性要素,τ∘exec≈id(共享
                     分量);τ 是下一案 s₀ 的前置;案级 τ 辖复位差集,机械可派生
                     (逆元回放);工程门=配对恢复门(呈报不硬拒)               S §0.4
(40) 处置分类学    : fail 处置对象{受害者/自污染者/互污染者/真环境/真缺陷/欠定/
                     期望可疑(第七类,2026-07-14 T5 增补)}×唯一非绕路出口;
                     终止性=处置映射下严格降秩;自污染者配复跑出口=永动机
                     (run12 五循环实证);满射完备性=对七类           §2.12.1
(41) 问询链保真    : 面板→人→答案→消化四跳可失真(截断/对位/token 化/**提交**——
                     run15+run17 两次实弹:多题面板静默部分提交),消化结果必须可被
                     用户核对(决策 echo-back);(27)(37) 的第三孪生          §2.12.2
(42) 报告保真      : Report=render(fold(facts)),render 零自由度;交付前独立重算
                     比对,失配拒绝交付——报告信任是 poisoned 的产生点,须设防 §2.12.3
(43) 错误吸收态    : Σ* = Σ∪{⊥_err},δ 全函数;¬ok(g) ⇒ δ(s,g)=⊥_err,
                     ∀g: δ(⊥_err,g)=⊥_err(吸收律,封闭)——初版"部分函数+未定义
                     态求值"破坏严格性已废(668030 空真实证)               §2.12.3b
(44) 断言求值三分  : Evaluate=Broken 若 s=⊥_err / Pass 若 s⊨e / Fail 若 s⊭e
                     (≙xUnit ERROR):不计签名/不深归因/不入交付分母        §2.12.3b
(45) 判例源权威    : 人源判例采信先验>机生;机生可采信性以经 oracle 残差审计为
                     前提(未审计 verified 卷降 uncertain);记载互斥时 (21) 采信门
                     原样接管(人源不自动赢)。(45b):检索排名不得被自产血统垄断  §2.9.4
(46) 问询三元组    : ask 题面必呈⟨R(带脑图出处子串),阻碍事实,已推导最优替代⟩;
                     没完成推导没资格问(第三分量非空是发问前置义务)         §2.9.5
(47) 执行位阶律    : 给定不变式 I,其强制落哪位阶——L_struct/L_oracle/L_human/
                     L_model 四位阶;正解常分解到多位阶非二选一;模型判断永不
                     单独当对自身产物的质量门(grade 之死)                  §2.9.6
     [(30) 链底消费补记 2026-07-13] 承载合取律的链底=设备可达性——run14 实弹:
     设备批中失联,s₀ 配对(必要条件)在失联批恒命中致 11 案批量误诊。判定树
     真正的第零层=可达性探针(先于域分诊);工程消费=digest 失联降 broken+
     run 入口拒跑(DESIGN §18.8)。教训:C 级公式的欠账以乱断言问询的形式
     向用户支付利息——问询有效性依赖承载链每一级的验证。
```

## 6.5 数据锚定（点估计表已删除——2026-07-13 中等力度校准）

原「理论量的现有点估计」表（压力路径失真率 1/2、poisoned 3/325、fail 毒率 1/5、stale 1/4、
归因误判 3/9、欠定 7/34、K→R1 斜率、重编翻正 4/4、观测失真 3/6、uncertain 产能…）＋「yzg
双回合新增点估计」＋「§6.5.1 交付卷保真审计」**整体删除**：全部是单域/极小样本（n=1~9）/
依赖已丢脚本的点估计，把它们当「理论量的锚定值」会误导设计（拿个位数观测当参数标定）。

**但它们推导出的设计规则是对的、且已在代码中执行**——保留为定性纪律（不带标定数字）：
- 单调门只守 **override/重编路径**（失真富集在压力路径与复杂流程，非交付常态）；
- stale 复验用**懒惰策略**（消费前验）而非全库扫，语法层 `?` 对账便宜先行；
- 毒扫描优先排查 **"fail 后强行 PASS" 血统**的卷（毒富集于此血统）；
- 欠定问询**不做默认预填**（预填有引导偏差，逐题实答，仅选项排序置顶）；
- 归因**版本层证据在场**时正确率显著高于缺席（dev_help 机制已接线；因果方向作为 DS-1 采集目标，§7）。

仍可复现/可核的硬事实另存别处（不在此处以「基率」形态标定）：build 锚源②失真（台账 …568 vs
设备自述 10.5.0.585，verified_runs 可查）在 §5.1/§8 映射表；yzg 同批受控对照（仅改环境锚）在
§4——那是同批 A/B，非跨批小样本斜率。

## 7. 数据集设计

理论要可检验，需要把金牌数据固化为四个数据集（建议落点 `knowledge/data/theory_eval/`，JSONL，git tracked）：

### DS-1 归因金标准集（attribution gold set）

```jsonc
{"case_id": "044572-selfheal2-r1",
 "observation": {"device_context_ref": "...", "fail_signature": "^ 拒绝 sdns pool cname"},
 "layer_gt": "syntax",                      // 语法|状态|行为|值域|通道
 "root_cause_gt": "case_error_P",           // Ω 中的 ground truth
 "resolution_path": ["? 直查(未做)", "历史先例对照", "换形态重编→PASS"],
 "wrong_attributions_observed": [{"by": "attributor-fork", "claimed": "defect(BUG-149877)"}],
 "evidence_refs": ["..."]}
```

- 种子：本会话 13+ 案（413/453/493/570/608/644/044572/毒卷族/exp_recheck5 三案/588691 族）。
- 用途：**归因器（机械+LLM）的回归评估**——每次动归因链跑一遍，报准确率与层级分派正确率。地位等同 511 卷反扫之于 grade_extract。
- 维护：每个 escalated/defect_candidate 案例人工裁决后追加（裁决本来就要做，边际成本≈0）。

### DS-2 意图保真集（fidelity set）

```jsonc
{"pair_id": "644-weakened",
 "intent": {"step_intents": [...]},          // D 的结构化投影（manifest 原件）
 "sheet_asserts": [...],                     // E（卷面断言全集）
 "label": "weakened",                        // faithful|weakened|inverted|overreach
 "mechanism": "frozen override 删 AAAA 断言"}
```

- 种子：毒卷 2 例（inverted）+ 644 弱化卷（weakened）+ 26 张 R1 正卷（faithful）+ 035373 迁就卷（inverted）。
- 用途：**补 grade 之死缺失的度量基础**——历史上从未有"忠实性"的 ground truth，942 对配对用上机结果错标了忠实性判断。未来任何合取② oracle 候选（含 LLM 判定器）先在此集上量判别力，达标才准入——避免 grade 重演。
- 关键设计：标签是意图↔断言关系，与上机结果**独立**。

### DS-3 K 状态标注集

- 对现库分层抽样（先例卷/footprint 条目/判例观察各 ~50），人工标七态。
- 用途：污染/陈旧扫描器的查准查全评估；发布 K 健康度基线（各态占比）。
- 已有存量事实可直接入种子：三域 325 卷恒真反扫结果（归档区 3 个历史假 PASS 卷 = poisoned 实证）、五卷体检记录。

### DS-4 K-性能曲线（中心论断的持续监测器）

Schema（每批一行）：`{batch, mindmap, k_state:{precedents, poisoned_known, anchor_drift, flow}, r1_hit(首跑口径), final, rounds, cost_cny}`。

> **种子数值已删（2026-07-13 中等力度校准）**：原内联的 selfheal2/yzg 种子行含误标/口径混用数据
> （selfheal2 首跑 6/13 被记 11/13=终态；pe1 数据不在盘），不再内联。唯一数据源=committed
> `knowledge/data/theory_eval/ds4_k_performance.jsonl`，且**引用前必须先做口径归一**（r1_hit 一律
> 取首跑上机 pass，与 dongkl 18/34 同口径），否则曲线不可比、误导中心论断。
> **数据文件已同步（2026-07-16 对抗修订闭环）**：此前 committed 文件自身仍带 11/13（终态误标）与
> 不可复算的 pe1 9/13——「口径归一」指令对当时文件不可执行（6/13 不在任何字段）、预注册的死法在
> 数据层是名义的。已修：selfheal2 行 r1_hit=6/13（首跑口径）+caliber=corrected_20260716；pe1 行
> 标 caliber=unverifiable（仅供定性）；README 行数同步（ds1 20 案/ds4 4 点）。

- 用途：**中心论断的可证伪监测**——若出现 K 健康而 R1 命中率持续低的批次，§4 被证伪，理论须修（见 §9-C6）。**前提：r1_hit 口径统一为首跑**。
- 增列（R7-M3 预注册）：框定配置维度（brief 布局版本/去劫持开关）——若框定改动的
  批级效应 ≥ K 效应，(37) 的两层和解被打。

### DS-5 自愈度曲线（2026-07-12 立）

Schema（每 run 一行）：`{run, new_keys, recur_keys, recur_auto_resolved, human_events:{total, genuine_problem, options_effective}, precedent_adoption, session_complete}`。

> **种子数值已删（2026-07-13）**：原「run5=1/6 → run11=4/7」等首条基线依赖已丢的 scratchpad 词干
> 定义（复键的精确计数不可复算，方向可复现但分数不可）——不再作为标定值内联；曲线以 facts.jsonl
> 现算为准（脚本待正式化入 `scripts/`）。

- **口径**：每 run 报「新键数/复键数/复键当轮引擎消化占比+要人事件率」；键=
  (autoid, fail 签名词干)/(主题, 面板类型)/(aid, layer×disposition) 三族；自愈
  判据=复现后同 run 内 `verdict:pass` 且无要人事实夹层（机械链可指认）。
- **样本准入=会话完整性**（run_start 到交付/挂起有终局事实）——人为中止不算
  不自愈（run7-10 结构性零的教训）。声明：测同域自愈，不测跨域冷启动（跨域
  按域分层另设）。
- **质量维度（DESIGN §14-R5 用户四标准，替代数量阈值）**：①问题真实率、②选项
  有效率（两布尔随裁决写回标注，用户答题即标注零额外成本）、③同键复问率
  （收敛律 (20) 既有）、④判例采信率（「人工怎么处理的 agent 按同法处理」的
  度量形态）。
- 基线数值不内联（见上「种子数值已删」）：方向性事实=床账/存量清偿后 T1 判据逐 run 下降、
  复键当轮消化随之上升；精确分数以 facts.jsonl 现算为准，脚本正式化后再回填。

## 8. 与现有系统的映射

| 理论构件 | 系统现状 |
|---|---|
| 合取③ oracle | 上机 digest（强，已有） |
| 合取② 压力路径投影 | 记录类型 gap（词面子集）+ 单调门（2026-07-09 落地：重编观测维度集合不减，缺口 B 关闭） |
| 合取① 半判定 | 配置元素 ref 溯源（emit 契约已有链，闭包检查未建） |
| 语法层算子 | ✅ dev_help 已接线 digest 归因预判（2026-07-09；真机首秀：`sdns pool cnome` typo 追问），signal `syntax_help_attached` 同步落 |
| 行为层实验 | 形态检验轮（已落地）、A/B 仲裁（人肉验证过） |
| uncertain/conditional/升级/观察组 | P1 自愈环（已落地）= 状态机右下角首批实现 |
| latent 消除 | PASS 即时写回（已落地） |
| stale 检测 | build 锚最小段已落（verified_runs→条目 device_run；**源②记提交值会失真**——yzg 实证 568vs585，源①设备自述+锚差判定未建） |
| poisoned 拦截 | 体检为手动、拦截门未建——唯二之二 |
| **case 状态机语境锚/oracle 残差（§5.5 四公理）** | **窗口出处面已落（2026-07-14）**:digest 旁路重分段原始流与断言窗对账,方向矛盾=broken(DESIGN §18.10,`_window_audit`+`test_window_audit.py`)——实弹坐实公理预言:3 run 假 FAIL 7/假 PASS 12（窗口=3 run,与 (45) 的「103 全量窗口清点」口径不同,勿直比）,668000 假 PASS 三连写回投毒后被本门反扫揪出。语境锚其余面(#10 终验吞 fail 同根)仍未落 |
| 矛盾即问（第三条 ask 边） | **部分机构已落**(DESIGN §11.11 A 片 ask_contradiction/contra≥2,2026-07-13)——「未建」指残差公理全落地后的完全形态;脑图 vs 设备入口边随 F1 立项(DESIGN §18.11) |

## 9. 对理论的挑战与未决问题（诚实清单）

- **C1 合取②全量不可判定**：忠实投影的一般判定=语义理解。记录类型 gap 抓 644 靠"AAAA"词面出现——纯巧合，脑图写"v6 地址"即失效。唯一全量实现是独立 LLM 判定器，而其判别力从未被正确度量（grade 之死的 942 对配对拿上机结果错标忠实性）。**当前立场：单调性守压力路径；R1 直接歪写 E 的威胁现代系统下实证为 0，列为待观察；DS-2 是任何未来候选的准入门。**
- **C2 absent 原理上不可观测**："不知道自己不知道"只有代理信号（检索零命中 ∧ 事后正解在 R\*）。自愈环把 absent 的暴露成本压到"一次 fail"，但压不到零。②′ 类根因的首次遭遇必然付一轮实验学费——这是知识论边界，不是工程缺陷。
- **C3 stale 扫描的成本与观察者效应**：build 锚只能缩小嫌疑集（build 变了 ≠ 语义变了），确认 stale 仍需重验，全库重验烧设备。可行策略只有懒惰验证（消费前验）+ 分层优先（语法层 `?` 对账便宜先做，行为层抽样）。锚差是必要信号非充分判据。
- **C4 状态机自身的复杂度风险**：七态×四可达=组合膨胀？辩护：状态是**条目上的标注**不是流程分支——消费端只读标注调权重，生产端只有锚差比较器。若实现中出现按状态组合写 if 的代码，即违背本设计。
- **C5 锚的可信性递归**：锚差监控的前提是锚不可伪造。run_ts/verified_runs.jsonl 在 runtime/（agent 沙箱黑名单）——锚可信度依赖沙箱完整性，沙箱是本理论的信任根。
- **C6 中心论断可证伪性**：若未来批次 K 健康而硬骨头依旧（DS-4 监测），§4 被打——届时候选解释按序重启：合取②首轮失真（C1 待观察项转正）、行为层实验设计不足、D 解码方差。**理论的死法已预注册。**
- **C7 词汇映射条目的递归性**：意图词↔实现词映射自身是 K 条目，同样会 stale/poisoned（产品改名）。量小，接受递归一层。
- **C9 manual 血统无 stale 锚**（对抗检查 A4）：手册无 build 粒度（manual_10.5 覆盖整个 10.5 家族），跨 build 行为差异（568→583）手册不区分——manual 条目的时效性**不可锚定**，其 stale 只能经 device 血统观察冲突（contested 路径）间接暴露。stale?(k) 谓词只对 device 血统有效是设计而非疏漏。
- **C10 poisoned 的引用传染不可查**（对抗检查 A6）：条目被 quarantine 后，引用过它的下游产物（据它编的先例卷、以它为 evidence 的观察）无引用图可循——644 弱化卷的回滚靠人工记忆。自动清算需要引用链索引（provenance 的 ref 链是雏形，未建反向索引）。当前立场：quarantine 记案底 + 富集扫描（fail 后强行 PASS 血统毒率 20%）近似定位传染面。
- **C11 case 状态机未经对抗性补全（2026-07-10 yzg 实证，已定性）**：信号闭集/迁移表的对抗
  推导只做了 K 状态机；case 台账的迁移表存在未定义点（δ(passed, oracle_fail)），实现以恒等
  补全——三次独立显形（瞬态护栏 dead code / #7 冻结失活 / #10 终验吞 fail）同根于
  「digest→台账无必达契约」。§5.5 四公理是补全方案；落地前该缺口视为开放。
- **C8 conditional 的语境不可枚举**：observed_under 是自由文本，语境空间不闭合；观察组计数是弱信号（≥2 才触发，首个语境相关观察无从标记）。缓解：conditional 是默认怀疑姿势（任何 behavior 观察都可能语境相关），渲染层"多语境观察，对照你的配置形态取用"即此姿势的固化——**宁可让消费者多一分怀疑，不给机器加语境枚举的假精确**。
- **C13 范式错配的结构性风险（2026-07-12）**：本理论曾两次试图向基础设施域扩张（批内
  B 差分检测器提案被用户否；K_infra 本体提案被文献否）——**理论有自我扩张倾向，每次
  失败都倾向解释为"再补一节理论"**。防线：新问题先过彼域分类学对号（engine-verify-loop
  理论层前置步骤）；本理论的增长必须伴随适用域声明。
- **C14 自我对抗的 recall 天花板（2026-07-12 R10 裁决,方法论级）**：R1-R9 九轮对抗
  「全部通过」,但 run12 实弹一天抓 3 缺陷、用户一句话抓 τ 缺元、fresh-context 双
  agent 一下午抓 25 个盲区——**生成者与对抗者同一模型同一上下文,盲区共享**(R8 八攻
  无一命中五元组缺元,R9 八攻无一命中处置分类学不完备)。实测有效通道排名:①实弹数据
  ②成熟范式对标(xUnit 抓 τ)③用户反问 ④fresh-context 审计 ⑤自我对抗(只抓得到措辞
  与边界,抓不到本体缺元)。**制度:对抗协议升级为三通道强制——自我攻击仅作初筛;
  范式对标扫描(该领域教科书第一章讲什么,我们有吗)为必选步;可检验预言为公理转正
  门槛**。附属纪律:公式须有工程消费者,无消费者的降为注记(防理论库存积压);每文档
  有自己的惯性盲区方向(S=词表封闭性/infra=自反性/K=异步人类),对抗时按此组织攻击面。
- **C12 行动论的冷启动与视野边界（§2.7 两轮对抗的站住项）**：行动判例库空载期纯付出
  （蒸馏/检索 miss/人批频繁），有界性靠"公共原因形态量级 ~10"的假设——若形态长尾比预估
  重，收敛慢于 3-5 批（E6）；批级诊断的视野=本批事实+检索半径，跨批因果链依赖检索命中
  而非结构保证（A4）。两者均预注册为 DS-4 型监测项：库命中率与人批频率若不随批次单调
  降，(22)-(25) 的成本模型被打。

## 10. 证据索引（案例 → 结论）

| 案例/实验 | 支撑的理论构件 |
|---|---|
| yzg 双回合受控对照（3/26 vs 24/26） | §4 中心论断（锚差第四参）；§2 合取③语境参数 |
| yzg 终验吞 fail（668015/030/044） | §5.5 oracle 残差公理/全射对账/审计器权威；C11 |
| yzg 归因自然实验（2/19，机械证据 2/2 vs 0/17） | §3.2 语法层读表价值的因果量化；DS-1 |
| yzg 655233 vlan 掉维度（PASS 假覆盖） | 验证盲区定理（合取① P⊬D 载体维度形态）；C1 投影盲区 |
| yzg 233 interface 污染链（9 案同 IP 超时；两孔散文全对被枚举拍扁；per-case 归因 9 份重讲一个故事） | §2.7 行动论全节（缺口本体+(22)-(25)）；E1/E7 的 token 数字（9×849k vs 1.06M） |
| yzg run8 同坑复撞（序①后：编写省 56%；persistence 门同轮拦别案却漏 233；X8 后归因未追到源头；233 两轮均未呈报 method_vs_implementation panel） | §2.7.6 覆盖裁定（机制无罪枚举增长方式是罪）；diagnose 论据②；C 层触发缺口 |
| yzg run9 非法 ask 裁决（233 自决等价路径+案首自清=X10 生效；归因均价 849k→150k=X8 生效；env 4 题被裁不该问——账内己方产物该自动恢复） | §2.7.7 (26) 非法 ask 判据+(25) 床账第一记账人；G1 账空时代最后合法 ask；G4 己方交叉验证 |
| yzg build 锚 568vs585 | §5.1 锚源①（设备自述）优先于源②（提交值） |
| 644 删 AAAA 假 PASS + 弱化卷写回 | 验证盲区、poisoned 产生点、单调性缺口、压力路径分布 |
| 首批 608/570 毒卷（E 反写，当年 PASS 入库） | 验证盲区、poisoned、K 污染史 |
| 493 三轮困死 vs K 恢复后一轮翻正 | 中心论断、②′ 不完备、"搜索低效"证据的掏空 |
| 413 lastresort（手册有、检索不到、历史卷重跑 PASS） | lexically-unreachable、K 问题≠D 问题 |
| 453 pool disable 被拒判死 → 解绑形态 PASS | 归因越权、①/③ 观测等价、形态检验必要性 |
| 044572 语法变体被 ^ 拒 → attributor 引缺陷库判死 | 语法层可判定性、缺口 A、单点观测不可辨识 |
| 570/608 monitor down 正解（A/B 仲裁 + selfheal1 自主解出 + selfheal2 三轮迭代保真 PASS） | 实验算子可导、正解等价类、即时写回价值 |
| exp_recheck5 三案 dig 超时 + digprobe/D3 对照 | 根因④观测通道失真、分层判定树"观测可信吗"层 |
| E5a disable 对照段（该拓扑 DOWN 全链传递） | conditional 本态、"disable=纯管理标记"观察的语境相关性 |
| 942 对 grade 配对（判别力 3pp） | C1、DS-2 的必要性（忠实性从未被正确度量） |
| 511 卷等价反扫 / 325 卷恒真反扫 | 机械门方法论、DS-3 种子 |
| P1 自愈环（uncertain 6 条入库/升级/观察组，pe1 608 演练零代码） | 状态机右下角实现、信号传递模式 |
| v1→v8 全史考古（30+ 文档四路审阅；delta 聚集三链接缝） | (35) 三链完备性；§2.10 L 语言回收；§2.11 定量线翻案 |
| 五路检验（3 数据+2 文献，RESEARCH_theory_data_checks.md） | (37) 文献锚；2.11.2 判别力 n=72/AUC 0.788；2.11.3 semantic entropy 同构；DS-5 首条曲线 |
| 035413 三轮锁死 vs main 一次中（TRAJECTORY） | (37) 框定保真（修复轮内收敛/发散的定性证据） |
| 检验一：yzg 时间轴不支持（富集混杂假象+2 配置面翻转反例） | S 文档 (34) 缓立的依据；「数据不支持即停」纪律实证 |
| run12 实弹（三缺陷+五次修床循环+面板截断/竞态+名义26/26 前科） | (39) τ 缺元；(40) 分类学与终止性；(41) 问询链保真；(42) 报告保真；C14 对抗独立性 |
| R10 全量审计（三通道:双 fresh-agent+范式对标+实弹,25 盲区） | §2.12 全批；(38) 写者全集；Ω ⑥类+判定树第零层；C14 方法论 |
| 六项范式对标（NeTestLLM/iPanda/Keysight/pyATS·Genie·NAPALM/NetConfEval/AutoSpec，2026-07-15） | §2.13 全节；(44)↔pyATS 7 码结果代数；§5.5↔三 LLM 竞品全缺独立 oracle；断言有效性↔AutoSpec MA/TA |
| dongkl 续跑（210998 broken 活实例 / 593516 h-变量断言 flaky 写回投毒 / env_blocked 2/2 误判） | (44) broken 显式态首现；S §0.5 h-不变式违反；(40) env_blocked 缺内部一致性后校验；§2.13 |
