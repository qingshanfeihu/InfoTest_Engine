# #45 执行批·实弹验证清单（Test-Eng 验证清单员）

> 2026-07-19 · Test-Eng · 用户定谳 ①A②B③A④B⑤A⑥C（#45 团队自决制+用户否决权）后建。
> **老规矩**：修复=**实弹验证了才闭环**。各件落地后逐项对照下方「可观测验证点」，下个编译轮/启动/pytest 见到即记 ✅、未见即挂 ⏳。
> **观测类型**：📄 文档核（fs_read 即验，非编译轮）/ 🔧 启动·gating 核（起 TUI 即验）/ 🏭 编译轮实弹（需跑批自然触发）/ 🧪 pytest 门（机器红绿）。

## 六件验证点

### ①A 缝合合法化（理论补公式(15)等价条款 + 实现逐条机械自查）
- **落地物**：THEORY 公式(15)增补「缝合语境等价条件」；实现照条款逐条机械自查（断批后拼接前检查）。
- **可观测验证点**🏭（条件触发）：下个编译轮**若遇断批**（网络闪断/中止），拼接前逐条自查——①床未被动过 ②前段已通过凭证有效（mtime 新鲜）③断点后子集补跑通过、broken 不吸收；三条满足才拼接成交付确认，不满足则拒绝拼接（回退整卷重跑或降级）。
- **观测法**：`facts.jsonl` 缝合自查事件 + 终验幂等闸/delivery 日志（`b3ce3b4b` 断批例外锚）。
- **闭环判据**：遇断批时见「逐条等价自查通过→拼接」或「某条不满足→拒绝拼接」。**注：断批偶发**，下轮不自然出现则标「条件触发时验证」，不强造。

### ②B 单元 E（评测基建）缓立（设计文档显式写下「缓立」）
- **落地物**：DESIGN §18.15 补显式「单元 E 缓立」条款（治「取代/搁置只做不说」的 P0 悬空）。
- **可观测验证点**📄：DESIGN §18.15 有显式缓立字样（不再零字悬空）。
- **观测法**：`fs_read docs/DESIGN_v8_engine.md` §18.15。
- **闭环判据**：见「单元 E·缓立·显式」条款在位（非「已被 dongkl 取代」空横幅）。

### ③A config_generator 管线退役删除（约 1200 行 + 激活映射清理）
- **落地物**：删 `config_generator.py`+`modules/`+`utils/`+`scripts/`（SKILL.md 保留）；tool_gating 激活映射对应行同步清理。
- **可观测验证点**🔧：①物理删除（整套 9 py 不存在、SKILL.md 在）②`middleware/tool_gating.py` 激活映射 config-automation 行清理干净、无残留引用 ③config-automation skill 仍可 invoke（走 LLM 直读拓扑路径、不依赖脚本）④启动 TUI/编译轮 gating 无异常（fail-open 或正常激活）。
- **观测法**：`fs_ls` skill 目录 + `fs_grep` tool_gating 映射 + 起 TUI 看 gating 无报错 + 试 invoke config-automation。
- **闭环判据**：物理删除确认 + 映射无残留 + skill 激活正常无 gating 异常。

### ④B DS-1/2/3 数据集最小承接（设计文档补最小承接条款）
- **落地物**：DESIGN 补 DS-1/2/3「最小承接·缓立+自然扩充」条款（消费/时机/转正显式化）。
- **可观测验证点**📄：DESIGN 有 DS-1/2/3 最小承接条款（治「立而不接」悬空）。
- **观测法**：`fs_read` DESIGN DS 节。
- **闭环判据**：三集各有「最小承接·缓立+零边际成本自然扩充」条款在位。

### ⑤A worker/attributor 指令小修（批后启动·redline 后合入）
- **落地物**：`compile-attributor.md` 等指令补——①归因器锚定卷面实际观察命令 ②归因输出语言指示（英文 LLM-facing）③评审入口白名单缺项 ④引擎 skill 拓扑一致性断言。（⑤⑥同两份指令文件→同批合并实施，一次 redline 一次合入）
- **可观测验证点**🏭：下个编译轮——**(a) 蒸发消失**：行为观察被锚门拒后归因器**重试**（不像 035644/035453 那样不知锚约束、没重试就收尾、观察白白蒸发）→ footprint 写回落库；**(b) 语言漂移消失**：归因输出语言一致（全英文，无一次英文一次中文）。
- **观测法**：编译轮 `facts.jsonl` 归因/writeback 事件（锚拒后是否重试落库）+ langfuse 归因输出语言 + footprint 是否新增该批观察（不蒸发）。
- **闭环判据**：有 fail 案走归因时，锚拒后重试落库（观察不蒸发）+ 归因输出全英文。**注**：需该批有触发锚门的行为观察才自然暴露。

### ⑥C prompt 行数预算换约束（废行数上限→「每行有据」机器检查）
- **落地物**：废行数上限，改「规则条数 == 归属行数」（每条规则在理论映射表有归属行），做成机器断言：对不上不放行。（可叠 B 熔断宽松上限如 150 行）
- **可观测验证点**🧪：机器门红绿——规则条数==归属行数断言过（对得上放行）；故意造一条无归属规则→门红（拒）。
- **观测法**：`pytest` 跑新增机器门 + prompt 结构门（`tests/ist_core/agents/test_prompt_structure.py` 族）。
- **闭环判据**：机器门绿（对应放行）+ 反例造不对应→门红（拒）。leader 亲跑权威 pytest 数认。

## 验证节奏与边界

- **即验（文档核 📄）**：②④ 各件落地即 `fs_read` 核条款在位（不等编译轮）。
- **启动验（🔧）**：③ 起 TUI/下个编译轮启动即核 gating 映射干净。
- **pytest 验（🧪）**：⑥ 机器门随合入 pytest 即见红绿（leader 权威跑）。
- **编译轮实弹（🏭）**：①⑤ 需下个编译轮自然触发（①断批偶发/⑤需 fail 案走归因触发锚门）——**触发才验、不强造场景**（老规矩：修复实弹验证才闭环，但不自然出现的条件触发项标「待触发」不误报未闭环为缺陷）。
- **诚实边界**：各件「落地」由对应角色（Theory/Design/Py-Eng/LLM-Eng）执行+四关，我只出**落地后下一轮的可观测验证点**并逐项对照记 ✅/⏳，不代验落地本身。
