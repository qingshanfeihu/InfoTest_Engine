# Test-Eng 复活首任务记录（TUI 重启 + 床态体检 + #55 准备）

> 2026-07-20 · Test-Eng（team4 复活编制）· HEAD=1e0e0298（#61）· 只读体检，未开任何批。

## 1. TUI 重启

| 项 | 值 |
|---|---|
| cmux surface | `surface:20` / UUID `1D30248F-976B-4BC2-B4BC-F910314AFD0D`（workspace:1，从 Test-Eng surface `1F3AF675…` new-split down） |
| infotest PID | **34041** |
| 载入 commit | HEAD=1e0e0298（#61 全角逗号 emit 归一化 + footprint surfacing） |
| 状态 | 输入提示符已就绪（read-screen 见「> 输入消息（/ 触发补全）」） |

## 2. 床态只读体检（10.4.127.103 跳板机）

**先记一个探针语义坑**：`_probe_stale_pytest(None)` 与 `_probe_device_reachable(None)` **都返回 `null`**，但读源码（`batch_tools.py:62-117`）确认 **None 是歧义值**——stale 探针 None = 「无残留」**或**「探测自身失败」；reachable 探针 None = 「探测失败」**或**「ping 输出不匹配两个判据」。**null 本身不构成「床干净」的证据**（护栏不是闸门，故意 fail-open）。因此拆开跑底层 SSH 取原始事实：

| 探测 | 原始输出 | 判定 |
|---|---|---|
| 跳板机 SSH `_connect(None)` | `SSH connect: OK` | ✅ 可达 |
| 残留 pytest（`ps … grep pytest \| grep ist_staging`） | 空 | ✅ **无残留** |
| 设备 ping（跳板机 → `APV_DEVICE_IP`，3 包） | `3 packets transmitted, 3 received, 0% packet loss` / rtt avg 4.0ms | ✅ **设备可达** |
| 跳板机 uptime | `up 89 days, load average: 0.16` | ✅ 空闲 |

**结论：床干净、设备活着、无并发残留，具备开批的物理条件**（放行令另议）。

> 证据边界：以上为**单时点**探测（2026-07-20 16:14 跳板机时间）。未探测设备配置基线面（`snapshot_only`），未验证框架 stdio server 就绪（`framework_ready`）——开批前若需，另跑。
> 噪声说明：脚本尾部 paramiko `BufferedFile.__del__` AttributeError 是解释器 teardown 时序噪声，不影响上述读数。

## 3. #55 准备清单

### 3.1 准入定谳（`team4_admission_report.md` §九）

- **ⓐ 按本报告准入**（2026-07-19 用户令「按照建议走」，团队自决制、用户否决权保留）。
- 表述 = **「特定域 L4」**（网关/SLB 配置验证域），**不主张 L5**。
- 验收口径：**「确认证据够格，非为缺陷背书」**——缺陷账/9 条未踩路径/9 条后批池持续记团队名下消化。
- 基线数字（照抄 engine_report totals）：**126 案编完，交付 105（83.3%）**，21 非交付案全有去向零静默丢失。

### 3.2 已准入 4 脑图（文件路径 + 规模）

| 脑图 | 路径 | cases | 交付 | 缺陷根 |
|---|---|---|---|---|
| CNAME_dongkl | `workspace/inputs/automatic_case/CNAME pool支持ipo算法_dongkl.txt` | 13 | 9 | 84938 |
| dongkl | `workspace/inputs/automatic_case/dongkl.txt` | 34 | 29 | 79993 |
| yzg | `workspace/inputs/automatic_case/yzg.txt` | 26 | 25 | 79758 |
| zhaiyq | `workspace/inputs/automatic_case/zhaiyq.txt` | 53 | 42 | 83112 |
| **合计** | | **126** | **105** | 4 根全覆盖 |

（另有 #54 校准脑图 `slbssl_calib.txt` 3 卷 / `slbssl_calib_003.txt` 1 卷——**校准物非真实脑图**。）

### 3.3 ⚠ 阻塞歧义：#55「真实脑图」指向未定，**待 leader 裁**

`workspace/inputs/automatic_case/` 下**只有 4 个已准入脑图 + 2 个 #54 校准脑图，无任何真实 SLB/SSL 脑图**。而 #54 收批结论是「SLB/SSL 两域 launch-ready，可 proceed #55 真实脑图放量」——两域指向与在库脑图不匹配。三种读法，工作量与产出完全不同：

- **读法 A（新域放量）**：#55 = 拿**真实 SLB/SSL 脑图**放量到 4 脑图同准入标准。→ **前置缺口：脑图不存在**，需用户提供或从 `knowledge/data/orgin/` 的 SLB/SSL 语料（`Test List_SLB Combination.xls`、`Test list_SLB_Connection_Table.xls`、`Test List_SIP SLB.xls` 等）转脑图。**这一步不是 Test-Eng 能自决的**（造脑图＝造测试意图，#54 FINDING #2 实证我造脑图会造错观测透镜）。
- **读法 B（存量救回）**：#55 = 跑准入后计划 §10 的「7 个可救回案换环境重调」+ 9 条残余路径定向造场景。→ 输入齐备，**今天就能开**。
- **读法 C（A+B）**：先 B 热身验证放量链路，再 A。

**我的建议＝C，且先跑 B**：B 输入齐备、零前置、直接消化准入后计划欠账，且能在真实放量前再验一次「attribution→repair 在换环境下工作」；A 的脑图来源问题同时上呈用户定夺（用户给真实 SLB/SSL 脑图，或授权按语料转）。

### 3.4 建议放量顺序与预计规模（若采纳 C）

1. **B-1 存量救回**：7 个可救回案（zhaiyq 5 escalated + 2 挂起，工程环境侧根因）——换环境/降并发重调。规模 ~7 案，1 批。
2. **B-2 残余路径**：9 条未踩交互路径定向造场景（P1-b 床停 / P2-e 等价 / P2-i env 确认 / P2-q Other 兜底 / P3-b 降级 / P3-c 改判等）——可搭 B-1 批顺手踩。
3. **A 新域放量**：待脑图落地后，**建议首批控规模 10-15 案**（对标 #54 的 3 案校准 → 一步到 50+ 风险过大；dongkl 34 案批曾烧 ¥320+/100M token），SLB 先行、SSL 次之（SSL 的 engine self-heal 刚坐实一例，样本 n=1）。

**未开任何批，等 leader 双保险放行令。**

---

# 附节·B 批备批清单（2026-07-20，用户裁 #55=只跑 B；A 新域放量挂起）

> 备批不开批。放行前置＝LLM-Eng 写回签名污染修复落地 + leader 核验 + 合入窗口 commit → 我重启 TUI 加载新代码（宽 pane ≥100 列、UUID 寻址）→ 双保险放行令。

## B-0 配置面与床态实测（只读，2026-07-20 16:2x）

| 项 | 实测值 | 来源 |
|---|---|---|
| `IST_MODEL` | **`deepseek-v4-pro`** ✅ 与 leader 所核一致，不改 | dotenv 加载后读 env |
| `IST_FLASH` | `deepseek-v4-flash` | 同上 |
| `IST_ENV_POOL_ENABLED` | **未设（池关，默认）** | 同上 |
| `load_environments()` | **只返回 `env-103` 一条** | `config.load_environments()` |

**池机就绪度实测**（`_DEFAULT_POOL_HOSTS` = 103/93/79/105，逐台 SSH 只读探）：

| 跳板机 | SSH | 框架 `server.py` | py38 | 残留 pytest | 自身设备床 ping `172.16.34.70` | 可用 |
|---|---|---|---|---|---|---|
| 10.4.127.103（现役） | OK | —（现役在跑） | — | 无 | **0% loss，avg 4.0ms** | ✅ |
| 10.4.127.93 | OK | 8148B, Jun 26 21:12 | ✅ | 无 | **0% loss，avg 4.4ms** | ✅ |
| 10.4.127.79 | OK | 8148B, Jun 26 21:13 | ✅ | 无 | **0% loss，avg 3.6ms** | ✅ |
| 10.4.127.105 | OK | 8148B, Jun 26 21:18 | ✅ | 无 | **100% packet loss, +3 errors** | ❌ **设备床死** |

**结论**：换环境**确实可用**（93 / 79 两张活床，框架已部署、无残留）；**105 设备床挂了**（跳板机活着但设备不可达）——池若开启需排除 105，否则 `env_pool.acquire()` 认领到它会烧一整个设备轮产 broken。

## B-1 七案具体化 ⚠ **「7 个可救回」需修正为「5 可救回 + 2 有前置」**

全 autoid = 前缀 `205271757988` + 尾 6（目录实证 `workspace/outputs/zhaiyq/unfinished/`，11 案齐）。

### 组① fork no-output 并发型（2 案）— 主修法＝降并发，非换床

| autoid | 尾6 | 原终局 | 重调方案 |
|---|---|---|---|
| `205271757988517027` | 517027 | escalated·fork no-output | **留 103，降并发**（根因在 fork 并发/墙钟，非床） |
| `205271757988600113` | 600113 | escalated·fork no-output | 同上 |

**为什么不换床**：这两案的 escalated 归因是 **fork no-output（引擎侧并发/墙钟）**，与设备床无因果。换床是无关变量；正确杠杆＝降 fanout 并发 + `IST_FORK_WALLCLOCK_S`（V8 fanout 特化 900，run18 有 935s 迟到产出佐证，可再放宽）。

### 组② broken+E 环境床（3 案）— 换床是对症的

| autoid | 尾6 | 原终局 | 重调方案 |
|---|---|---|---|
| `205271757988588766` | 588766 | escalated·broken+E 环境床 | **换 10.4.127.93** |
| `205271757988589503` | 589503 | escalated·broken+E 环境床 | **换 10.4.127.93** |
| `205271757988589432` | 589432 | escalated·broken+E 环境床 | **换 10.4.127.79**（与上二分床，隔离"是否床特异"） |

**为什么这样分床**：三案同批同床 broken+E，若全换同一张新床、结果又 broken，无法区分「原 103 床瞬态」与「三案本身有共性缺陷」。**588766/589503 → 93、589432 → 79 的二分**能在一批内拿到对照信号（跨床同 fail ⇒ 案侧根因；仅 103 fail ⇒ 原床瞬态坐实）。

### 组③ IPv6 两挂起（2 案）— ⚠ **换床救不回，本批建议不纳入**

| autoid | 尾6 | 原终局 | 我的判定 |
|---|---|---|---|
| `205271757988532349` | 532349 | 挂起·IPv6 无触发机真床限制 | **换床无效** |
| `205271757988532519` | 532519 | 挂起·IPv6 前缀 runtime 本体 | **换床无效** |

**两条证伪依据**：
1. **床是同拓扑克隆**：4 机共用 `knowledge/data/auto_env/network_topology.json`（CLAUDE.md 明载"设备床隔离克隆、地址相同"）。我实读该 JSON——clientC/clientD **有 IPv6 地址**（`3ffa::ac10:21d8/64`、`3ffa::ac10:21d9/64`）。即**拓扑本身不缺 IPv6 编址**，"无触发机"是**该拓扑共有的能力缺口**（缺 IPv6 流量发起手段），换到 93/79 是**同一份拓扑的克隆 ⇒ 同一缺口复现**。
2. **keep 类不自动 resume**：这 2 案是批末 gather 答"保持挂起"落 `suspended(keep:qid)`，`nodes.py:405-408` reopen 谓词对 `keep:` **不重开**（disposition CAVEAT + P2-m 实弹坐实）。**必须走 `user_decision` 更新显式重提**，不是"调批就回来"。

**建议**：组③本批**不纳入放量**。若 leader 仍要动，正确形态是**单独一个 1-2 案探针批**、目标改为**证伪"IPv6 限制是床级还是拓扑级"**（而非期待 PASS）——这是求知不是求 PASS，预期产出＝一条结论，不是一个交付卷。

> **证据边界**：以上 IPv6 判定基于「拓扑 JSON 编址 + 克隆声明 + 原批 attribution 文字」三项，**未实机验证 93/79 上 IPv6 触发能力**。若 leader 要更硬的结论，我可上 93 做一次 IPv6 连通性只读探（~5min）。

### B-1 批规模与时长预估

- **纳入 5 案**（组①2 + 组②3），**单批**。
- **编译**：5 案并发 fanout，首败即升 max 思考（`rounds_used≥1`）；单 fork 墙钟 900s（max 档 1200s）。预计 **2-3 轮**收敛。
- **上机**：整卷单跑 O(N)，超时 `clamp(max(5×45,600),600,2400)` = **600s/轮**；子集复测轮更短。含跨床，93/79 各一次。
- **预计总时长 1.5-3h**；成本量级参考 yzg 基线（编写 ¥15.7-41/批），**本批 5 案预计远低于 dongkl 34 案的 ¥320+**。

## B-2 九条残余路径·定向场景（各一句话）

| ID | 未踩内容 | 定向造场景（一句话） | 可造性 |
|---|---|---|---|
| **P1-b** | bed_gate 答"停止"→closing 修床同参重跑 | **把批指向 10.4.127.105**（实测设备床 100% 丢包）→ bed_gate 体检必报不可达 → 答"停止" → 观察 closing 修床后同参重跑 | ⭐ **天然场景已就位**（105 现成，零构造成本） |
| **P2-e** | test_point 采纳题答"我给别的等价方案" | 挑一个会触发采纳面板的案，面板出现时选"我给别的等价方案"并打自由文本，核 brief 是否注入 | 高（依赖面板自然弹出） |
| **P2-f** | 采纳题答"挂起，如实报告" | 同上面板，改选"挂起"→核落 `S_PENDING`（区别于 P2-l 的挂起处理族） | 高（同上） |
| **P2-i** | env 确认面板答"确认环境"→attribution(env_blocked) | 批跑中途人为使床不可达（或用 105 中途切入），使归因走 env 类并弹 env 确认面板 → 答"确认环境" | 中（需引擎真弹 env 面板，非 attribution 层归因——原 P2-i 自纠即栽在这） |
| **P2-j** | env 面板答"retry 不接受"→rerun_isolated | 同 P2-i 场景，改答 retry → 核是否走 `rerun_isolated` | 中（同上，依赖 P2-i 先能弹出） |
| **P2-k** | bed 床治理 token→床恢复/呈报 | 床污染态（残留 pytest 或配置漂移）下开批 → 床治理面板 → 答治理 token | 中（需先造床污染，**有风险，须 leader 单独批准**） |
| **P2-q** | Other 自由输入（token 空）→语义兜底 | **任一** ask 面板弹出时选 "Other" 打一句中文自由文本 → 核语义兜底是否正确解析 | ⭐ 高（任何面板都能搭，零额外构造） |
| **P3-b** | contra 答"如实降级"→不入交付卷 | 造终验与轮次结论矛盾（如同案跨轮 verdict 翻转）→ contra 面板 → 答"降级" → 核**不入**交付卷 | 低-中（矛盾需自然发生，人工造易失真） |
| **P3-c** | contra 答 confirm/correct→adjudication 写回 | 同 P3-b 场景，改答 confirm/correct → 核 adjudication 写回落盘 | 低-中（同上） |

**搭批策略**：**P1-b（105 天然）+ P2-q（任意面板搭 Other）** 是零成本高确定性两条，建议**必搭 B-1 批**；P2-e/P2-f 依赖采纳面板自然弹出，**机会主义搭**（弹到就踩）；**P2-i/j/k 与 P3-b/c 需人为制造异常态，建议不混进 B-1**（会污染组②的跨床对照信号），另开一个"路径补踩专批"，且 P2-k 造床污染须单独批准。

## B 批放行前 checklist（我的执行序）

1. ⏳ 等 LLM-Eng 写回签名污染修复落地 + leader 核验 + 合入窗口 commit。
2. ⏳ **重启 TUI 加载新代码**——新 pane **≥100 列宽**（TUI-Eng 实测 61 列布局降级、ask 面板折行伤 User 观察员主战场），cmux **一律 UUID 寻址**（索引号会 not found）。重启后报新 surface UUID + PID。
3. ⏳ 开批前重跑床态体检（103 + 目标 93/79），确认无残留、设备可达。
4. ⏳ 等双保险放行令 → 开 B-1（5 案）+ 搭 P1-b / P2-q。

---

# 附节二·床定向机制 setup + IPv6 实机探结论（2026-07-20，leader 三回执后）

## S-1 床定向机制：三选一实测后**只有一条可行**

| 候选机制 | 实测判定 | 依据 |
|---|---|---|
| **① `IST_JUMPHOST_HOST` 覆盖** | ✅ **唯一可行的确定性定向**，但**每换一张床 = 一次 TUI 重启** | `device_mcp_client.py:25` `JUMPHOST = os.environ.get("IST_JUMPHOST_HOST", _cfg.jumphost.host)` 是**模块级**、import 时求值——进程内改 env 不生效 |
| **② 开池 `IST_ENV_POOL_ENABLED=1`** | ❌ **不能定向**（只能并行，不能指定哪案跑哪床） | `env_pool.acquire()` 对 `ready` 列表**取第一个抢到锁的**（`env_pool.py:104-113`），认领是机会主义的，无法把 588766 钉到 93 |
| **③ `runtime/compiler_config.json` environments** | ◐ 能定义子集，但仍经 `acquire()` 认领 ⇒ 同②不能定向 | 同上 |

**⚡ 顺带实证：105 无需手工排除——池的 health-check 会自动剔它。** 我实跑了 `framework_ready()`：

```
env-103 = True (0.9s)   env-93 = True (0.8s)
env-79  = True (0.9s)   env-105 = False (3.8s)   ← 自动剔除
```

`framework_ready` 是 **device-aware** 的（源码注释明写"设备 down 则 False、池剔除它"），105 因设备 ping 不通被判 False。**所以 `IST_ENV_POOL_HOSTS` 排除 105 是双保险、非必需**。（但仍建议显式排除：`acquire()` 有一条 `if not ready: ready = envs[:1]` 的兜底回退，极端情况下不希望它把批甩回默认床。）

## S-2 批次切分（按机制①，每床一段）

| 段 | 床 | 案 | TUI 重启 | bed_gate 预期 |
|---|---|---|---|---|
| **B-1a** | **103**（现役，零改动） | `…517027` `…600113`（fork no-output，降并发） | 复用"写回修复后"那次重启，**零额外** | **通过**（实测 0% loss / 无残留） |
| **B-1b** | **93**（`IST_JUMPHOST_HOST=10.4.127.93`） | `…588766` `…589503` `…589432` **三案全上**（✅ leader 裁定，撤原二分） | **+1 次** | **通过**（实测 0% loss，框架已部署） |
| **B-1c**（条件触发） | **79** | **仅当 B-1b 三案全 fail** 时取 1 案做仲裁 | +1 次（多半用不上） | 通过 |
| **P1-b 路径踩点** | **105** | 不带案，只为踩 bed_gate 停止路 | 可搭 B-1b 段前后 | **必报设备不可达 → 答"停止" → closing 修床同参重跑** |

### ✅ 已裁：全上 93，撤原二分（leader 回执 2026-07-20）

**leader 裁定：撤销原二分设计，采纳自适应方案**——B-1b 三案全上 93；**79 保留为 B-1c 条件仲裁，仅在"全 fail"分支触发**。裁定理由：机制实测（定向＝改 env + 重启）改变了成本-信息比，自适应设计严格更优——全 PASS 即拿到目标信号且省掉 79。

下节为该修正的原始论证，存档备查。

### ⚠ 原始论证（我要修正一处你已批准的设计，不敢默改，请裁）

你批了组②**二分换床 588766→93 / 589503→93 / 589432→79**。实测机制后我建议**改为三案全上 93，79 降级为条件仲裁**，理由是**成本-信息比变了**：

- 定向只能靠"改 env + 重启 TUI"，**二分 = 强制 2 次额外重启**（93 一次、79 一次），而三案全上 93 只要 1 次。
- 信息几乎不损失：三案全上 93 后——**全 PASS ⇒ 103 床瞬态坐实**（想要的信号已到手，79 根本不用跑）；**全 fail ⇒ 案侧共性根因**，此时才需要 79 排除"两床同缺陷"这个小概率分支；**结果分裂 ⇒ 逐案看**，也不需要预先分床。
- 即：**79 的信息价值只在"全 fail"分支才兑现**，预付一次重启去买一个大概率用不上的对照，不划算。

**若你坚持原二分我照办**（多一次重启而已，不是原则问题），请给一句回执。

## S-3 IPv6 只读实机探（93）——⚡ **结论与预期相反，原挂起理由被推翻**

### 探测实录（jumphost 93，只读，零配置写）

| 探测项 | 结果 |
|---|---|
| jumphost 自身全局 IPv6 | 只有 `172:16:35::215/64`（**伪 v6 写法，模仿 v4 编址**，不属拓扑的 3ffa/3ffb/3ffc/3ffd 任一段） |
| `ping6` 二进制 | 存在（`/bin/ping6`） |
| ping APV **v4** `172.16.34.70` | ✅ 通（3.6ms） |
| **ping6 APV v6 `3ffc::70` / `3ffb::70`** | ❌ **`Network is unreachable`** |
| ping clientC **v4** `172.16.33.217` | ✅ 通（0.3ms） |
| ping6 clientC v6 `3ffa::ac10:21d8` | ❌ `Network is unreachable` |
| IPv6 路由表 | 只有 `::1` / `172:16:35::/64` / 四条 `fe80::/64` ——**无任何 3ffa/3ffb/3ffc/3ffd 路由** |

### ⚡ 但机读账把我引向了真相：原案的挂起理由**有一半是错的**

读 `unfinished/205271757988532349/needs_decision.json` 拿到 worker 原文 obstacle：

> 「测试床触发主机（routera/routerb）**仅记录IPv4地址，无IPv6地址文档**；dig触发配对表仅覆盖IPv4目标（如172.16.34.70→routera），**未记录IPv6目标（3ffc::70）的触发机配对**。」

**前半句经一手材料证伪**：`network_topology.json`（mtime **6月15**，早于 7月中下旬的 zhaiyq 批一个月）**和** `network_topology_rag.md` **都完整记录了 routerA/routerB 的 IPv6 地址**：

- routerA：`3ffa::ac10:21ce/64`、`3ffa::ac10:22ce/64`
- routerB：`3ffa::ac10:20d2/64`、`3ffb::ac10:20d2/64`
- APV0：`3ffb::70`、`3ffc::70`、`3ffd::70`

RAG 文档甚至有**专章「四、IPv6地址规划」**（分层表：3ffa 接入层含 routerA / 3ffb 核心层 / 3ffc 核心层 / 3ffd 服务器层）。**"无IPv6地址文档"是假的——文档一直在，worker 没找到/没用。**

**后半句（触发配对表无 v6 目标）大体成立**：`env_capabilities.json` 实测 **`has ipv6: False`**（全文无 ipv6），其 `test_env_hosts` 只说"主机名闭集见 EXCEL_FUNCTIONS.md、具体 IP 以 topology 为准"——**确实没有"v6 目标该用哪台触发机"的配对指引**。

### 判定：这是**知识层 gap，不是床能力 gap**（同 #54 SSL escalation 的类）

- 命中 CLAUDE.md 红线 #10「判『框架做不到』前先查是不是用错了已有能力」，也命中 #54 二阶教训「claim 可能误报，要回一手材料核」——**这次误报的是 worker 的 obstacle 陈述，我用 topology/RAG 两个一手源核穿了它**。
- **⚠ 我同时要自纠**：我上一版说「拓扑本身缺 IPv6 能力 ⇒ 换床必复现」——**这个理由是错的**，拓扑不缺 v6 编址，缺的是"v6 目标↔触发机"的配对投影。**结论（组③不进 B-1）不变，但理由必须换**。

### 诚实边界（这条我探不动，别当已证）

**"文档有 v6 编址" ≠ "routerA 真能发 v6 dig"。** 我的 93 探测只证明**跳板机自己**无 v6 路由到 3ff* 段——**但触发机是 routerA/routerB，不是跳板机**，跳板机没路由完全可能只是它不在那个 v6 平面上。**routerA 的实际 v6 发包能力仍未验证**，而验证它需要经框架下发一次真实 dig（＝上机轮），**超出只读范围、也超出我当前授权**。

### 由此给出的处置建议（替换我上一版的"换床无效"）

**532349/532519 的正确修法候选是知识层投影，不是换床**：把"v6 目标 ↔ 触发机配对"补进可达投影（`env_capabilities.json` 或 `compile_ref/` 投影，走自愈四层的文法/判例层），让 worker 能查到"要打 `3ffc::70` 该从 routerA 发"。**成本远低于换床，且可能一次修好两案。**

**但落地前必须先花一个上机轮证实 routerA 的 v6 发包能力**——否则补了投影、worker 照着打、仍 `Network is unreachable`，等于把知识层 gap 换成一轮白烧。**建议：这一个探针轮（1 案、只验 `dig -6 @3ffc::70` 能否从 routerA 发出）单独立项，不进 B-1**，与你已裁的"IPv6 床级 vs 拓扑级探针批留档"合并——**且它现在有了更具体的目标：不是探床，是探 routerA 的 v6 发包能力 + 验证配对投影修法**。

### ✅ 已裁（leader 回执 2026-07-20）

- **IPv6 探针批新目标：准**——从「探床」改为「**探触发机 v6 发包能力 + 验证配对投影修法**」（1 案 `dig -6`），**排 B-1 之后**。
  - ⚠ **触发机对象后经 LLM-Eng 纠偏为 `routerb` → `3ffb::70`**（routerA 与任何 APV 无共享 v6 段，验了没用）——详见下文 **U-1**，以 U-1 为准。
- **我的自纠按新版记档**：「拓扑缺 v6 能力 ⇒ 换床必复现」**作废、原话不留**；正式理由＝**缺"v6 目标↔触发机"配对投影**（知识层 gap，非床能力 gap）。
- **`env_capabilities.has_ipv6=False` 与拓扑 JSON 有完整 v6 编址的矛盾**已由 leader 转 **LLM-Eng 查投影链**，修法形状由其出（不在我 scope）。
- **`IST_ENV_POOL_HOSTS` 仍显式排除 105**（防 `acquire()` 的 `ready=envs[:1]` 兜底回退甩批），105 池自动剔的实证一并收录。

---

# 备批定谳（2026-07-20）

**leader 判定：S-1/S-2/S-3 齐，B-1 计划完备。** 唯一剩余等待项＝**unit-1（写回签名污染）修复 commit**。

## 待命执行序（放行后按序走，不跳步）

1. ⏳ **等 unit-1 修复 commit**（LLM-Eng 落地 → leader 核验 → 合入窗口）。
2. **重启 TUI 加载新代码**——新 pane **≥100 列宽**（TUI-Eng 实测 61 列布局降级、ask 面板折行伤 User 观察员主战场），cmux **一律 UUID 寻址**（索引号 not found）。**重启后报新 surface UUID + PID。**
3. **开批前床态体检**：103 + 93（+79 备）——无残留 pytest、设备可达。
4. **等双保险放行令** → 开 **B-1a（103：517027 / 600113，降并发）**。
5. **改 `IST_JUMPHOST_HOST=10.4.127.93` + 重启 TUI** → **B-1b（93：588766 / 589503 / 589432 三案）**。
6. **搭踩 P1-b**（批指向 105，bed_gate 必报不可达 → 答"停止" → 核 closing 修床同参重跑）+ **P2-q**（任一 ask 面板选 Other 打一句中文，核语义兜底）。
7. **条件分支**：B-1b 三案全 fail → 才起 B-1c（79，1 案仲裁）。
8. **B-1 之后**：IPv6 探针批（1 案，**`routerb` 对 `3ffb::70` 发 `dig -6`**——见 U-1 纠偏；**前置＝`env_facts.py` v4-only 派生器修法先落**）。

**缓办/不办**：「路径补踩专批」（P2-i/j/k、P3-b/c）leader 裁**缓**；**P2-k 造床污染 leader 不批，留用户亲裁——我不自行造**。A 新域放量挂起。

## ⚡ 更新（leader 2026-07-20，两条）

### U-1 IPv6 探针目标纠偏：**routerA → routerB**，我的原设计选错了触发机

LLM-Eng 从拓扑 CIDR 机械派生，纠正我的探针对象：

- **routerA 与任何 APV 无共享 v6 网段** ⇒ **「验 routerA 能发 v6」验了也没用**。（routerA 在 `3ffa::/64`，该段无 APV；APV 在 `3ffb/3ffc/3ffd`，其中 `3ffc`/`3ffd` 段无触发机。）
- **唯一可用配对 = routerB ↔ `3ffb::70/71`**——`3ffb::/64` 段成员 = routerB + APV0 + APV1，是**唯一同时含触发机与 APV 的 v6 段**。
- **探针轮改为**：**`routerb`（小写）对 `3ffb::70` 发 `dig -6`**。
- **532519 是否同因，我救回前先从机读账确认**（不假定它与 532349 同根）。

**投影根因（LLM-Eng 定）**：`env_facts.py` 派生器 **v4-only** ⇒ 投出去的配对表只有 v4。**worker 是被"权威口吻的 v4-only 配对表"骗了，不是它不查**——这比我原先的判断更准：我说"文档一直在、worker 没找到/没用"，实际是**投影层给了它一张自称完整的 v4-only 表**，worker 依权威表作答是合理行为。修法＝**代码非数据**（~4 处），已出规格，**排下个窗口、探针批之前落**。

> 自我校准记一笔：我用 topology/RAG 证伪了"无 v6 文档"这半句（成立），但**没往下追一层问"那 worker 到底看到的是什么"**——答案是投影表，不是原始拓扑。**"原始事实源有" ≠ "投影给 worker 的有"**，这是 #58 mirror dead pointer 的同型教训再现（worker 只能看见投影）。

### U-2 ASK-1 驱动纪律（放行令正文含，写进操作卡）

**多题面板出现「还有 N 题未答」挡板告警后，只用 `↑↓` + `enter` 落答，永不裸数字。**

- **挡板 armed 时按数字会提交旧选择、静默丢弃新意图 ＝ 错答实锤**（TUI-Eng 实证）。
- 这是常态下「数字只高亮不选中」语义在挡板态的**反转**，两条不可混用。
- **比丢答案更坏**：丢答案还能靠 echo 行数点出来，错答会留下一个看起来正常的错误裁决。
- 已同步进我的长期记忆 `tui-multiquestion-panel-key-semantics`。

## 终检三铁律挂钩（B 批跑完必对的账）

- **裁决执行链对账**：decision→authored→verdict→终局，链断即三分判据。
- **引擎自认异常字段零放过**：`effective=false` 等一律不放。
- **该交付而没交付的比交付了什么更要查**。
- **signal 核查先看 `runtime/logs/k_signals.jsonl`**（#61 实证：`fullwidth_comma_normalized` 只落这里，grep fastlog 是假阴性）。
- **归因姿势**：读 `needs_decision.json` 机读账拿 claim → **但 claim 可能误报/陈旧，device error 才是 ground truth**（#54 + #58 两条教训；本轮 IPv6 又添一例——**worker 的 obstacle 陈述同样可能是假的，要回一手材料核**）。

