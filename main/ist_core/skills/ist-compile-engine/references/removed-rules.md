# Rules removed from v6 prompts (less-is-more discipline, 2026-07-10)

Each line: the removed rule → why it no longer earns prompt residency. A rule returns only with
fresh evidence of the failure mode under the current regime (thinking models + gates + healthy K).

## From compile-worker.md (137 → ~75 lines)

- Frozen-override "never delete failing coverage" narrative (AAAA incident story) → the
  monotonicity gate enforces it mechanically; the gate's violation message is the
  just-in-time teaching. Rule kept as one line; incident narrative removed.
- dev_probe strategy paragraph ("turn dynamic behavior into stable text") → lives in the
  tool's own docstring (taught at retrieval time).
- Precedent verbatim-copy lecture (quoting/parameter-order discipline) → compile_precedent's
  return carries it at retrieval time; duplicated residency removed.
- CNAME alias-pool over-config story (three-round measured) → condensed to the one-line rule
  "every config element traces to intent"; narrative moved to case law (footprint).
- user-decision redo form details (dist/member/captured_relation mapping) → the emit gate
  cross-checks mechanically; enum details live in the questions template + gate message.
- desc-column example triplet trimmed to essentials (delivery contract kept, examples kept
  minimal per official guidance).
- Old-model defensive repetitions ("do not fabricate", duplicated red-line restatements) →
  covered once in <rules>; thinking models do not need triple restatement.

## From compile-attributor.md (71 → ~88 lines net, but v6 content restructured)

- Four-layer 保留:all sections carried current-regime measured anchors — trimmed wording only.
- Added (not removed): version layer as step 1 (2/2 vs 0/17 natural experiment);
  cross-case consistency duty; contradiction/rerun_isolated handling.

## From ist-compile-engine SKILL.md (rewritten at switch)

- Streaming-truncation relay warnings (deepseek era) → keep one line "short summary + report
  path"; the multi-paragraph rationale removed.
- Escalation relay micro-choreography → engine report now carries the evidence structure;
  relay discipline is one line.
