# Create root "environment" from environment.example before run, or export vars
requests>=2.28.0,<3
python-dotenv>=1.0.0,<2
httpx>=0.27,<1                         # §15 VPN 探测
paramiko>=3.4,<4                       # SSH 远程连接（脚本自动化）
# LangChain / Anthropic / OpenAI 兼容（IST-Core agent 内核）
langchain>=1.2.15,<2
langchain-core>=1.2.27,<2
langchain-community>=0.3.20,<1
langchain-openai>=1.0,<2              # IST-Core openai:qwen3.6-plus DashScope 兼容路径
# 百炼 Qwen Anthropic 兼容端点（让 deepagents 0.5.7 Anthropic-first middleware 激活）
langchain-anthropic>=1.4,<2
# 修 LLM 输出裸 JSON 含未转义嵌套引号
json-repair>=0.30
langsmith>=0.3.0,<1
dashscope>=1.14.0
# LangGraph（解锁 astream_events v2 + SqliteSaver）
langgraph>=1.1,<2
langgraph-checkpoint>=4,<5
langgraph-checkpoint-sqlite>=3,<4
aiosqlite>=0.20,<1                    # AsyncSqliteSaver（TUI astream_events 必需）
# Deep Agents：主 Agent 内核（要求 Python >= 3.11）
deepagents>=0.5.3,<0.6
# Product testing platform runtime state store + LangGraph Postgres checkpointer.
psycopg[binary,pool]>=3.1,<4
langgraph-checkpoint-postgres>=3,<4
# Langfuse：LLM 可观测性追踪（自建实例）— 暂时禁用
# langfuse>=2.0,<3
# Schema / Prompt
pydantic>=2.6,<3
jinja2>=3,<4
pyyaml>=6,<7
# xlsx 解析（main/xlsx_to_markdown + file_tools._read_spreadsheet）
openpyxl>=3.1,<4
# 旧 .xls 兜底（xlrd 2.0+ 已只支持 .xls）
xlrd>=2.0,<3
# §15 HTML 抓取管线（缺陷库 v1.5.1 真实 WebVPN 对接）
playwright>=1.46,<2
beautifulsoup4>=4.12,<5
lxml>=5,<7
# 验证码 OCR（scripts/*_capture.py + _capture_session.py 使用；未安装时自动退化到手动输入）
ddddocr>=1.4,<2
pillow>=10,<12
# CLI 渲染
rich>=13,<15
# Textual TUI (infotest 命令入口)
textual>=0.85,<1
# qa_search_knowledge_ref 的 web fallback（langchain_community DuckDuckGo wrapper）
duckduckgo-search>=6.0.0
ddgs>=2.0.0

# Tests
pytest>=8,<10
pytest-asyncio>=0.23,<1
pytest-textual-snapshot>=0.4,<2

# Web Terminal
fastapi>=0.110,<1
uvicorn>=0.27,<1
python-multipart>=0.0.9,<1
pikepdf>=10
